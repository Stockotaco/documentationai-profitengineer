openapi: 3.0.3
info:
  title: Mythic OS API
  version: 1.0.0
  description: The REST surface of the Mythic OS client API. Every endpoint takes an API key from your
    portal.
servers:
- url: https://api-service-production-4d43.up.railway.app
components:
  securitySchemes:
    bearerAuth:
      type: http
      scheme: bearer
      description: 'An API key from your portal, sent as `Authorization: Bearer secret_...`.'
security:
- bearerAuth: []
paths:
  /v1/alerts:
    get:
      operationId: listAlerts
      summary: List this client's alert rules
      description: 'Returns the authenticated client''s alert rules (summary fields only), newest first.
        Required scope: `alerts:read` OR `alerts:write` (requireAnyScope). Auth: API key; agency-wide
        keys must send X-Client-Id.'
      tags:
      - alerts
      parameters:
      - name: X-Client-Id
        in: header
        required: false
        description: Target client id, slug, or name. Required for agency-wide API keys.
        schema:
          type: string
      responses:
        '200':
          description: Alert rule summaries
          content:
            application/json:
              schema:
                type: object
                required:
                - alerts
                properties:
                  alerts:
                    type: array
                    items:
                      type: object
                      properties:
                        id:
                          type: string
                        name:
                          type: string
                        description:
                          type: string
                          nullable: true
                        clientId:
                          type: string
                        status:
                          type: string
                          enum:
                          - active
                          - paused
                          - error
                        lastRunAt:
                          type: string
                          format: date-time
                          nullable: true
                        lastTriggeredAt:
                          type: string
                          format: date-time
                          nullable: true
                        lastError:
                          type: string
                          nullable: true
                        conditionOperator:
                          type: string
                          nullable: true
                          enum:
                          - greater_than
                          - less_than
                          - equals
                          - not_equals
                          - greater_than_or_equal
                          - less_than_or_equal
                          - changed_by_percent
                          - ai_analysis
                        conditionCombinator:
                          type: string
                          enum:
                          - and
                          - or
                        scheduleType:
                          type: string
                          enum:
                          - interval
                          - cron
                        createdAt:
                          type: string
                          format: date-time
        '400':
          description: Agency-wide key sent no X-Client-Id header
          content:
            application/json:
              schema:
                type: object
                properties:
                  error:
                    type: string
                  message:
                    type: string
        '401':
          description: Missing or invalid API key
          content:
            application/json:
              schema:
                type: object
                properties:
                  error:
                    type: string
                  message:
                    type: string
        '403':
          description: API key lacks the required scope
          content:
            application/json:
              schema:
                type: object
                properties:
                  error:
                    type: string
                  message:
                    type: string
        '404':
          description: No client in this agency matches X-Client-Id
          content:
            application/json:
              schema:
                type: object
                properties:
                  error:
                    type: string
                  message:
                    type: string
        '409':
          description: X-Client-Id name is ambiguous across multiple clients
          content:
            application/json:
              schema:
                type: object
                properties:
                  error:
                    type: string
                  message:
                    type: string
        '429':
          description: Rate limit exceeded
          content:
            application/json:
              schema:
                type: object
                properties:
                  error:
                    type: string
                  message:
                    type: string
                  retryAfter:
                    type: integer
    post:
      operationId: createAlert
      summary: Create an alert rule with conditions and optional inline actions
      description: 'Validates the BigQuery SQL against the client''s dataset allowlist, rejects webhook
        URLs pointed at internal hosts, and checks that any Slack actions are reachable before persisting.
        At least one condition is required, supplied either as `conditions[]` or via the legacy single-condition
        fields (`conditionOperator` + `resultColumn`). Required scope: `alerts:write` (requireScope).'
      tags:
      - alerts
      parameters:
      - name: X-Client-Id
        in: header
        required: false
        description: Target client id, slug, or name. Required for agency-wide API keys.
        schema:
          type: string
      requestBody:
        required: true
        content:
          application/json:
            schema:
              type: object
              required:
              - name
              - bigqueryQuery
              properties:
                name:
                  type: string
                  minLength: 1
                description:
                  type: string
                  nullable: true
                bigqueryQuery:
                  type: string
                  minLength: 1
                scheduleType:
                  type: string
                  enum:
                  - interval
                  - cron
                  default: interval
                intervalMinutes:
                  type: integer
                  minimum: 1
                  default: 60
                cronExpression:
                  type: string
                  nullable: true
                timezone:
                  type: string
                  default: UTC
                lookbackMinutes:
                  type: integer
                  minimum: 1
                  default: 1440
                conditionCombinator:
                  type: string
                  enum:
                  - and
                  - or
                  default: and
                conditions:
                  type: array
                  minItems: 1
                  description: Multi-condition form. `ai_analysis` conditions require `conditionValue`
                    (the instruction text); all other operators require `resultColumn`.
                  items:
                    type: object
                    required:
                    - operator
                    properties:
                      operator:
                        type: string
                        enum:
                        - greater_than
                        - less_than
                        - equals
                        - not_equals
                        - greater_than_or_equal
                        - less_than_or_equal
                        - changed_by_percent
                        - ai_analysis
                      resultColumn:
                        type: string
                        nullable: true
                      conditionValue:
                        type: string
                        nullable: true
                      compareToColumn:
                        type: string
                        nullable: true
                      aiModel:
                        type: string
                        nullable: true
                      agentId:
                        type: string
                        nullable: true
                resultColumn:
                  type: string
                  nullable: true
                  description: Legacy single-condition field (mirrors condition
                conditionOperator:
                  type: string
                  description: Legacy single-condition field (mirrors condition
                  enum:
                  - greater_than
                  - less_than
                  - equals
                  - not_equals
                  - greater_than_or_equal
                  - less_than_or_equal
                  - changed_by_percent
                  - ai_analysis
                conditionValue:
                  type: string
                  nullable: true
                  description: Legacy single-condition field (mirrors condition
                compareToColumn:
                  type: string
                  nullable: true
                  description: Legacy single-condition field (mirrors condition
                aiModel:
                  type: string
                  nullable: true
                  description: Legacy single-condition field (mirrors condition
                actions:
                  type: array
                  items:
                    type: object
                    required:
                    - actionType
                    properties:
                      actionType:
                        type: string
                        enum:
                        - email
                        - webhook
                        - slack
                      emailRecipients:
                        type: string
                        nullable: true
                        description: JSON array of email addresses.
                      emailSubjectTemplate:
                        type: string
                        nullable: true
                      emailBodyTemplate:
                        type: string
                        nullable: true
                      webhookUrl:
                        type: string
                        format: uri
                        nullable: true
                      webhookMethod:
                        type: string
                        default: POST
                      webhookHeaders:
                        type: string
                        nullable: true
                        description: JSON object of headers.
                      webhookBodyTemplate:
                        type: string
                        nullable: true
                      slackConnectionId:
                        type: string
                        nullable: true
                      slackChannelId:
                        type: string
                        nullable: true
                        description: Required when actionType is `slack`.
                      slackChannelName:
                        type: string
                        nullable: true
                      slackMessageTemplate:
                        type: string
                        nullable: true
                      isEnabled:
                        type: boolean
                        default: true
      responses:
        '201':
          description: Alert created
          content:
            application/json:
              schema:
                type: object
                properties:
                  alert:
                    type: object
                    description: Full alert_rules row.
                    properties:
                      id:
                        type: string
                      clientId:
                        type: string
                      name:
                        type: string
                      description:
                        type: string
                        nullable: true
                      bigqueryQuery:
                        type: string
                      resultColumn:
                        type: string
                        nullable: true
                      scheduleType:
                        type: string
                        enum:
                        - interval
                        - cron
                      intervalMinutes:
                        type: integer
                        nullable: true
                      cronExpression:
                        type: string
                        nullable: true
                      timezone:
                        type: string
                        nullable: true
                      lookbackMinutes:
                        type: integer
                      conditionOperator:
                        type: string
                        nullable: true
                      conditionValue:
                        type: string
                        nullable: true
                      compareToColumn:
                        type: string
                        nullable: true
                      aiModel:
                        type: string
                        nullable: true
                      conditionCombinator:
                        type: string
                        enum:
                        - and
                        - or
                      status:
                        type: string
                        enum:
                        - active
                        - paused
                        - error
                      lastRunAt:
                        type: string
                        format: date-time
                        nullable: true
                      lastTriggeredAt:
                        type: string
                        format: date-time
                        nullable: true
                      lastError:
                        type: string
                        nullable: true
                      consecutiveErrors:
                        type: integer
                      createdAt:
                        type: string
                        format: date-time
                      updatedAt:
                        type: string
                        format: date-time
                  actions:
                    type: array
                    items:
                      type: object
                      description: Full alert_actions row.
                      properties:
                        id:
                          type: string
                        alertRuleId:
                          type: string
                        actionType:
                          type: string
                          enum:
                          - email
                          - webhook
                          - slack
                        emailRecipients:
                          type: string
                          nullable: true
                        emailSubjectTemplate:
                          type: string
                          nullable: true
                        emailBodyTemplate:
                          type: string
                          nullable: true
                        webhookUrl:
                          type: string
                          nullable: true
                        webhookMethod:
                          type: string
                          nullable: true
                        webhookHeaders:
                          type: string
                          nullable: true
                        webhookBodyTemplate:
                          type: string
                          nullable: true
                        slackConnectionId:
                          type: string
                          nullable: true
                        slackChannelId:
                          type: string
                          nullable: true
                        slackChannelName:
                          type: string
                          nullable: true
                        slackMessageTemplate:
                          type: string
                          nullable: true
                        isEnabled:
                          type: boolean
                        createdAt:
                          type: string
                          format: date-time
                        updatedAt:
                          type: string
                          format: date-time
                  conditions:
                    type: array
                    items:
                      type: object
                      description: Full alert_conditions row.
                      properties:
                        id:
                          type: string
                        alertRuleId:
                          type: string
                        position:
                          type: integer
                        operator:
                          type: string
                          enum:
                          - greater_than
                          - less_than
                          - equals
                          - not_equals
                          - greater_than_or_equal
                          - less_than_or_equal
                          - changed_by_percent
                          - ai_analysis
                        resultColumn:
                          type: string
                          nullable: true
                        conditionValue:
                          type: string
                          nullable: true
                        compareToColumn:
                          type: string
                          nullable: true
                        aiModel:
                          type: string
                          nullable: true
                        agentId:
                          type: string
                          nullable: true
                        createdAt:
                          type: string
                          format: date-time
                        updatedAt:
                          type: string
                          format: date-time
        '400':
          description: Request body failed validation, the query referenced unauthorized tables, a webhook
            URL pointed at an internal host, a Slack action was unreachable, or an agency key sent no
            X-Client-Id.
          content:
            application/json:
              schema:
                type: object
                properties:
                  error:
                    type: string
        '401':
          description: Missing or invalid API key
          content:
            application/json:
              schema:
                type: object
                properties:
                  error:
                    type: string
                  message:
                    type: string
        '403':
          description: API key lacks the `alerts:write` scope
          content:
            application/json:
              schema:
                type: object
                properties:
                  error:
                    type: string
                  message:
                    type: string
        '404':
          description: No client in this agency matches X-Client-Id
          content:
            application/json:
              schema:
                type: object
                properties:
                  error:
                    type: string
                  message:
                    type: string
        '429':
          description: Rate limit exceeded
          content:
            application/json:
              schema:
                type: object
                properties:
                  error:
                    type: string
                  message:
                    type: string
                  retryAfter:
                    type: integer
        '500':
          description: Failed to create alert
          content:
            application/json:
              schema:
                type: object
                properties:
                  error:
                    type: string
                    example: Failed to create alert
  /v1/alerts/preview-query:
    post:
      operationId: previewAlertQuery
      summary: Validate an alert query and preview its first rows
      description: 'Validates the SQL against the client''s dataset allowlist, then runs it capped at
        10 rows through the configured BigQuery MCP tool. Required scope: `alerts:read` OR `alerts:write`
        (requireAnyScope).'
      tags:
      - alerts
      parameters:
      - name: X-Client-Id
        in: header
        required: false
        description: Target client id, slug, or name. Required for agency-wide API keys.
        schema:
          type: string
      requestBody:
        required: true
        content:
          application/json:
            schema:
              type: object
              required:
              - query
              properties:
                query:
                  type: string
                  minLength: 1
      responses:
        '200':
          description: Preview rows (raw BigQuery result rows, shape depends on the query)
          content:
            application/json:
              schema:
                type: object
                required:
                - results
                properties:
                  results:
                    type: array
                    items:
                      type: object
                      additionalProperties: true
        '400':
          description: Query validation failed, or request body invalid
          content:
            application/json:
              schema:
                type: object
                properties:
                  error:
                    type: string
                  validation:
                    type: object
                    properties:
                      valid:
                        type: boolean
                      unauthorizedTables:
                        type: array
                        items:
                          type: string
                      referencedTables:
                        type: array
                        items:
                          type: string
                      reason:
                        type: string
        '401':
          description: Missing or invalid API key
          content:
            application/json:
              schema:
                type: object
                properties:
                  error:
                    type: string
                  message:
                    type: string
        '403':
          description: API key lacks the required scope
          content:
            application/json:
              schema:
                type: object
                properties:
                  error:
                    type: string
                  message:
                    type: string
        '429':
          description: Rate limit exceeded
          content:
            application/json:
              schema:
                type: object
                properties:
                  error:
                    type: string
                  message:
                    type: string
                  retryAfter:
                    type: integer
        '500':
          description: Query execution failed (message is the thrown error text)
          content:
            application/json:
              schema:
                type: object
                properties:
                  error:
                    type: string
  /v1/alerts/{id}:
    parameters:
    - name: id
      in: path
      required: true
      description: Alert rule id.
      schema:
        type: string
    - name: X-Client-Id
      in: header
      required: false
      description: Target client id, slug, or name. Required for agency-wide API keys.
      schema:
        type: string
    get:
      operationId: getAlert
      summary: Get one alert rule with its actions and conditions
      description: 'Returns the full alert_rules row plus its actions and conditions (position-ordered).
        Legacy rules with no condition rows have their single condition synthesized from the legacy columns.
        Required scope: `alerts:read` OR `alerts:write` (requireAnyScope).'
      tags:
      - alerts
      responses:
        '200':
          description: Alert with actions and conditions
          content:
            application/json:
              schema:
                type: object
                properties:
                  alert:
                    type: object
                    description: Full alert_rules row (see createAlert 201 for fields).
                    additionalProperties: true
                  actions:
                    type: array
                    items:
                      type: object
                      description: Full alert_actions row.
                      additionalProperties: true
                  conditions:
                    type: array
                    items:
                      type: object
                      description: Full alert_conditions row.
                      additionalProperties: true
        '401':
          description: Missing or invalid API key
          content:
            application/json:
              schema:
                type: object
                properties:
                  error:
                    type: string
                  message:
                    type: string
        '403':
          description: API key lacks the required scope
          content:
            application/json:
              schema:
                type: object
                properties:
                  error:
                    type: string
                  message:
                    type: string
        '404':
          description: Alert not found (or not owned by the resolved client)
          content:
            application/json:
              schema:
                type: object
                properties:
                  error:
                    type: string
                    example: Alert not found
        '429':
          description: Rate limit exceeded
          content:
            application/json:
              schema:
                type: object
                properties:
                  error:
                    type: string
                  message:
                    type: string
                  retryAfter:
                    type: integer
    put:
      operationId: updateAlert
      summary: Update an alert rule
      description: 'Partial update; only supplied keys are written. Supplying `conditions` replaces the
        rule''s conditions wholesale; supplying only the legacy single-condition fields keeps the position-0
        condition row in sync. A new `bigqueryQuery` is re-validated against the client''s dataset allowlist.
        Required scope: `alerts:write` (requireScope).'
      tags:
      - alerts
      requestBody:
        required: true
        content:
          application/json:
            schema:
              type: object
              description: All fields optional (partial of the create schema, minus `actions`).
              properties:
                name:
                  type: string
                  minLength: 1
                description:
                  type: string
                  nullable: true
                bigqueryQuery:
                  type: string
                  minLength: 1
                scheduleType:
                  type: string
                  enum:
                  - interval
                  - cron
                intervalMinutes:
                  type: integer
                  minimum: 1
                cronExpression:
                  type: string
                  nullable: true
                timezone:
                  type: string
                lookbackMinutes:
                  type: integer
                  minimum: 1
                conditionCombinator:
                  type: string
                  enum:
                  - and
                  - or
                conditions:
                  type: array
                  minItems: 1
                  items:
                    type: object
                    required:
                    - operator
                    properties:
                      operator:
                        type: string
                        enum:
                        - greater_than
                        - less_than
                        - equals
                        - not_equals
                        - greater_than_or_equal
                        - less_than_or_equal
                        - changed_by_percent
                        - ai_analysis
                      resultColumn:
                        type: string
                        nullable: true
                      conditionValue:
                        type: string
                        nullable: true
                      compareToColumn:
                        type: string
                        nullable: true
                      aiModel:
                        type: string
                        nullable: true
                      agentId:
                        type: string
                        nullable: true
                resultColumn:
                  type: string
                  nullable: true
                conditionOperator:
                  type: string
                  enum:
                  - greater_than
                  - less_than
                  - equals
                  - not_equals
                  - greater_than_or_equal
                  - less_than_or_equal
                  - changed_by_percent
                  - ai_analysis
                conditionValue:
                  type: string
                  nullable: true
                compareToColumn:
                  type: string
                  nullable: true
                aiModel:
                  type: string
                  nullable: true
      responses:
        '200':
          description: Updated alert row
          content:
            application/json:
              schema:
                type: object
                properties:
                  alert:
                    type: object
                    description: Full alert_rules row (see createAlert 201 for fields).
                    additionalProperties: true
        '400':
          description: Body failed validation, or the new query referenced unauthorized tables
          content:
            application/json:
              schema:
                type: object
                properties:
                  error:
                    type: string
        '401':
          description: Missing or invalid API key
          content:
            application/json:
              schema:
                type: object
                properties:
                  error:
                    type: string
                  message:
                    type: string
        '403':
          description: API key lacks the `alerts:write` scope
          content:
            application/json:
              schema:
                type: object
                properties:
                  error:
                    type: string
                  message:
                    type: string
        '404':
          description: Alert not found (or not owned by the resolved client)
          content:
            application/json:
              schema:
                type: object
                properties:
                  error:
                    type: string
                    example: Alert not found
        '429':
          description: Rate limit exceeded
          content:
            application/json:
              schema:
                type: object
                properties:
                  error:
                    type: string
                  message:
                    type: string
                  retryAfter:
                    type: integer
        '500':
          description: Failed to update alert
          content:
            application/json:
              schema:
                type: object
                properties:
                  error:
                    type: string
                    example: Failed to update alert
    delete:
      operationId: deleteAlert
      summary: Delete an alert rule
      description: 'Deletes the rule; its actions, conditions, and history cascade. Required scope: `alerts:write`
        (requireScope).'
      tags:
      - alerts
      responses:
        '200':
          description: Deleted
          content:
            application/json:
              schema:
                type: object
                properties:
                  success:
                    type: boolean
                    example: true
        '401':
          description: Missing or invalid API key
          content:
            application/json:
              schema:
                type: object
                properties:
                  error:
                    type: string
                  message:
                    type: string
        '403':
          description: API key lacks the `alerts:write` scope
          content:
            application/json:
              schema:
                type: object
                properties:
                  error:
                    type: string
                  message:
                    type: string
        '404':
          description: Alert not found (or not owned by the resolved client)
          content:
            application/json:
              schema:
                type: object
                properties:
                  error:
                    type: string
                    example: Alert not found
        '429':
          description: Rate limit exceeded
          content:
            application/json:
              schema:
                type: object
                properties:
                  error:
                    type: string
                  message:
                    type: string
                  retryAfter:
                    type: integer
  /v1/alerts/{id}/actions:
    post:
      operationId: addAlertAction
      summary: Add an action to an alert rule
      description: 'Rejects webhook URLs pointed at internal/private hosts and Slack actions whose channel
        is not reachable by the resolved client (agency-wide keys may use agency-wide Slack connections).
        Required scope: `alerts:write` (requireScope).'
      tags:
      - alerts
      parameters:
      - name: id
        in: path
        required: true
        description: Alert rule id.
        schema:
          type: string
      - name: X-Client-Id
        in: header
        required: false
        description: Target client id, slug, or name. Required for agency-wide API keys.
        schema:
          type: string
      requestBody:
        required: true
        content:
          application/json:
            schema:
              type: object
              required:
              - actionType
              properties:
                actionType:
                  type: string
                  enum:
                  - email
                  - webhook
                  - slack
                emailRecipients:
                  type: string
                  nullable: true
                  description: JSON array of email addresses.
                emailSubjectTemplate:
                  type: string
                  nullable: true
                emailBodyTemplate:
                  type: string
                  nullable: true
                webhookUrl:
                  type: string
                  format: uri
                  nullable: true
                webhookMethod:
                  type: string
                  default: POST
                webhookHeaders:
                  type: string
                  nullable: true
                  description: JSON object of headers.
                webhookBodyTemplate:
                  type: string
                  nullable: true
                slackConnectionId:
                  type: string
                  nullable: true
                slackChannelId:
                  type: string
                  nullable: true
                  description: Required when actionType is `slack`.
                slackChannelName:
                  type: string
                  nullable: true
                slackMessageTemplate:
                  type: string
                  nullable: true
                isEnabled:
                  type: boolean
                  default: true
      responses:
        '201':
          description: Action created
          content:
            application/json:
              schema:
                type: object
                properties:
                  action:
                    type: object
                    description: Full alert_actions row.
                    properties:
                      id:
                        type: string
                      alertRuleId:
                        type: string
                      actionType:
                        type: string
                        enum:
                        - email
                        - webhook
                        - slack
                      emailRecipients:
                        type: string
                        nullable: true
                      emailSubjectTemplate:
                        type: string
                        nullable: true
                      emailBodyTemplate:
                        type: string
                        nullable: true
                      webhookUrl:
                        type: string
                        nullable: true
                      webhookMethod:
                        type: string
                        nullable: true
                      webhookHeaders:
                        type: string
                        nullable: true
                      webhookBodyTemplate:
                        type: string
                        nullable: true
                      slackConnectionId:
                        type: string
                        nullable: true
                      slackChannelId:
                        type: string
                        nullable: true
                      slackChannelName:
                        type: string
                        nullable: true
                      slackMessageTemplate:
                        type: string
                        nullable: true
                      isEnabled:
                        type: boolean
                      createdAt:
                        type: string
                        format: date-time
                      updatedAt:
                        type: string
                        format: date-time
        '400':
          description: Body failed validation, webhook URL pointed at an internal host, or the Slack channel
            is not reachable.
          content:
            application/json:
              schema:
                type: object
                properties:
                  error:
                    type: string
        '401':
          description: Missing or invalid API key
          content:
            application/json:
              schema:
                type: object
                properties:
                  error:
                    type: string
                  message:
                    type: string
        '403':
          description: API key lacks the `alerts:write` scope
          content:
            application/json:
              schema:
                type: object
                properties:
                  error:
                    type: string
                  message:
                    type: string
        '404':
          description: Alert not found (or not owned by the resolved client)
          content:
            application/json:
              schema:
                type: object
                properties:
                  error:
                    type: string
                    example: Alert not found
        '429':
          description: Rate limit exceeded
          content:
            application/json:
              schema:
                type: object
                properties:
                  error:
                    type: string
                  message:
                    type: string
                  retryAfter:
                    type: integer
  /v1/alerts/{id}/actions/{aid}:
    parameters:
    - name: id
      in: path
      required: true
      description: Alert rule id.
      schema:
        type: string
    - name: aid
      in: path
      required: true
      description: Alert action id.
      schema:
        type: string
    - name: X-Client-Id
      in: header
      required: false
      description: Target client id, slug, or name. Required for agency-wide API keys.
      schema:
        type: string
    put:
      operationId: updateAlertAction
      summary: Update an action on an alert rule
      description: 'Partial update; only supplied keys are written. A supplied `webhookUrl` is re-checked
        against internal hosts and Slack reachability is re-verified. Required scope: `alerts:write` (requireScope).'
      tags:
      - alerts
      requestBody:
        required: true
        content:
          application/json:
            schema:
              type: object
              description: All fields optional. `slackChannelId` is still required when `actionType` is
                `slack`.
              properties:
                actionType:
                  type: string
                  enum:
                  - email
                  - webhook
                  - slack
                emailRecipients:
                  type: string
                  nullable: true
                emailSubjectTemplate:
                  type: string
                  nullable: true
                emailBodyTemplate:
                  type: string
                  nullable: true
                webhookUrl:
                  type: string
                  format: uri
                  nullable: true
                webhookMethod:
                  type: string
                webhookHeaders:
                  type: string
                  nullable: true
                webhookBodyTemplate:
                  type: string
                  nullable: true
                slackConnectionId:
                  type: string
                  nullable: true
                slackChannelId:
                  type: string
                  nullable: true
                slackChannelName:
                  type: string
                  nullable: true
                slackMessageTemplate:
                  type: string
                  nullable: true
                isEnabled:
                  type: boolean
      responses:
        '200':
          description: Updated action
          content:
            application/json:
              schema:
                type: object
                properties:
                  action:
                    type: object
                    description: Full alert_actions row (see addAlertAction 201 for fields).
                    additionalProperties: true
        '400':
          description: Body failed validation, webhook URL pointed at an internal host, or the Slack channel
            is not reachable.
          content:
            application/json:
              schema:
                type: object
                properties:
                  error:
                    type: string
        '401':
          description: Missing or invalid API key
          content:
            application/json:
              schema:
                type: object
                properties:
                  error:
                    type: string
                  message:
                    type: string
        '403':
          description: API key lacks the `alerts:write` scope
          content:
            application/json:
              schema:
                type: object
                properties:
                  error:
                    type: string
                  message:
                    type: string
        '404':
          description: Action not found — returned both when the parent alert is not owned by the client
            and when the action id does not exist.
          content:
            application/json:
              schema:
                type: object
                properties:
                  error:
                    type: string
                    example: Action not found
        '429':
          description: Rate limit exceeded
          content:
            application/json:
              schema:
                type: object
                properties:
                  error:
                    type: string
                  message:
                    type: string
                  retryAfter:
                    type: integer
    delete:
      operationId: deleteAlertAction
      summary: Delete an action from an alert rule
      description: 'Required scope: `alerts:write` (requireScope).'
      tags:
      - alerts
      responses:
        '200':
          description: Deleted
          content:
            application/json:
              schema:
                type: object
                properties:
                  success:
                    type: boolean
                    example: true
        '401':
          description: Missing or invalid API key
          content:
            application/json:
              schema:
                type: object
                properties:
                  error:
                    type: string
                  message:
                    type: string
        '403':
          description: API key lacks the `alerts:write` scope
          content:
            application/json:
              schema:
                type: object
                properties:
                  error:
                    type: string
                  message:
                    type: string
        '404':
          description: Action not found — returned both when the parent alert is not owned by the client
            and when the action id does not exist.
          content:
            application/json:
              schema:
                type: object
                properties:
                  error:
                    type: string
                    example: Action not found
        '429':
          description: Rate limit exceeded
          content:
            application/json:
              schema:
                type: object
                properties:
                  error:
                    type: string
                  message:
                    type: string
                  retryAfter:
                    type: integer
  /v1/alerts/{id}/history:
    get:
      operationId: getAlertHistory
      summary: Get an alert's execution history
      description: 'Returns alert_history rows for the alert, newest execution first. Required scope:
        `alerts:read` OR `alerts:write` (requireAnyScope).'
      tags:
      - alerts
      parameters:
      - name: id
        in: path
        required: true
        description: Alert rule id.
        schema:
          type: string
      - name: limit
        in: query
        required: false
        description: Max rows to return. Non-numeric or zero values fall back to 50.
        schema:
          type: integer
          default: 50
      - name: X-Client-Id
        in: header
        required: false
        description: Target client id, slug, or name. Required for agency-wide API keys.
        schema:
          type: string
      responses:
        '200':
          description: Execution history
          content:
            application/json:
              schema:
                type: object
                required:
                - history
                properties:
                  history:
                    type: array
                    items:
                      type: object
                      properties:
                        id:
                          type: string
                        alertRuleId:
                          type: string
                        executedAt:
                          type: string
                          format: date-time
                        queryResult:
                          type: string
                          nullable: true
                        evaluatedValue:
                          type: string
                          nullable: true
                        conditionMet:
                          type: boolean
                        aiAnalysis:
                          type: string
                          nullable: true
                        conditionResults:
                          type: array
                          nullable: true
                          description: Per-condition verdicts (jsonb array of ConditionResult).
                          items:
                            type: object
                            additionalProperties: true
                        actionsExecuted:
                          type: string
                          nullable: true
                          description: JSON array of action execution results.
                        errorMessage:
                          type: string
                          nullable: true
                        createdAt:
                          type: string
                          format: date-time
        '401':
          description: Missing or invalid API key
          content:
            application/json:
              schema:
                type: object
                properties:
                  error:
                    type: string
                  message:
                    type: string
        '403':
          description: API key lacks the required scope
          content:
            application/json:
              schema:
                type: object
                properties:
                  error:
                    type: string
                  message:
                    type: string
        '404':
          description: Alert not found (or not owned by the resolved client)
          content:
            application/json:
              schema:
                type: object
                properties:
                  error:
                    type: string
                    example: Alert not found
        '429':
          description: Rate limit exceeded
          content:
            application/json:
              schema:
                type: object
                properties:
                  error:
                    type: string
                  message:
                    type: string
                  retryAfter:
                    type: integer
  /v1/alerts/{id}/status:
    put:
      operationId: setAlertStatus
      summary: Pause or resume an alert rule
      description: 'Required scope: `alerts:write` (requireScope).'
      tags:
      - alerts
      parameters:
      - name: id
        in: path
        required: true
        description: Alert rule id.
        schema:
          type: string
      - name: X-Client-Id
        in: header
        required: false
        description: Target client id, slug, or name. Required for agency-wide API keys.
        schema:
          type: string
      requestBody:
        required: true
        content:
          application/json:
            schema:
              type: object
              required:
              - status
              properties:
                status:
                  type: string
                  enum:
                  - active
                  - paused
      responses:
        '200':
          description: Status set
          content:
            application/json:
              schema:
                type: object
                properties:
                  success:
                    type: boolean
                    example: true
                  status:
                    type: string
                    enum:
                    - active
                    - paused
        '400':
          description: Body failed validation
          content:
            application/json:
              schema:
                type: object
                additionalProperties: true
        '401':
          description: Missing or invalid API key
          content:
            application/json:
              schema:
                type: object
                properties:
                  error:
                    type: string
                  message:
                    type: string
        '403':
          description: API key lacks the `alerts:write` scope
          content:
            application/json:
              schema:
                type: object
                properties:
                  error:
                    type: string
                  message:
                    type: string
        '404':
          description: Alert not found (or not owned by the resolved client)
          content:
            application/json:
              schema:
                type: object
                properties:
                  error:
                    type: string
                    example: Alert not found
        '429':
          description: Rate limit exceeded
          content:
            application/json:
              schema:
                type: object
                properties:
                  error:
                    type: string
                  message:
                    type: string
                  retryAfter:
                    type: integer
  /v1/alerts/{id}/trigger:
    post:
      operationId: triggerAlert
      summary: Enqueue an immediate run of an alert
      description: 'Queues the alert for execution by the worker. No request body. Required scope: `alerts:write`
        (requireScope).'
      tags:
      - alerts
      parameters:
      - name: id
        in: path
        required: true
        description: Alert rule id.
        schema:
          type: string
      - name: X-Client-Id
        in: header
        required: false
        description: Target client id, slug, or name. Required for agency-wide API keys.
        schema:
          type: string
      responses:
        '200':
          description: Alert queued
          content:
            application/json:
              schema:
                type: object
                properties:
                  success:
                    type: boolean
                    example: true
                  message:
                    type: string
                    example: Alert queued for execution
        '401':
          description: Missing or invalid API key
          content:
            application/json:
              schema:
                type: object
                properties:
                  error:
                    type: string
                  message:
                    type: string
        '403':
          description: API key lacks the `alerts:write` scope
          content:
            application/json:
              schema:
                type: object
                properties:
                  error:
                    type: string
                  message:
                    type: string
        '404':
          description: Alert not found (or not owned by the resolved client)
          content:
            application/json:
              schema:
                type: object
                properties:
                  error:
                    type: string
                    example: Alert not found
        '429':
          description: Rate limit exceeded
          content:
            application/json:
              schema:
                type: object
                properties:
                  error:
                    type: string
                  message:
                    type: string
                  retryAfter:
                    type: integer
        '500':
          description: Failed to queue alert execution
          content:
            application/json:
              schema:
                type: object
                properties:
                  error:
                    type: string
                    example: Failed to queue alert execution
  /v1/chat:
    post:
      operationId: createChatCompletion
      summary: Send a chat message (non-streaming)
      tags:
      - chat
      description: 'Runs the client''s configured AI agent over `message` in an agentic tool loop and

        returns the complete assistant reply once the loop finishes.


        Required scope: none. This route carries no `requireScope`/`requireAnyScope`;

        access is gated by the group-wide `.use("*", ...)` middleware chain

        `apiKeyAuth` -> `requireResolvedClient` -> `requireChatPermission` -> `rateLimit`,

        so the API key must have the `chat` permission flag. The key''s effective scopes are

        still used indirectly: client-connected MCP tools are filtered out of the agent''s

        tool list unless the key holds `mcp:read` / `mcp:write` (see `clientMcpToolAllowed`).

        '
      security:
      - bearerAuth: []
      parameters:
      - name: X-Client-Id
        in: header
        required: false
        description: 'Target client (id, slug, or name). Required when the API key is an agency-wide

          key (`keyType === "agency"`); ignored for client-scoped keys, whose client is

          resolved from the key itself.

          '
        schema:
          type: string
      requestBody:
        required: true
        content:
          application/json:
            schema:
              type: object
              required:
              - message
              properties:
                message:
                  type: string
                  minLength: 1
                  description: The user message to send to the agent.
                sessionId:
                  type: string
                  description: 'Existing chat session id to continue. Omit to have a new session created;
                    the id is returned in the response.

                    '
                model:
                  type: string
                  description: 'Override the model for this request. Falls back to the agent''s configured
                    model, then the agency default.

                    '
                clientApiKey:
                  type: string
                  description: 'Accepted by the request validator for BYOK support. This handler does
                    not read it — BYOK keys are resolved from the stored client/agency records via `resolveChatModelAndKey`.

                    '
      responses:
        '200':
          description: Assistant reply
          content:
            application/json:
              schema:
                type: object
                required:
                - id
                - sessionId
                - message
                - usage
                properties:
                  id:
                    type: string
                    format: uuid
                    description: Id of the persisted assistant message.
                  sessionId:
                    type: string
                    description: Session the exchange was recorded under.
                  message:
                    type: string
                    description: Full assistant response text.
                  usage:
                    type: object
                    properties:
                      inputTokens:
                        type: integer
                      outputTokens:
                        type: integer
        '400':
          description: 'Request body failed validation, or an agency-wide key omitted the `X-Client-Id`
            header.

            '
          content:
            application/json:
              schema:
                type: object
                properties:
                  error:
                    type: string
                  message:
                    type: string
        '401':
          description: Missing or invalid API key.
          content:
            application/json:
              schema:
                type: object
                properties:
                  error:
                    type: string
                  message:
                    type: string
        '402':
          description: 'AI credits exhausted for the agency (`checkAiUsageAllowed` denied), or no provider
            API key is configured / required BYOK key is absent.

            '
          content:
            application/json:
              schema:
                type: object
                properties:
                  error:
                    type: string
                  balance:
                    type: string
                    nullable: true
                    description: Remaining AI credit balance; present only on the credit-limit error.
        '403':
          description: API key does not have chat permission.
          content:
            application/json:
              schema:
                type: object
                properties:
                  error:
                    type: string
                  message:
                    type: string
        '404':
          description: No client in this agency matches `X-Client-Id`.
          content:
            application/json:
              schema:
                type: object
                properties:
                  error:
                    type: string
                  message:
                    type: string
        '409':
          description: '`X-Client-Id` name matched multiple clients; send an id instead.'
          content:
            application/json:
              schema:
                type: object
                properties:
                  error:
                    type: string
                  message:
                    type: string
        '429':
          description: Rate limit exceeded (`rateLimit` middleware).
        '500':
          description: 'No AI agent configured for the agency, model resolution failed (`{ error }` only),
            or the agent run threw (`{ error: "Chat failed", message }`).

            '
          content:
            application/json:
              schema:
                type: object
                properties:
                  error:
                    type: string
                  message:
                    type: string
  /v1/chat/sessions:
    get:
      operationId: listChatSessions
      summary: List chat sessions for the calling API key
      description: 'Returns up to 100 chat sessions belonging to the authenticating API key, newest `lastMessageAt`
        first, each with a message count and a preview (first 100 characters of the first user message).


        Auth: Bearer API key. The route file declares no `requireScope`/`requireAnyScope`; the whole group
        is gated by `.use("*", ...)` middleware — `apiKeyAuth`, `requireResolvedClient` (agency-wide keys
        must send `X-Client-Id`), `requireChatPermission` (the API key must have the `chat` permission),
        and `rateLimit`.'
      tags:
      - sessions
      parameters:
      - name: X-Client-Id
        in: header
        required: false
        description: Target client id, slug, or name. Required only for agency-wide API keys (`requireResolvedClient`);
          ignored for client-scoped keys.
        schema:
          type: string
      responses:
        '200':
          description: Session list
          content:
            application/json:
              schema:
                type: object
                required:
                - sessions
                properties:
                  sessions:
                    type: array
                    items:
                      type: object
                      required:
                      - id
                      - createdAt
                      - lastMessageAt
                      - messageCount
                      - preview
                      properties:
                        id:
                          type: string
                        createdAt:
                          type: string
                          format: date-time
                        lastMessageAt:
                          type: string
                          format: date-time
                        messageCount:
                          type: integer
                          description: Total messages in the session (user and assistant).
                        preview:
                          type: string
                          description: First 100 characters of the first user message; empty string if
                            none.
        '400':
          description: 'Agency-wide key did not send the `X-Client-Id` header (`{ error: "Missing target
            client", message }`).'
          content:
            application/json:
              schema:
                type: object
                properties:
                  error:
                    type: string
                  message:
                    type: string
        '401':
          description: Missing or invalid API key (from `apiKeyAuth`).
          content:
            application/json:
              schema:
                type: object
                properties:
                  error:
                    type: string
                  message:
                    type: string
        '403':
          description: 'API key lacks the `chat` permission (`{ error: "Permission denied", message: "This
            API key does not have chat permissions" }`).'
          content:
            application/json:
              schema:
                type: object
                properties:
                  error:
                    type: string
                  message:
                    type: string
        '404':
          description: '`X-Client-Id` names no client in the key''s agency.'
          content:
            application/json:
              schema:
                type: object
                properties:
                  error:
                    type: string
                  message:
                    type: string
        '409':
          description: '`X-Client-Id` name is ambiguous across multiple clients.'
          content:
            application/json:
              schema:
                type: object
                properties:
                  error:
                    type: string
                  message:
                    type: string
        '429':
          description: Rate limit exceeded.
          content:
            application/json:
              schema:
                type: object
                properties:
                  error:
                    type: string
                  message:
                    type: string
                  retryAfter:
                    type: integer
        '500':
          description: '`{ error: "Failed to list sessions" }`'
          content:
            application/json:
              schema:
                type: object
                properties:
                  error:
                    type: string
  /v1/chat/sessions/{sessionId}:
    parameters:
    - name: sessionId
      in: path
      required: true
      description: Session id. Read with `c.req.param("sessionId")`; no Zod validator on this route.
      schema:
        type: string
    - name: X-Client-Id
      in: header
      required: false
      description: Target client id, slug, or name. Required only for agency-wide API keys (`requireResolvedClient`);
        ignored for client-scoped keys.
      schema:
        type: string
    get:
      operationId: getChatSession
      summary: Get a chat session with its messages
      description: 'Returns the session row and its messages in ascending `createdAt` order. The session
        must belong to the authenticating API key, otherwise 404.


        Auth: Bearer API key. No `requireScope` on the route; the group-level `.use("*", ...)` middleware
        requires the API key `chat` permission (`requireChatPermission`), plus `apiKeyAuth`, `requireResolvedClient`,
        and `rateLimit`.'
      tags:
      - sessions
      responses:
        '200':
          description: Session and messages
          content:
            application/json:
              schema:
                type: object
                required:
                - session
                - messages
                properties:
                  session:
                    type: object
                    description: Full `api_chat_sessions` row (selected with `db.select()`, no projection).
                    required:
                    - id
                    - apiKeyId
                    - clientId
                    - createdAt
                    - lastMessageAt
                    properties:
                      id:
                        type: string
                      apiKeyId:
                        type: string
                      clientId:
                        type: string
                      createdAt:
                        type: string
                        format: date-time
                      lastMessageAt:
                        type: string
                        format: date-time
                  messages:
                    type: array
                    items:
                      type: object
                      description: Projected message — the handler maps only these four fields, so `toolCalls`,
                        `inputTokens`, and `outputTokens` are not returned.
                      required:
                      - id
                      - role
                      - content
                      - createdAt
                      properties:
                        id:
                          type: string
                        role:
                          type: string
                          enum:
                          - user
                          - assistant
                        content:
                          type: string
                        createdAt:
                          type: string
                          format: date-time
        '400':
          description: Agency-wide key did not send the `X-Client-Id` header.
          content:
            application/json:
              schema:
                type: object
                properties:
                  error:
                    type: string
                  message:
                    type: string
        '401':
          description: Missing or invalid API key.
          content:
            application/json:
              schema:
                type: object
                properties:
                  error:
                    type: string
                  message:
                    type: string
        '403':
          description: API key lacks the `chat` permission.
          content:
            application/json:
              schema:
                type: object
                properties:
                  error:
                    type: string
                  message:
                    type: string
        '404':
          description: '`{ error: "Session not found" }` when no session matches the id for this API key;
            also returned by `requireResolvedClient` when `X-Client-Id` names no client in the agency.'
          content:
            application/json:
              schema:
                type: object
                properties:
                  error:
                    type: string
                  message:
                    type: string
        '409':
          description: '`X-Client-Id` name is ambiguous across multiple clients.'
          content:
            application/json:
              schema:
                type: object
                properties:
                  error:
                    type: string
                  message:
                    type: string
        '429':
          description: Rate limit exceeded.
          content:
            application/json:
              schema:
                type: object
                properties:
                  error:
                    type: string
                  message:
                    type: string
                  retryAfter:
                    type: integer
        '500':
          description: '`{ error: "Failed to get session" }`'
          content:
            application/json:
              schema:
                type: object
                properties:
                  error:
                    type: string
    delete:
      operationId: deleteChatSession
      summary: Delete a chat session
      description: 'Deletes a session owned by the authenticating API key. Messages are removed by the
        `api_chat_messages.session_id` cascade.


        Auth: Bearer API key. No `requireScope` on the route; the group-level `.use("*", ...)` middleware
        requires the API key `chat` permission (`requireChatPermission`), plus `apiKeyAuth`, `requireResolvedClient`,
        and `rateLimit`.'
      tags:
      - sessions
      responses:
        '200':
          description: '`{ success: true }`'
          content:
            application/json:
              schema:
                type: object
                required:
                - success
                properties:
                  success:
                    type: boolean
        '400':
          description: Agency-wide key did not send the `X-Client-Id` header.
          content:
            application/json:
              schema:
                type: object
                properties:
                  error:
                    type: string
                  message:
                    type: string
        '401':
          description: Missing or invalid API key.
          content:
            application/json:
              schema:
                type: object
                properties:
                  error:
                    type: string
                  message:
                    type: string
        '403':
          description: API key lacks the `chat` permission.
          content:
            application/json:
              schema:
                type: object
                properties:
                  error:
                    type: string
                  message:
                    type: string
        '404':
          description: '`{ error: "Session not found" }` when no session matches the id for this API key;
            also returned by `requireResolvedClient` when `X-Client-Id` names no client in the agency.'
          content:
            application/json:
              schema:
                type: object
                properties:
                  error:
                    type: string
                  message:
                    type: string
        '409':
          description: '`X-Client-Id` name is ambiguous across multiple clients.'
          content:
            application/json:
              schema:
                type: object
                properties:
                  error:
                    type: string
                  message:
                    type: string
        '429':
          description: Rate limit exceeded.
          content:
            application/json:
              schema:
                type: object
                properties:
                  error:
                    type: string
                  message:
                    type: string
                  retryAfter:
                    type: integer
        '500':
          description: '`{ error: "Failed to delete session" }`'
          content:
            application/json:
              schema:
                type: object
                properties:
                  error:
                    type: string
  /v1/chat/stream:
    post:
      operationId: streamChatCompletion
      summary: Send a chat message (Server-Sent Events stream)
      tags:
      - chat
      description: 'Same agent run as `POST /v1/chat`, streamed back as Server-Sent Events.


        Required scope: none — identical middleware chain to `POST /v1/chat`

        (`apiKeyAuth`, `requireResolvedClient`, `requireChatPermission`, `rateLimit`), so

        the API key needs the `chat` permission flag. `mcp:read` / `mcp:write` again only

        affect which client-connected MCP tools the agent is given.


        Every SSE frame uses the event name `message`; the payload is

        `{"type": "<event type>", "data": {...}}`. Emitted types, in order of the agent

        loop: `thinking`, `thinking_delta`, `thinking_end`, `content_delta`,

        `tool_call_start`, `tool_call_progress`, `tool_call_result`, `content_done`,

        `done`, and `error`. `done.data` carries `{ sessionId, messageId, usage: {

        inputTokens, outputTokens, costUsd } }`. Note that errors raised after the stream

        opens arrive as an in-stream `error` frame on a 200 response, not as an HTTP error.


        Aborting the HTTP request aborts the agent loop server-side.

        '
      security:
      - bearerAuth: []
      parameters:
      - name: X-Client-Id
        in: header
        required: false
        description: 'Target client (id, slug, or name). Required for agency-wide API keys, ignored for
          client-scoped keys.

          '
        schema:
          type: string
      requestBody:
        required: true
        content:
          application/json:
            schema:
              type: object
              required:
              - message
              properties:
                message:
                  type: string
                  minLength: 1
                sessionId:
                  type: string
                  description: Existing session to continue; a new one is created if omitted.
                model:
                  type: string
                clientApiKey:
                  type: string
                  description: Accepted by the validator for BYOK; not read by this handler.
      responses:
        '200':
          description: 'SSE stream. Response headers set by the handler: `X-Accel-Buffering: no`,

            `Cache-Control: no-cache`, `Connection: keep-alive`.

            '
          content:
            text/event-stream:
              schema:
                type: string
                description: 'Newline-delimited SSE frames, each `event: message` with a JSON `data` payload
                  of shape `{"type": string, "data": object}`.

                  '
        '400':
          description: Validation failure, or agency key without `X-Client-Id`.
          content:
            application/json:
              schema:
                type: object
                properties:
                  error:
                    type: string
                  message:
                    type: string
        '401':
          description: Missing or invalid API key.
          content:
            application/json:
              schema:
                type: object
                properties:
                  error:
                    type: string
                  message:
                    type: string
        '402':
          description: AI credit limit exceeded, or no usable provider API key.
          content:
            application/json:
              schema:
                type: object
                properties:
                  error:
                    type: string
                  balance:
                    type: string
                    nullable: true
        '403':
          description: API key does not have chat permission.
          content:
            application/json:
              schema:
                type: object
                properties:
                  error:
                    type: string
                  message:
                    type: string
        '404':
          description: No client matching `X-Client-Id` in this agency.
          content:
            application/json:
              schema:
                type: object
                properties:
                  error:
                    type: string
                  message:
                    type: string
        '409':
          description: Ambiguous `X-Client-Id` name.
          content:
            application/json:
              schema:
                type: object
                properties:
                  error:
                    type: string
                  message:
                    type: string
        '429':
          description: Rate limit exceeded.
        '500':
          description: 'No agent configured for the agency, or model resolution failed. Errors that occur
            after the stream has started are delivered as an in-stream `error` event instead.

            '
          content:
            application/json:
              schema:
                type: object
                properties:
                  error:
                    type: string
  /v1/chats/{chatId}/stream:
    post:
      operationId: streamChatById
      tags:
      - reports
      summary: Streaming AI chat for a studio chat (Server-Sent Events)
      description: 'Chat-centric entry point: resolves `studio_chats` by id to its session, owning

        client, working report, and agent, then runs the same `handleChatStream`. A

        chat with no working report streams against `__draft-<session_id>`.


        Required scope: NONE declared on the route — dualAuth /

        requireResolvedClient / rateLimit on the `chatStreamRoutes` router, plus

        assertCallerClientAccess(c, chat.client_id) in the handler. Per-tool API-key

        scope gating is the same as /v1/reports/{reportId}/chat/stream.

        '
      parameters:
      - name: chatId
        in: path
        required: true
        schema:
          type: string
        description: studio_chats.id.
      requestBody:
        required: true
        description: Validated by zValidator("json", chatMessageSchema).
        content:
          application/json:
            schema:
              $ref: '#/components/schemas/ChatStreamRequest'
      responses:
        '200':
          description: SSE stream of chat events (see /v1/reports/{reportId}/chat/stream).
          content:
            text/event-stream:
              schema:
                type: string
        '400':
          description: Request body failed chatMessageSchema validation (zValidator).
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ReportsError'
        '401':
          description: Authentication required.
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ReportsError'
        '403':
          description: Caller is not authorized for this chat's client.
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ReportsError'
        '404':
          description: Chat not found.
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ReportsError'
        '500':
          description: Unknown error.
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ReportsError'
  /v1/constraints:
    get:
      operationId: diagnoseFunnel
      tags:
      - constraints
      summary: Diagnose which funnel step a paid campaign is stuck on
      description: 'Requires scope `constraints:read`.


        The headline endpoint: trust-gated, self-configuring, ranked. Delegates to the shared

        `diagnose_funnel` tool (`executeConstraintTool` -> `constraintsCore.diagnose`), which

        self-configures the conversion/lead event pairing off Mythic''s event inventory, gates the

        funnel on the tracking match rate and on conversion-credit coverage, and returns work

        items ranked by recoverable conversions.


        Scoped to the API key''s resolved client (`location_id`). The whole group also requires API

        key auth and a resolved client (`apiKeyAuth` + `requireResolvedClient` middleware) and is

        rate limited (`rateLimit`); the upstream Constraint Finder is 20 req/min per location.


        The large upstream `analysis` payload is omitted unless `include_raw=true`.

        '
      parameters:
      - &id002
        name: days
        in: query
        required: false
        description: Window length in days, inclusive of both ends. Ignored when `from` is given. Clamped
          to 1-365.
        schema:
          type: integer
          minimum: 1
          maximum: 365
      - &id003
        name: from
        in: query
        required: false
        description: Window start date (YYYY-MM-DD). Takes precedence over `days`.
        schema:
          type: string
          format: date
      - &id004
        name: to
        in: query
        required: false
        description: Window end date (YYYY-MM-DD).
        schema:
          type: string
          format: date
      - &id005
        name: group_by
        in: query
        required: false
        description: Grain of the analysis rows.
        schema:
          type: string
          enum:
          - campaign
          - adset
          - ad
      - &id006
        name: attribution
        in: query
        required: false
        description: Attribution model applied to sessions.
        schema:
          type: string
          enum:
          - session
          - first_touch
      - &id007
        name: table
        in: query
        required: false
        description: Meta ads-insights table name, when the client's differs from the platform default.
        schema:
          type: string
      - name: conversion_event
        in: query
        required: false
        description: Event name that counts as the conversion. Omitted means it is auto-selected upstream,
          and the choice is reported back in `config`.
        schema:
          type: string
      - name: lead_event
        in: query
        required: false
        description: Event name that counts as the lead. Supplying it splits the `site` step into `landing`
          and `sales`.
        schema:
          type: string
      - name: revenue_path
        in: query
        required: false
        description: Path to revenue on the conversion event.
        schema:
          type: string
      - name: campaign
        in: query
        required: false
        description: Drill into one campaign by id or name. Benchmarks stay account-wide.
        schema:
          type: string
      - name: attribution_window_days
        in: query
        required: false
        description: Days a paid touch may precede a conversion and still be credited. Clamped upstream
          to 0-365; 0 means own-session credit only.
        schema:
          type: integer
          minimum: 0
          maximum: 365
      - name: min_impressions
        in: query
        required: false
        description: Rows below this many impressions drop out of the ranking but still set the pooled
          benchmark.
        schema:
          type: integer
          minimum: 0
      - name: resolve_identities
        in: query
        required: false
        description: Resolve person identities when crediting conversions. Only the literal string `true`
          enables it.
        schema:
          type: string
          enum:
          - 'true'
          - 'false'
      - name: include_audit
        in: query
        required: false
        description: Run the tracking audit alongside the analysis. It is attached automatically whenever
          the trust verdict is not `ok`.
        schema:
          type: string
          enum:
          - 'true'
          - 'false'
      - name: include_raw
        in: query
        required: false
        description: Include Mythic's full untransformed `analysis` payload in the response.
        schema:
          type: string
          enum:
          - 'true'
          - 'false'
      responses:
        '200':
          description: The ranked, trust-gated diagnosis.
          content:
            application/json:
              schema:
                type: object
                properties:
                  window:
                    type: object
                    properties:
                      days:
                        type: integer
                      from:
                        type: string
                        format: date
                      to:
                        type: string
                        format: date
                  config:
                    type: object
                    description: Which event pairing the diagnosis actually used, and where it came from.
                    properties:
                      conversion_event:
                        type: string
                        nullable: true
                      lead_event:
                        type: string
                        nullable: true
                      revenue_path:
                        type: string
                        nullable: true
                      source:
                        type: string
                        enum:
                        - caller
                        - auto
                        - none
                      note:
                        type: string
                        description: Present only when no conversion event could be chosen.
                    required:
                    - conversion_event
                    - lead_event
                    - revenue_path
                    - source
                  brief:
                    type: object
                    description: The shared FunnelBrief read-model (shared/src/services/constraints.ts).
                    properties:
                      headline:
                        type: string
                        description: One sentence actionable without reading anything else.
                      trust:
                        type: object
                        description: Tracking match-rate gate. Floor is 0.7. `unknown` is not treated
                          as `ok`.
                        properties:
                          level:
                            type: string
                            enum:
                            - ok
                            - low
                            - unknown
                          matchRate:
                            type: number
                            nullable: true
                          message:
                            type: string
                            nullable: true
                            description: Present only when the funnel should not be read at face value.
                        required:
                        - level
                        - matchRate
                        - message
                      credit:
                        type: object
                        description: Conversion-credit coverage gate, independent of the tracking gate.
                          Floor is 0.5.
                        properties:
                          level:
                            type: string
                            enum:
                            - ok
                            - low
                            - none
                            - unknown
                          coverage:
                            type: number
                            nullable: true
                          credited:
                            type: number
                            nullable: true
                          total:
                            type: number
                            nullable: true
                          via_session:
                            type: number
                            nullable: true
                          via_person:
                            type: number
                            nullable: true
                          attribution_window_days:
                            type: number
                            nullable: true
                          message:
                            type: string
                            nullable: true
                        required:
                        - level
                        - coverage
                        - credited
                        - total
                        - message
                      by_step:
                        type: array
                        description: Recoverable conversions summed per step, reliable steps first, each
                          half biggest-first.
                        items:
                          type: object
                          properties:
                            step:
                              type: string
                              enum:
                              - ad
                              - tracking
                              - landing
                              - sales
                              - site
                            label:
                              type: string
                            recoverable_conversions:
                              type: number
                            rows:
                              type: integer
                            reliable:
                              type: boolean
                              description: False when the step is measured in conversions that mostly
                                could not be credited.
                          required:
                          - step
                          - label
                          - recoverable_conversions
                          - rows
                          - reliable
                      findings:
                        type: array
                        description: Constrained rows as ranked work items, ordered by recoverable conversions
                          with caveated findings last.
                        items:
                          type: object
                          properties:
                            name:
                              type: string
                              description: Campaign, ad set or ad name depending on `group_by`.
                            id:
                              type: string
                              nullable: true
                            step:
                              type: string
                              enum:
                              - ad
                              - tracking
                              - landing
                              - sales
                              - site
                            recoverable_conversions:
                              type: number
                              description: Extra conversions closing the gap would return over the window.
                                The ranking key.
                            recoverable_revenue:
                              type: number
                              nullable: true
                              description: Same, valued at this row's own revenue-per-conversion.
                            spend:
                              type: number
                              nullable: true
                            rate:
                              type: number
                              nullable: true
                            benchmark:
                              type: number
                              nullable: true
                            benchmark_source:
                              type: string
                              nullable: true
                              description: '`pooled` (the account average) versus a stored target.'
                            benchmark_note:
                              type: string
                              nullable: true
                            gap:
                              type: string
                              description: The shortfall as a sentence, e.g. "3.1x below the account average".
                            why:
                              type: string
                            fix:
                              type: string
                            landing_page:
                              type: string
                              nullable: true
                            counts:
                              type: object
                              nullable: true
                              description: The two absolute counts the rate is made of.
                              properties:
                                from:
                                  type: string
                                to:
                                  type: string
                                from_value:
                                  type: number
                                  nullable: true
                                to_value:
                                  type: number
                                  nullable: true
                            caveat:
                              type: string
                              nullable: true
                              description: Why this finding should not be actioned at face value. Set
                                when the step is conversion-denominated and credit coverage is thin.
                          required:
                          - name
                          - id
                          - step
                          - recoverable_conversions
                          - gap
                          - why
                          - fix
                          - caveat
                      scale:
                        type: array
                        description: Rows at or above the pooled rate on every step - budget candidates,
                          not optimisation candidates.
                        items:
                          type: object
                          properties:
                            name:
                              type: string
                            id:
                              type: string
                              nullable: true
                            spend:
                              type: number
                              nullable: true
                            conversions:
                              type: number
                              nullable: true
                          required:
                          - name
                          - id
                          - spend
                          - conversions
                      totals:
                        type: object
                        properties:
                          rows:
                            type: integer
                          constrained:
                            type: integer
                          healthy:
                            type: integer
                          recoverable_conversions:
                            type: number
                          recoverable_revenue:
                            type: number
                            nullable: true
                          constrained_spend:
                            type: number
                            nullable: true
                          excluded_low_volume:
                            type: number
                            nullable: true
                            description: Rows the upstream dropped for volume; absent from `rows`.
                          min_impressions:
                            type: number
                            nullable: true
                        required:
                        - rows
                        - constrained
                        - healthy
                        - recoverable_conversions
                        - recoverable_revenue
                        - constrained_spend
                      next_steps:
                        type: array
                        description: Ordered, concrete actions. Empty when there is genuinely nothing
                          to do.
                        items:
                          type: string
                    required:
                    - headline
                    - trust
                    - credit
                    - by_step
                    - findings
                    - scale
                    - totals
                    - next_steps
                  attribution:
                    type: object
                    nullable: true
                    description: Upstream `conversion_attribution` block, passed through unshaped.
                    additionalProperties: true
                  audit:
                    type: object
                    nullable: true
                    description: 'Tracking audit, attached when `include_audit=true` or when the trust
                      verdict is not `ok`. A body of `{ unavailable: true, error }` means the audit ran
                      and failed - never "no tracking problems".'
                    additionalProperties: true
                  analysis:
                    type: object
                    description: Mythic's full untransformed analysis. Present only when `include_raw=true`.
                    additionalProperties: true
                required:
                - window
                - config
                - brief
                - attribution
                - audit
        '400':
          description: '`invalid_step` / `invalid_benchmark` / `invalid_window`, and the fallback status
            for any error whose code is unmapped or absent - including "the Constraint Finder is not configured
            for this client (Mythic Analytics not linked)".'
          content:
            application/json:
              schema: &id001
                type: object
                description: Error envelope unwrapped from the shared tool result.
                properties:
                  error:
                    type: string
                    description: Human-readable message, with any leading "[code]" stripped.
                  code:
                    type: string
                    description: Machine-readable upstream code, e.g. `dataset_missing`. Switch on this,
                      never on the prose.
                  retryAfter:
                    type: string
                    description: Seconds; mirrored into the `Retry-After` header on a 429.
                required:
                - error
        '404':
          description: '`campaign_not_found` / `not_found` - nothing matched.'
          content:
            application/json:
              schema: *id001
        '413':
          description: '`scan_limit_exceeded` - the query would scan too much data.'
          content:
            application/json:
              schema: *id001
        '422':
          description: '`insufficient_data` - nothing in this client''s data can serve as a conversion
            in this window. The message appends usable event candidates when any exist.'
          content:
            application/json:
              schema: *id001
        '424':
          description: 'An upstream or provisioning fault, deliberately not a 5xx of ours: `dataset_missing`,
            `ads_insights_missing`, `bq_unconfigured`, `upstream_error`, `query_failed`.'
          content:
            application/json:
              schema: *id001
        '429':
          description: '`rate_limited` - the upstream is 20 req/min per location.'
          headers:
            Retry-After: &id008
              description: Seconds to wait before retrying. Sent only when the upstream supplied it.
              schema:
                type: string
          content:
            application/json:
              schema: *id001
  /v1/constraints/analyze:
    get:
      operationId: analyzeFunnel
      tags:
      - constraints
      summary: Raw funnel analysis rows, untransformed
      description: 'Requires scope `constraints:read`.


        Mythic''s untransformed response, for callers that want the rows rather than the verdict.

        Delegates to the `analyze_funnel` tool (`constraintsCore.analyze`). No trust gate, no

        ranking, no self-configuration; reach for `GET /v1/constraints` unless you need the rows.


        Also requires API key auth plus a resolved client, and is rate limited.

        '
      parameters:
      - *id002
      - *id003
      - *id004
      - *id005
      - *id006
      - *id007
      - name: conversion_event
        in: query
        required: false
        description: Event name that counts as the conversion. Not auto-selected on this endpoint.
        schema:
          type: string
      - name: lead_event
        in: query
        required: false
        description: Event name that counts as the lead.
        schema:
          type: string
      - name: revenue_path
        in: query
        required: false
        schema:
          type: string
      - name: campaign
        in: query
        required: false
        schema:
          type: string
      - name: attribution_window_days
        in: query
        required: false
        schema:
          type: integer
          minimum: 0
          maximum: 365
      - name: min_impressions
        in: query
        required: false
        schema:
          type: integer
          minimum: 0
      - name: resolve_identities
        in: query
        required: false
        schema:
          type: string
          enum:
          - 'true'
          - 'false'
      - name: benchmark_ad
        in: query
        required: false
        description: Benchmark override for the `ad` step as a RATE, not a percentage (2% is 0.02). Non-numeric
          values are ignored.
        schema:
          type: number
          minimum: 0
          maximum: 1
      - name: benchmark_tracking
        in: query
        required: false
        description: Benchmark override for the `tracking` step, as a rate between 0 and 1.
        schema:
          type: number
          minimum: 0
          maximum: 1
      - name: benchmark_landing
        in: query
        required: false
        description: Benchmark override for the `landing` step, as a rate between 0 and 1.
        schema:
          type: number
          minimum: 0
          maximum: 1
      - name: benchmark_sales
        in: query
        required: false
        description: Benchmark override for the `sales` step, as a rate between 0 and 1.
        schema:
          type: number
          minimum: 0
          maximum: 1
      - name: benchmark_site
        in: query
        required: false
        description: Benchmark override for the `site` step, as a rate between 0 and 1.
        schema:
          type: number
          minimum: 0
          maximum: 1
      responses:
        '200':
          description: Upstream analysis, returned as received (`json.data ?? json`). The shape is defined
            by Mythic's Constraint Finder API rather than by this gateway; the keys listed are the ones
            the shared read-model reads off it, and are indicative rather than exhaustive.
          content:
            application/json:
              schema:
                type: object
                additionalProperties: true
                properties:
                  rows:
                    type: array
                    description: One row per campaign/ad set/ad at the requested grain, each optionally
                      carrying a `constraint` block.
                    items:
                      type: object
                      additionalProperties: true
                  tracking:
                    type: object
                    additionalProperties: true
                    description: Carries `match_rate`, the tracking trust gate's input.
                  conversion_attribution:
                    type: object
                    additionalProperties: true
                    description: Carries `total_conversions_in_window`, `credited_via_session`, `credited_via_person`,
                      `window_days`.
                  summary:
                    type: object
                    additionalProperties: true
                    description: Carries `window_days`.
                  diagnostic:
                    type: string
                    description: 'A response-level diagnostic instead of a funnel: `no_campaign_data`,
                      `no_conversion_events`, `no_conversions_attributed`, `campaign_not_found`.'
                  auto_selected:
                    type: object
                    additionalProperties: true
                  excluded_low_volume:
                    type: number
                  min_impressions:
                    type: number
        '400':
          description: '`invalid_step` / `invalid_benchmark` / `invalid_window`, and the fallback for
            any unmapped error code.'
          content:
            application/json:
              schema: *id001
        '404':
          description: '`campaign_not_found` / `not_found`.'
          content:
            application/json:
              schema: *id001
        '413':
          description: '`scan_limit_exceeded`.'
          content:
            application/json:
              schema: *id001
        '422':
          description: '`insufficient_data`.'
          content:
            application/json:
              schema: *id001
        '424':
          description: 'Upstream or provisioning fault: `dataset_missing`, `ads_insights_missing`, `bq_unconfigured`,
            `upstream_error`, `query_failed`.'
          content:
            application/json:
              schema: *id001
        '429':
          description: '`rate_limited`.'
          headers:
            Retry-After: *id008
          content:
            application/json:
              schema: *id001
  /v1/constraints/audit:
    get:
      operationId: auditFunnelTracking
      tags:
      - constraints
      summary: Tracking hygiene per campaign, untracked clicks bucketed by cause
      description: 'Requires scope `constraints:read`.


        Delegates to the `audit_funnel_tracking` tool (`constraintsCore.audit`). No volume floor,

        and it takes no conversion or benchmark inputs at all - only the window and grain knobs.


        Also requires API key auth plus a resolved client, and is rate limited.

        '
      parameters:
      - *id002
      - *id003
      - *id004
      - *id005
      - *id006
      - *id007
      responses:
        '200':
          description: Upstream tracking audit, returned as received. The shape is defined by Mythic's
            `/client/v1/constraints/audit` rather than by this gateway.
          content:
            application/json:
              schema:
                type: object
                additionalProperties: true
                properties:
                  unavailable:
                    description: Present when the audit ran and failed. Never means "no tracking problems".
                  error:
                    type: string
        '400':
          description: '`invalid_window`, and the fallback for any unmapped error code.'
          content:
            application/json:
              schema: *id001
        '404':
          description: '`campaign_not_found` / `not_found`.'
          content:
            application/json:
              schema: *id001
        '413':
          description: '`scan_limit_exceeded`.'
          content:
            application/json:
              schema: *id001
        '422':
          description: '`insufficient_data`.'
          content:
            application/json:
              schema: *id001
        '424':
          description: Upstream or provisioning fault.
          content:
            application/json:
              schema: *id001
        '429':
          description: '`rate_limited`.'
          headers:
            Retry-After: *id008
          content:
            application/json:
              schema: *id001
  /v1/constraints/benchmarks:
    get:
      operationId: getFunnelBenchmarks
      tags:
      - constraints
      summary: Read the stored benchmark targets
      description: 'Requires scope `constraints:read`.


        Delegates to the `get_funnel_benchmarks` tool (`constraintsCore.getBenchmarks`). Takes no

        parameters at all - the route passes an empty argument object.


        Also requires API key auth plus a resolved client, and is rate limited.

        '
      responses:
        '200':
          description: Upstream stored-benchmark listing, returned as received. The shape is defined by
            Mythic's `/client/v1/constraints/benchmarks` rather than by this gateway.
          content:
            application/json:
              schema:
                type: object
                additionalProperties: true
        '400':
          description: Fallback for any unmapped or absent error code, including "the Constraint Finder
            is not configured for this client".
          content:
            application/json:
              schema: *id001
        '404':
          description: '`not_found`.'
          content:
            application/json:
              schema: *id001
        '424':
          description: Upstream or provisioning fault.
          content:
            application/json:
              schema: *id001
        '429':
          description: '`rate_limited`.'
          headers:
            Retry-After: *id008
          content:
            application/json:
              schema: *id001
    put:
      operationId: setFunnelBenchmark
      tags:
      - constraints
      summary: Store standing benchmark targets for a scope
      description: 'Requires scope `constraints:write` (staff-grantable only: a stored target silently
        changes

        what every future diagnosis says about every campaign).


        Delegates to the `set_funnel_benchmark` tool (`constraintsCore.setBenchmarks`).


        PUT, not PATCH, because it REPLACES the whole scope: any step you omit falls back to the

        account''s pooled rate rather than keeping its previous stored value. The success response

        restates this in `notice`.


        Rates, never percentages - a 2% rate is `0.02`. A value above 1, an unknown step name, or

        an empty benchmarks object is rejected with 400.


        Also requires API key auth plus a resolved client, and is rate limited.

        '
      requestBody:
        required: true
        description: An absent or malformed JSON body is tolerated as `{}` and then fails validation on
          the missing `benchmarks`.
        content:
          application/json:
            schema:
              type: object
              properties:
                benchmarks:
                  type: object
                  description: Per-step target rates. At least one step is required.
                  minProperties: 1
                  additionalProperties: false
                  properties:
                    ad:
                      type: number
                      minimum: 0
                      maximum: 1
                    tracking:
                      type: number
                      minimum: 0
                      maximum: 1
                    landing:
                      type: number
                      minimum: 0
                      maximum: 1
                    sales:
                      type: number
                      minimum: 0
                      maximum: 1
                    site:
                      type: number
                      minimum: 0
                      maximum: 1
                campaign:
                  type: string
                  description: Scope the targets to one campaign. Omitted stores the account default.
                note:
                  type: string
                  description: Free-text note stored with the targets; surfaces as `benchmark_note` on
                    findings.
              required:
              - benchmarks
      responses:
        '200':
          description: Targets stored.
          content:
            application/json:
              schema:
                type: object
                properties:
                  success:
                    type: boolean
                    enum:
                    - true
                  stored:
                    type: object
                    additionalProperties: true
                    description: Upstream's confirmation payload, returned as received.
                  notice:
                    type: string
                    description: Constant reminder that a PUT replaces the whole scope, sent on every
                      success.
                required:
                - success
                - stored
                - notice
        '400':
          description: '`benchmarks` missing, not an object, empty, naming an unknown step, or carrying
            a value outside 0-1 (the percentage-versus-rate mistake). Also upstream `invalid_benchmark`,
            and the fallback for any unmapped error code.'
          content:
            application/json:
              schema: *id001
        '404':
          description: '`campaign_not_found` / `not_found`.'
          content:
            application/json:
              schema: *id001
        '424':
          description: Upstream or provisioning fault.
          content:
            application/json:
              schema: *id001
        '429':
          description: '`rate_limited`.'
          headers:
            Retry-After: *id008
          content:
            application/json:
              schema: *id001
    delete:
      operationId: clearFunnelBenchmark
      tags:
      - constraints
      summary: Clear a stored benchmark target
      description: 'Requires scope `constraints:write`.


        Delegates to the `clear_funnel_benchmark` tool (`constraintsCore.clearBenchmarks`). Clears

        one step across scopes, or an entire scope. Both parameters omitted clears the account

        default.


        Also requires API key auth plus a resolved client, and is rate limited.

        '
      parameters:
      - name: step
        in: query
        required: false
        description: Funnel step to clear. An unknown value is rejected with 400.
        schema:
          type: string
          enum:
          - ad
          - tracking
          - landing
          - sales
          - site
      - name: campaign
        in: query
        required: false
        description: Scope to clear. Omitted means the account default.
        schema:
          type: string
      responses:
        '200':
          description: Cleared.
          content:
            application/json:
              schema:
                type: object
                properties:
                  success:
                    type: boolean
                    enum:
                    - true
                  remaining:
                    type: object
                    additionalProperties: true
                    description: 'Upstream''s remaining-targets payload, returned as received. Falls back
                      to `{ cleared: true }` when the upstream returns no body.'
                required:
                - success
                - remaining
        '400':
          description: Unknown funnel step, and the fallback for any unmapped error code.
          content:
            application/json:
              schema: *id001
        '404':
          description: '`campaign_not_found` / `not_found`.'
          content:
            application/json:
              schema: *id001
        '424':
          description: Upstream or provisioning fault.
          content:
            application/json:
              schema: *id001
        '429':
          description: '`rate_limited`.'
          headers:
            Retry-After: *id008
          content:
            application/json:
              schema: *id001
  /v1/constraints/events:
    get:
      operationId: listFunnelEvents
      tags:
      - constraints
      summary: Event inventory - which events can serve as the lead or conversion step
      description: 'Requires scope `constraints:read`.


        Delegates to the `list_funnel_events` tool (`constraintsCore.listEvents`). This is the

        endpoint that makes the funnel self-configuring, and the only one reporting session

        coverage - the single thing the funnel depends on. Takes only the window parameters.


        Also requires API key auth plus a resolved client, and is rate limited.

        '
      parameters:
      - *id002
      - *id003
      - *id004
      responses:
        '200':
          description: Upstream event inventory, returned as received. The gateway itself only relies
            on `events[].usable` and `events[].event_name`; the remaining per-event fields are defined
            by Mythic.
          content:
            application/json:
              schema:
                type: object
                additionalProperties: true
                properties:
                  events:
                    type: array
                    items:
                      type: object
                      additionalProperties: true
                      properties:
                        event_name:
                          type: string
                        usable:
                          type: boolean
                          description: Whether this event can serve as a conversion or lead step.
        '400':
          description: '`invalid_window`, and the fallback for any unmapped error code.'
          content:
            application/json:
              schema: *id001
        '404':
          description: '`not_found`.'
          content:
            application/json:
              schema: *id001
        '413':
          description: '`scan_limit_exceeded`.'
          content:
            application/json:
              schema: *id001
        '422':
          description: '`insufficient_data`.'
          content:
            application/json:
              schema: *id001
        '424':
          description: Upstream or provisioning fault.
          content:
            application/json:
              schema: *id001
        '429':
          description: '`rate_limited`.'
          headers:
            Retry-After: *id008
          content:
            application/json:
              schema: *id001
  /v1/documents:
    get:
      operationId: listDocuments
      summary: List documents
      description: 'Lists the authenticated client''s Studio knowledge documents (metadata only,

        no document body), newest first by createdAt. `clientId` is derived from the

        API key; agency-scoped keys may pass `X-Client-Id` to target a client in their

        agency. Agency-only documents are returned only for agency-type keys.


        Required scope: `documents:read` OR `documents:write` (requireAnyScope).

        '
      tags:
      - documents
      parameters:
      - name: X-Client-Id
        in: header
        required: false
        description: 'Agency-scoped keys only: target client id within the agency. Ignored for client-scoped
          keys, which always resolve to their own client.'
        schema:
          type: string
      responses:
        '200':
          description: Document metadata list
          headers:
            X-RateLimit-Limit:
              schema:
                type: integer
            X-RateLimit-Remaining:
              schema:
                type: integer
            X-RateLimit-Reset:
              schema:
                type: integer
          content:
            application/json:
              schema:
                type: object
                required:
                - documents
                properties:
                  documents:
                    type: array
                    items:
                      type: object
                      required:
                      - id
                      - name
                      - category
                      - sourceLink
                      - fileType
                      - sizeBytes
                      - agencyOnly
                      - createdAt
                      properties:
                        id:
                          type: string
                        name:
                          type: string
                        category:
                          type: string
                          nullable: true
                        sourceLink:
                          type: string
                          nullable: true
                        fileType:
                          type: string
                          description: Lowercased file extension, fixed at creation.
                          example: md
                        sizeBytes:
                          type: integer
                        agencyOnly:
                          type: boolean
                        createdAt:
                          type: string
                          format: date-time
        '401':
          description: Missing or invalid API key
          content:
            application/json:
              schema:
                type: object
                properties:
                  error:
                    type: string
                  message:
                    type: string
        '403':
          description: Missing required scope
          content:
            application/json:
              schema:
                type: object
                properties:
                  error:
                    type: string
                    example: Permission denied
                  message:
                    type: string
                    example: 'Requires one of: documents:read, documents:write'
        '429':
          description: Rate limit exceeded
          content:
            application/json:
              schema:
                type: object
                properties:
                  error:
                    type: string
                    example: Rate limit exceeded
                  message:
                    type: string
                  retryAfter:
                    type: integer
                    description: Seconds until the window resets.
    post:
      operationId: createDocument
      summary: Create a document
      description: 'Creates a document for the authenticated client. `name` must carry an allowed

        text extension (txt, md, markdown, csv, tsv, json, log) or the request is

        rejected with 415; `content` over 1,000,000 bytes (UTF-8) is rejected with 413.

        `agencyOnly: true` requires an agency-scoped key (403 otherwise).

        Response carries metadata only (the body is not echoed back).


        Required scope: `documents:write`.

        '
      tags:
      - documents
      parameters:
      - name: X-Client-Id
        in: header
        required: false
        description: 'Agency-scoped keys only: target client id within the agency.'
        schema:
          type: string
      requestBody:
        required: true
        content:
          application/json:
            schema:
              type: object
              required:
              - name
              - content
              properties:
                name:
                  type: string
                  minLength: 1
                  description: File name including extension; the extension sets fileType.
                  example: pricing-notes.md
                content:
                  type: string
                  description: Full document text (max 1,000,000 bytes UTF-8).
                category:
                  type: string
                  nullable: true
                sourceLink:
                  type: string
                  nullable: true
                agencyOnly:
                  type: boolean
                  description: Agency-internal document, hidden from client-scoped keys. Accepts loosely-typed
                    truthy values (coerced via coerceBooleanArg). Setting true requires an agency-scoped
                    key.
      responses:
        '201':
          description: Document created
          content:
            application/json:
              schema:
                type: object
                required:
                - document
                properties:
                  document:
                    type: object
                    required:
                    - id
                    - name
                    - category
                    - sourceLink
                    - fileType
                    - sizeBytes
                    - agencyOnly
                    - createdAt
                    properties:
                      id:
                        type: string
                      name:
                        type: string
                      category:
                        type: string
                        nullable: true
                      sourceLink:
                        type: string
                        nullable: true
                      fileType:
                        type: string
                      sizeBytes:
                        type: integer
                      agencyOnly:
                        type: boolean
                      createdAt:
                        type: string
                        format: date-time
        '400':
          description: Request body failed validation
          content:
            application/json:
              schema:
                type: object
                properties:
                  error:
                    type: string
        '401':
          description: Missing or invalid API key
          content:
            application/json:
              schema:
                type: object
                properties:
                  error:
                    type: string
                  message:
                    type: string
        '403':
          description: 'Missing `documents:write` scope, or `agencyOnly: true` from a non-agency key'
          content:
            application/json:
              schema:
                type: object
                properties:
                  error:
                    type: string
                    example: agencyOnly documents require an agency-scoped key
                  message:
                    type: string
        '413':
          description: Content too large (over 1,000,000 bytes)
          content:
            application/json:
              schema:
                type: object
                properties:
                  error:
                    type: string
                    example: Content too large (1200000 bytes; max 1000KB).
        '415':
          description: Unsupported file type (extension not one of txt, md, markdown, csv, tsv, json,
            log)
          content:
            application/json:
              schema:
                type: object
                properties:
                  error:
                    type: string
                    example: Unsupported file type ".pdf". Use a text file (txt, md, markdown, csv, tsv,
                      json, log).
        '429':
          description: Rate limit exceeded
          content:
            application/json:
              schema:
                type: object
                properties:
                  error:
                    type: string
                  message:
                    type: string
                  retryAfter:
                    type: integer
  /v1/documents/{id}:
    parameters:
    - name: id
      in: path
      required: true
      description: Document id (UUID).
      schema:
        type: string
    - name: X-Client-Id
      in: header
      required: false
      description: 'Agency-scoped keys only: target client id within the agency.'
      schema:
        type: string
    get:
      operationId: getDocument
      summary: Get a document with its full text
      description: 'Returns one document including its body (`content`, from the stored

        extractedText; empty string when unset). Scoped to the authenticated client;

        agency-only documents are invisible (404) to client-scoped keys.


        Required scope: `documents:read` OR `documents:write` (requireAnyScope).

        '
      tags:
      - documents
      responses:
        '200':
          description: Document with content
          content:
            application/json:
              schema:
                type: object
                required:
                - document
                properties:
                  document:
                    type: object
                    required:
                    - id
                    - name
                    - category
                    - sourceLink
                    - fileType
                    - sizeBytes
                    - agencyOnly
                    - createdAt
                    - content
                    properties:
                      id:
                        type: string
                      name:
                        type: string
                      category:
                        type: string
                        nullable: true
                      sourceLink:
                        type: string
                        nullable: true
                      fileType:
                        type: string
                      sizeBytes:
                        type: integer
                      agencyOnly:
                        type: boolean
                      createdAt:
                        type: string
                        format: date-time
                      content:
                        type: string
                        description: Full document text ("" when the document has no extracted text).
        '401':
          description: Missing or invalid API key
          content:
            application/json:
              schema:
                type: object
                properties:
                  error:
                    type: string
                  message:
                    type: string
        '403':
          description: Missing required scope
          content:
            application/json:
              schema:
                type: object
                properties:
                  error:
                    type: string
                    example: Permission denied
                  message:
                    type: string
        '404':
          description: Document not found (or agency-only and the key is client-scoped)
          content:
            application/json:
              schema:
                type: object
                properties:
                  error:
                    type: string
                    example: Document not found
        '429':
          description: Rate limit exceeded
          content:
            application/json:
              schema:
                type: object
                properties:
                  error:
                    type: string
                  message:
                    type: string
                  retryAfter:
                    type: integer
    patch:
      operationId: updateDocument
      summary: Update a document
      description: 'Partially updates a document. `content` replaces the body by default, or is

        appended when `mode: "append"` (a newline is inserted first when the existing

        body does not end with one). `category` / `sourceLink` set to null clear the

        field. Setting `agencyOnly: true` requires an agency-scoped key (403); an

        explicit false from a client-scoped key is a no-op rather than an error.

        Documents synced from a Drive folder watch reject `agencyOnly` with 400.

        Response carries metadata only (the body is not echoed back).


        Required scope: `documents:write`.

        '
      tags:
      - documents
      requestBody:
        required: true
        content:
          application/json:
            schema:
              type: object
              properties:
                name:
                  type: string
                  minLength: 1
                content:
                  type: string
                  description: New body text (max 1,000,000 bytes UTF-8).
                mode:
                  type: string
                  enum:
                  - replace
                  - append
                  description: How `content` is applied. Defaults to replace.
                category:
                  type: string
                  nullable: true
                  description: null clears the category.
                sourceLink:
                  type: string
                  nullable: true
                  description: null clears the source link.
                agencyOnly:
                  type: boolean
                  description: Coerced via coerceBooleanArg. Setting true requires an agency-scoped key.
      responses:
        '200':
          description: Updated document metadata
          content:
            application/json:
              schema:
                type: object
                required:
                - document
                properties:
                  document:
                    type: object
                    required:
                    - id
                    - name
                    - category
                    - sourceLink
                    - fileType
                    - sizeBytes
                    - agencyOnly
                    - createdAt
                    properties:
                      id:
                        type: string
                      name:
                        type: string
                      category:
                        type: string
                        nullable: true
                      sourceLink:
                        type: string
                        nullable: true
                      fileType:
                        type: string
                      sizeBytes:
                        type: integer
                      agencyOnly:
                        type: boolean
                      createdAt:
                        type: string
                        format: date-time
        '400':
          description: Rejected by the document core (an append that overflows the size cap, or setting
            agencyOnly on a Drive-synced document), or an invalid request body.
          content:
            application/json:
              schema:
                type: object
                properties:
                  error:
                    type: string
                    example: Failed to update document
        '401':
          description: Missing or invalid API key
          content:
            application/json:
              schema:
                type: object
                properties:
                  error:
                    type: string
                  message:
                    type: string
        '403':
          description: 'Missing `documents:write` scope, or `agencyOnly: true` from a non-agency key'
          content:
            application/json:
              schema:
                type: object
                properties:
                  error:
                    type: string
                    example: Setting agencyOnly requires an agency-scoped key
                  message:
                    type: string
        '404':
          description: Document not found (or agency-only and the key is client-scoped)
          content:
            application/json:
              schema:
                type: object
                properties:
                  error:
                    type: string
                    example: Document not found
        '413':
          description: Content too large (raw `content` over 1,000,000 bytes)
          content:
            application/json:
              schema:
                type: object
                properties:
                  error:
                    type: string
                    example: Content too large (max 1000KB).
        '429':
          description: Rate limit exceeded
          content:
            application/json:
              schema:
                type: object
                properties:
                  error:
                    type: string
                  message:
                    type: string
                  retryAfter:
                    type: integer
    delete:
      operationId: deleteDocument
      summary: Delete a document
      description: 'Deletes a document belonging to the authenticated client. Agency-only

        documents are not deletable by client-scoped keys (404).


        Required scope: `documents:write`.

        '
      tags:
      - documents
      responses:
        '200':
          description: Document deleted
          content:
            application/json:
              schema:
                type: object
                required:
                - success
                properties:
                  success:
                    type: boolean
                    example: true
        '401':
          description: Missing or invalid API key
          content:
            application/json:
              schema:
                type: object
                properties:
                  error:
                    type: string
                  message:
                    type: string
        '403':
          description: Missing `documents:write` scope
          content:
            application/json:
              schema:
                type: object
                properties:
                  error:
                    type: string
                    example: Permission denied
                  message:
                    type: string
        '404':
          description: Document not found (or agency-only and the key is client-scoped)
          content:
            application/json:
              schema:
                type: object
                properties:
                  error:
                    type: string
                    example: Document not found
        '429':
          description: Rate limit exceeded
          content:
            application/json:
              schema:
                type: object
                properties:
                  error:
                    type: string
                  message:
                    type: string
                  retryAfter:
                    type: integer
  /v1/emails:
    get:
      operationId: listEmails
      tags:
      - emails
      summary: List recent emails
      description: 'Recent emails for the API key''s resolved client, newest first (by receivedAt).

        Read-only. Requires scope `emails:read` (route-level `requireScope(SCOPES.EMAILS_READ)`).

        Also requires a resolved client (`requireResolvedClient` middleware — agency keys

        select the tenant with the `X-Client-Id` header) and is subject to rate limiting.

        Delegates to `executeEmailTool("list_emails")`.

        '
      parameters:
      - name: limit
        in: query
        required: false
        description: Max emails to return. Clamped in the executor to 1..100; defaults to 30 when absent
          or not a positive finite number.
        schema:
          type: integer
          minimum: 1
          maximum: 100
          default: 30
      - name: direction
        in: query
        required: false
        description: Filter by message direction. Any other value is ignored (no filter applied).
        schema:
          type: string
          enum:
          - inbound
          - outbound
      - name: include_archived
        in: query
        required: false
        description: Pass the literal string `true` to include archived emails. Anything else restricts
          to `archived = false`.
        schema:
          type: string
          enum:
          - 'true'
          - 'false'
      - $ref: '#/components/parameters/XClientId'
      responses:
        '200':
          description: Emails for the resolved client.
          content:
            application/json:
              schema:
                type: object
                properties:
                  emails:
                    type: array
                    items:
                      $ref: '#/components/schemas/EmailMessageSnippet'
                  count:
                    type: integer
                    description: Number of emails in `emails`.
        '400':
          description: Executor error (returned with the error payload, e.g. no client selected or a query
            failure).
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/EmailError'
  /v1/emails/search:
    get:
      operationId: searchEmails
      tags:
      - emails
      summary: Search emails by subject or body
      description: 'Case-insensitive substring match (`ILIKE %query%`) against subject OR body text,

        scoped to the resolved client, newest first. Requires scope `emails:read`

        (route-level `requireScope(SCOPES.EMAILS_READ)`), a resolved client, and is rate

        limited. Delegates to `executeEmailTool("search_emails")`.

        '
      parameters:
      - name: query
        in: query
        required: false
        description: 'Search string. Not validated at the route — a missing or blank value reaches the

          executor, which returns HTTP 400 with `{"error":"query is required"}`.

          '
        schema:
          type: string
      - name: limit
        in: query
        required: false
        description: Max matches to return. Clamped in the executor to 1..50; defaults to 20 when absent
          or not a positive finite number.
        schema:
          type: integer
          minimum: 1
          maximum: 50
          default: 20
      - $ref: '#/components/parameters/XClientId'
      responses:
        '200':
          description: Matching emails.
          content:
            application/json:
              schema:
                type: object
                properties:
                  emails:
                    type: array
                    items:
                      $ref: '#/components/schemas/EmailMessageSnippet'
                  count:
                    type: integer
                  query:
                    type: string
                    description: The trimmed query that was executed.
        '400':
          description: Missing/blank `query`, or another executor error.
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/EmailError'
  /v1/emails/thread:
    get:
      operationId: getEmailThread
      tags:
      - emails
      summary: Get a full email thread
      description: 'Full conversation for one thread, oldest message first, with full body text (not

        snippets) and the `awaitingReply` open/closed heuristic. `threadId` is a Message-ID

        such as `<id@domain>`; it is a query param rather than a path segment to avoid

        encoding its angle brackets and slashes. Requires scope `emails:read` (route-level

        `requireScope(SCOPES.EMAILS_READ)`), a resolved client, and is rate limited.

        Delegates to `executeEmailTool("get_email_thread")`.

        '
      parameters:
      - name: threadId
        in: query
        required: true
        description: Thread identifier (Message-ID). Missing value returns HTTP 400 from the route itself.
        schema:
          type: string
      - $ref: '#/components/parameters/XClientId'
      responses:
        '200':
          description: The thread and its messages.
          content:
            application/json:
              schema:
                type: object
                properties:
                  threadId:
                    type: string
                  subject:
                    type: string
                    nullable: true
                    description: Raw subject of the earliest message in the thread.
                  messageCount:
                    type: integer
                  awaitingReply:
                    type: boolean
                    description: True when the latest inbound message is newer than the latest outbound
                      one, or there is no outbound reply.
                  messages:
                    type: array
                    items:
                      $ref: '#/components/schemas/EmailMessageFull'
        '400':
          description: '`threadId` query param missing (route-level check).'
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/EmailError'
        '404':
          description: Executor error, including thread not found. Any executor error on this route is
            returned as 404.
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/EmailError'
  /v1/emails/threads:
    get:
      operationId: listEmailThreads
      tags:
      - emails
      summary: List email threads (triage view)
      description: 'Thread-level triage view: one row per conversation with participants, first/last

        timestamps, message count and `awaitingReply` (latest inbound newer than our latest

        outbound, or never replied to). Threads are built by grouping at most 500 scanned

        messages; when that cap is hit the oldest threads are omitted and `truncated` is true.

        Requires scope `emails:read` (route-level `requireScope(SCOPES.EMAILS_READ)`), a

        resolved client, and is rate limited. Delegates to

        `executeEmailTool("list_email_threads")`.

        '
      parameters:
      - name: limit
        in: query
        required: false
        description: Max threads to return. Clamped in the executor to 1..100; defaults to 25 when absent
          or not a positive finite number.
        schema:
          type: integer
          minimum: 1
          maximum: 100
          default: 25
      - name: include_archived
        in: query
        required: false
        description: Pass the literal string `true` to include archived emails when grouping threads.
          Anything else restricts to `archived = false`.
        schema:
          type: string
          enum:
          - 'true'
          - 'false'
      - $ref: '#/components/parameters/XClientId'
      responses:
        '200':
          description: Threads for the resolved client, most recently active first.
          content:
            application/json:
              schema:
                type: object
                properties:
                  threads:
                    type: array
                    items:
                      $ref: '#/components/schemas/EmailThreadSummary'
                  count:
                    type: integer
                    description: Number of threads in `threads`.
                  openCount:
                    type: integer
                    description: Threads in `threads` with `awaitingReply = true`.
                  truncated:
                    type: boolean
                    description: True when the 500-message scan cap was reached, so older threads may
                      be missing.
        '400':
          description: Executor error.
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/EmailError'
  /v1/incrementality/coefficients:
    get:
      operationId: getIncrementalityCoefficients
      tags:
      - incrementality
      summary: Get the current per-channel incrementality coefficients
      description: 'Requires scope `incrementality:read`.


        The progressive-truth coefficient derived from this client''s computed geo-holdout experiments
        (partial results excluded). The 200 body is Mythic''s `data` passed through.'
      parameters:
      - name: X-Client-Id
        in: header
        required: false
        description: 'REQUIRED for agency-scoped API keys: names the target client by id, slug or name.
          Ignored for client-bound keys. Omitting it on an agency key returns 400; no match returns 404;
          an ambiguous name returns 409.'
        schema:
          type: string
      - name: channel
        in: query
        required: false
        description: Narrow to one channel, e.g. meta. Omit for all channels.
        schema:
          type: string
      responses:
        '200':
          description: Mythic's coefficients payload. Field-level shape owned by Mythic.
          content:
            application/json:
              schema:
                type: object
                additionalProperties: true
        '400':
          description: 'The caller''s request is wrong, or an unmapped failure (400 is this route''s fallback
            status). Mapped codes: invalid_config, invalid_definition, unsupported_channel, scope_rejected.
            Also `Incrementality is not configured for this client (Mythic Analytics not linked)`.'
          content:
            application/json:
              schema:
                type: object
                properties:
                  error:
                    type: string
                    description: Human-readable message. For an upstream taxonomy failure the leading
                      '[code] ' is stripped out into `code`.
                  code:
                    type: string
                    description: Mythic's machine-readable code, e.g. dataset_missing, scan_limit_exceeded,
                      insufficient_data, invalid_definition, not_found, rate_limited.
                    example: scan_limit_exceeded
                  retryAfter:
                    type: string
                    description: Seconds to wait; present on rate_limited. Also sent as the Retry-After
                      header.
                required:
                - error
        '401':
          description: Missing, invalid, revoked or expired API key, or a key that resolves to neither
            a client nor an agency.
          content:
            application/json:
              schema:
                type: object
                properties:
                  error:
                    type: string
                  message:
                    type: string
                required:
                - error
        '403':
          description: 'Permission denied: key missing the required scope, its run-as user deactivated,
            or (on writes) a non-agency key. Body is {error, message} from the middleware, or {error,
            code} when Mythic refused (agency_required, hipaa_restricted).'
          content:
            application/json:
              schema:
                type: object
                properties:
                  error:
                    type: string
                  message:
                    type: string
                required:
                - error
        '422':
          description: 'Well-formed but the data cannot answer it: insufficient_data, result_truncated.
            Also a Mythic 2xx carrying success:false.'
          content:
            application/json:
              schema:
                type: object
                properties:
                  error:
                    type: string
                    description: Human-readable message. For an upstream taxonomy failure the leading
                      '[code] ' is stripped out into `code`.
                  code:
                    type: string
                    description: Mythic's machine-readable code, e.g. dataset_missing, scan_limit_exceeded,
                      insufficient_data, invalid_definition, not_found, rate_limited.
                    example: scan_limit_exceeded
                  retryAfter:
                    type: string
                    description: Seconds to wait; present on rate_limited. Also sent as the Retry-After
                      header.
                required:
                - error
        '424':
          description: 'Upstream fault, deliberately reported as 424 rather than a 5xx of ours. Codes:
            dataset_missing, bq_unconfigured, upstream_error, query_failed, spend_table_missing, stale_spend_data,
            mixed_currencies.'
          content:
            application/json:
              schema:
                type: object
                properties:
                  error:
                    type: string
                    description: Human-readable message. For an upstream taxonomy failure the leading
                      '[code] ' is stripped out into `code`.
                  code:
                    type: string
                    description: Mythic's machine-readable code, e.g. dataset_missing, scan_limit_exceeded,
                      insufficient_data, invalid_definition, not_found, rate_limited.
                    example: scan_limit_exceeded
                  retryAfter:
                    type: string
                    description: Seconds to wait; present on rate_limited. Also sent as the Retry-After
                      header.
                required:
                - error
        '429':
          description: 'Rate limited. Either the gateway''s own limiter (body {error, message, retryAfter}
            plus X-RateLimit-* headers) or Mythic''s 120 req/10s per-location limit (code: rate_limited).
            Retry-After is forwarded in both cases.'
          headers:
            Retry-After:
              description: Seconds to wait before retrying.
              schema:
                type: string
          content:
            application/json:
              schema:
                type: object
                properties:
                  error:
                    type: string
                    description: Human-readable message. For an upstream taxonomy failure the leading
                      '[code] ' is stripped out into `code`.
                  code:
                    type: string
                    description: Mythic's machine-readable code, e.g. dataset_missing, scan_limit_exceeded,
                      insufficient_data, invalid_definition, not_found, rate_limited.
                    example: scan_limit_exceeded
                  retryAfter:
                    type: string
                    description: Seconds to wait; present on rate_limited. Also sent as the Retry-After
                      header.
                required:
                - error
  /v1/incrementality/coefficients/history:
    get:
      operationId: getCoefficientHistory
      tags:
      - incrementality
      summary: Get the history of derived incrementality coefficients
      description: 'Requires scope `incrementality:read`.


        How the coefficient moved as each experiment was computed. Takes no parameters beyond auth. The
        200 body is Mythic''s `data` passed through.'
      parameters:
      - name: X-Client-Id
        in: header
        required: false
        description: 'REQUIRED for agency-scoped API keys: names the target client by id, slug or name.
          Ignored for client-bound keys. Omitting it on an agency key returns 400; no match returns 404;
          an ambiguous name returns 409.'
        schema:
          type: string
      responses:
        '200':
          description: Mythic's coefficient-history payload. Field-level shape owned by Mythic.
          content:
            application/json:
              schema:
                type: object
                additionalProperties: true
        '400':
          description: 'The caller''s request is wrong, or an unmapped failure (400 is this route''s fallback
            status). Mapped codes: invalid_config, invalid_definition, unsupported_channel, scope_rejected.
            Also `Incrementality is not configured for this client (Mythic Analytics not linked)`.'
          content:
            application/json:
              schema:
                type: object
                properties:
                  error:
                    type: string
                    description: Human-readable message. For an upstream taxonomy failure the leading
                      '[code] ' is stripped out into `code`.
                  code:
                    type: string
                    description: Mythic's machine-readable code, e.g. dataset_missing, scan_limit_exceeded,
                      insufficient_data, invalid_definition, not_found, rate_limited.
                    example: scan_limit_exceeded
                  retryAfter:
                    type: string
                    description: Seconds to wait; present on rate_limited. Also sent as the Retry-After
                      header.
                required:
                - error
        '401':
          description: Missing, invalid, revoked or expired API key, or a key that resolves to neither
            a client nor an agency.
          content:
            application/json:
              schema:
                type: object
                properties:
                  error:
                    type: string
                  message:
                    type: string
                required:
                - error
        '403':
          description: 'Permission denied: key missing the required scope, its run-as user deactivated,
            or (on writes) a non-agency key. Body is {error, message} from the middleware, or {error,
            code} when Mythic refused (agency_required, hipaa_restricted).'
          content:
            application/json:
              schema:
                type: object
                properties:
                  error:
                    type: string
                  message:
                    type: string
                required:
                - error
        '422':
          description: 'Well-formed but the data cannot answer it: insufficient_data, result_truncated.
            Also a Mythic 2xx carrying success:false.'
          content:
            application/json:
              schema:
                type: object
                properties:
                  error:
                    type: string
                    description: Human-readable message. For an upstream taxonomy failure the leading
                      '[code] ' is stripped out into `code`.
                  code:
                    type: string
                    description: Mythic's machine-readable code, e.g. dataset_missing, scan_limit_exceeded,
                      insufficient_data, invalid_definition, not_found, rate_limited.
                    example: scan_limit_exceeded
                  retryAfter:
                    type: string
                    description: Seconds to wait; present on rate_limited. Also sent as the Retry-After
                      header.
                required:
                - error
        '424':
          description: 'Upstream fault, deliberately reported as 424 rather than a 5xx of ours. Codes:
            dataset_missing, bq_unconfigured, upstream_error, query_failed, spend_table_missing, stale_spend_data,
            mixed_currencies.'
          content:
            application/json:
              schema:
                type: object
                properties:
                  error:
                    type: string
                    description: Human-readable message. For an upstream taxonomy failure the leading
                      '[code] ' is stripped out into `code`.
                  code:
                    type: string
                    description: Mythic's machine-readable code, e.g. dataset_missing, scan_limit_exceeded,
                      insufficient_data, invalid_definition, not_found, rate_limited.
                    example: scan_limit_exceeded
                  retryAfter:
                    type: string
                    description: Seconds to wait; present on rate_limited. Also sent as the Retry-After
                      header.
                required:
                - error
        '429':
          description: 'Rate limited. Either the gateway''s own limiter (body {error, message, retryAfter}
            plus X-RateLimit-* headers) or Mythic''s 120 req/10s per-location limit (code: rate_limited).
            Retry-After is forwarded in both cases.'
          headers:
            Retry-After:
              description: Seconds to wait before retrying.
              schema:
                type: string
          content:
            application/json:
              schema:
                type: object
                properties:
                  error:
                    type: string
                    description: Human-readable message. For an upstream taxonomy failure the leading
                      '[code] ' is stripped out into `code`.
                  code:
                    type: string
                    description: Mythic's machine-readable code, e.g. dataset_missing, scan_limit_exceeded,
                      insufficient_data, invalid_definition, not_found, rate_limited.
                    example: scan_limit_exceeded
                  retryAfter:
                    type: string
                    description: Seconds to wait; present on rate_limited. Also sent as the Retry-After
                      header.
                required:
                - error
  /v1/incrementality/eligibility:
    get:
      operationId: checkGeoEligibility
      tags:
      - incrementality
      summary: Check whether this client is a viable geo-holdout candidate
      description: 'Requires scope `incrementality:read`.


        Pre-flight assessment: does the client have enough geographically spread conversion volume to
        run a Meta geo-holdout test? `daily_budget` sharpens the detectability check against realistic
        spend. `auto=true` has Mythic inventory the location''s conversion events and assess the best
        one, which is the fix for the all-zeros trap where the default purchase event does not exist.


        Proxies Mythic GET /client/v1/incrementality/eligibility; the 200 body is Mythic''s `data` passed
        through, so its field-level shape is owned by Mythic and not declared here.'
      parameters:
      - name: X-Client-Id
        in: header
        required: false
        description: 'REQUIRED for agency-scoped API keys: names the target client by id, slug or name.
          Ignored for client-bound keys. Omitting it on an agency key returns 400; no match returns 404;
          an ambiguous name returns 409.'
        schema:
          type: string
      - name: daily_budget
        in: query
        required: false
        description: Planned daily ad budget. Coerced with Number(); a non-finite value is dropped by
          the core.
        schema:
          type: number
      - name: auto
        in: query
        required: false
        description: Send the exact string `true` to let Mythic pick the best conversion event. Anything
          else is false.
        schema:
          type: string
          enum:
          - 'true'
          - 'false'
      - name: measure
        in: query
        required: false
        description: '`revenue` (monetary value, Mythic''s default) or `count` for lead-gen events that
          carry no value.'
        schema:
          type: string
          enum:
          - revenue
          - count
      - name: event_name
        in: query
        required: false
        description: Conversion event to measure. Mythic defaults to the literal `purchase` event.
        schema:
          type: string
      - name: json_path
        in: query
        required: false
        description: Where the monetary value lives in the event properties, e.g. $.meta_amount. Mythic
          default $.value.
        schema:
          type: string
      - name: lookback_days
        in: query
        required: false
        description: 'Window in days, clamped by the core to 1..365. This is the lever for a scan_limit_exceeded
          failure: narrow the window.'
        schema:
          type: integer
          minimum: 1
          maximum: 365
      responses:
        '200':
          description: Mythic's eligibility payload, plus `notice` when the window was narrowed.
          content:
            application/json:
              schema:
                type: object
                additionalProperties: true
                properties:
                  notice:
                    type: string
                    description: Present only when Mythic silently shortened the lookback window. Verdicts
                      are then NOT comparable with a full-window result.
                  window_narrowed:
                    type: object
                    additionalProperties: true
                    properties:
                      requested_lookback_days:
                        type: integer
                      used_lookback_days:
                        type: integer
                      reason:
                        type: string
        '400':
          description: 'The caller''s request is wrong, or an unmapped failure (400 is this route''s fallback
            status). Mapped codes: invalid_config, invalid_definition, unsupported_channel, scope_rejected.
            Also `Incrementality is not configured for this client (Mythic Analytics not linked)`.'
          content:
            application/json:
              schema:
                type: object
                properties:
                  error:
                    type: string
                    description: Human-readable message. For an upstream taxonomy failure the leading
                      '[code] ' is stripped out into `code`.
                  code:
                    type: string
                    description: Mythic's machine-readable code, e.g. dataset_missing, scan_limit_exceeded,
                      insufficient_data, invalid_definition, not_found, rate_limited.
                    example: scan_limit_exceeded
                  retryAfter:
                    type: string
                    description: Seconds to wait; present on rate_limited. Also sent as the Retry-After
                      header.
                required:
                - error
        '401':
          description: Missing, invalid, revoked or expired API key, or a key that resolves to neither
            a client nor an agency.
          content:
            application/json:
              schema:
                type: object
                properties:
                  error:
                    type: string
                  message:
                    type: string
                required:
                - error
        '403':
          description: 'Permission denied: key missing the required scope, its run-as user deactivated,
            or (on writes) a non-agency key. Body is {error, message} from the middleware, or {error,
            code} when Mythic refused (agency_required, hipaa_restricted).'
          content:
            application/json:
              schema:
                type: object
                properties:
                  error:
                    type: string
                  message:
                    type: string
                required:
                - error
        '404':
          description: '`not_found` from Mythic, or this route''s 404 fallback (`Experiment not found`)
            when the failure carries no mapped code.'
          content:
            application/json:
              schema:
                type: object
                properties:
                  error:
                    type: string
                    description: Human-readable message. For an upstream taxonomy failure the leading
                      '[code] ' is stripped out into `code`.
                  code:
                    type: string
                    description: Mythic's machine-readable code, e.g. dataset_missing, scan_limit_exceeded,
                      insufficient_data, invalid_definition, not_found, rate_limited.
                    example: scan_limit_exceeded
                  retryAfter:
                    type: string
                    description: Seconds to wait; present on rate_limited. Also sent as the Retry-After
                      header.
                required:
                - error
        '413':
          description: '`scan_limit_exceeded`: the query would scan too much data. Retry with a shorter
            lookback_days.'
          content:
            application/json:
              schema:
                type: object
                properties:
                  error:
                    type: string
                    description: Human-readable message. For an upstream taxonomy failure the leading
                      '[code] ' is stripped out into `code`.
                  code:
                    type: string
                    description: Mythic's machine-readable code, e.g. dataset_missing, scan_limit_exceeded,
                      insufficient_data, invalid_definition, not_found, rate_limited.
                    example: scan_limit_exceeded
                  retryAfter:
                    type: string
                    description: Seconds to wait; present on rate_limited. Also sent as the Retry-After
                      header.
                required:
                - error
        '422':
          description: 'Well-formed but the data cannot answer it: insufficient_data, result_truncated.
            Also a Mythic 2xx carrying success:false.'
          content:
            application/json:
              schema:
                type: object
                properties:
                  error:
                    type: string
                    description: Human-readable message. For an upstream taxonomy failure the leading
                      '[code] ' is stripped out into `code`.
                  code:
                    type: string
                    description: Mythic's machine-readable code, e.g. dataset_missing, scan_limit_exceeded,
                      insufficient_data, invalid_definition, not_found, rate_limited.
                    example: scan_limit_exceeded
                  retryAfter:
                    type: string
                    description: Seconds to wait; present on rate_limited. Also sent as the Retry-After
                      header.
                required:
                - error
        '424':
          description: 'Upstream fault, deliberately reported as 424 rather than a 5xx of ours. Codes:
            dataset_missing, bq_unconfigured, upstream_error, query_failed, spend_table_missing, stale_spend_data,
            mixed_currencies.'
          content:
            application/json:
              schema:
                type: object
                properties:
                  error:
                    type: string
                    description: Human-readable message. For an upstream taxonomy failure the leading
                      '[code] ' is stripped out into `code`.
                  code:
                    type: string
                    description: Mythic's machine-readable code, e.g. dataset_missing, scan_limit_exceeded,
                      insufficient_data, invalid_definition, not_found, rate_limited.
                    example: scan_limit_exceeded
                  retryAfter:
                    type: string
                    description: Seconds to wait; present on rate_limited. Also sent as the Retry-After
                      header.
                required:
                - error
        '429':
          description: 'Rate limited. Either the gateway''s own limiter (body {error, message, retryAfter}
            plus X-RateLimit-* headers) or Mythic''s 120 req/10s per-location limit (code: rate_limited).
            Retry-After is forwarded in both cases.'
          headers:
            Retry-After:
              description: Seconds to wait before retrying.
              schema:
                type: string
          content:
            application/json:
              schema:
                type: object
                properties:
                  error:
                    type: string
                    description: Human-readable message. For an upstream taxonomy failure the leading
                      '[code] ' is stripped out into `code`.
                  code:
                    type: string
                    description: Mythic's machine-readable code, e.g. dataset_missing, scan_limit_exceeded,
                      insufficient_data, invalid_definition, not_found, rate_limited.
                    example: scan_limit_exceeded
                  retryAfter:
                    type: string
                    description: Seconds to wait; present on rate_limited. Also sent as the Retry-After
                      header.
                required:
                - error
  /v1/incrementality/experiments:
    get:
      operationId: listGeoExperiments
      tags:
      - incrementality
      summary: List the client's geo-holdout experiments
      description: 'Requires scope `incrementality:read`.


        An upstream outage surfaces as an error rather than an empty list (the core throws instead of
        swallowing failures), so an empty `experiments` array genuinely means this client has none.'
      parameters:
      - name: X-Client-Id
        in: header
        required: false
        description: 'REQUIRED for agency-scoped API keys: names the target client by id, slug or name.
          Ignored for client-bound keys. Omitting it on an agency key returns 400; no match returns 404;
          an ambiguous name returns 409.'
        schema:
          type: string
      responses:
        '200':
          description: Experiment list. Each element is Mythic's experiment object.
          content:
            application/json:
              schema:
                type: object
                properties:
                  experiments:
                    type: array
                    items:
                      type: object
                      additionalProperties: true
                required:
                - experiments
        '400':
          description: 'The caller''s request is wrong, or an unmapped failure (400 is this route''s fallback
            status). Mapped codes: invalid_config, invalid_definition, unsupported_channel, scope_rejected.
            Also `Incrementality is not configured for this client (Mythic Analytics not linked)`.'
          content:
            application/json:
              schema:
                type: object
                properties:
                  error:
                    type: string
                    description: Human-readable message. For an upstream taxonomy failure the leading
                      '[code] ' is stripped out into `code`.
                  code:
                    type: string
                    description: Mythic's machine-readable code, e.g. dataset_missing, scan_limit_exceeded,
                      insufficient_data, invalid_definition, not_found, rate_limited.
                    example: scan_limit_exceeded
                  retryAfter:
                    type: string
                    description: Seconds to wait; present on rate_limited. Also sent as the Retry-After
                      header.
                required:
                - error
        '401':
          description: Missing, invalid, revoked or expired API key, or a key that resolves to neither
            a client nor an agency.
          content:
            application/json:
              schema:
                type: object
                properties:
                  error:
                    type: string
                  message:
                    type: string
                required:
                - error
        '403':
          description: 'Permission denied: key missing the required scope, its run-as user deactivated,
            or (on writes) a non-agency key. Body is {error, message} from the middleware, or {error,
            code} when Mythic refused (agency_required, hipaa_restricted).'
          content:
            application/json:
              schema:
                type: object
                properties:
                  error:
                    type: string
                  message:
                    type: string
                required:
                - error
        '422':
          description: 'Well-formed but the data cannot answer it: insufficient_data, result_truncated.
            Also a Mythic 2xx carrying success:false.'
          content:
            application/json:
              schema:
                type: object
                properties:
                  error:
                    type: string
                    description: Human-readable message. For an upstream taxonomy failure the leading
                      '[code] ' is stripped out into `code`.
                  code:
                    type: string
                    description: Mythic's machine-readable code, e.g. dataset_missing, scan_limit_exceeded,
                      insufficient_data, invalid_definition, not_found, rate_limited.
                    example: scan_limit_exceeded
                  retryAfter:
                    type: string
                    description: Seconds to wait; present on rate_limited. Also sent as the Retry-After
                      header.
                required:
                - error
        '424':
          description: 'Upstream fault, deliberately reported as 424 rather than a 5xx of ours. Codes:
            dataset_missing, bq_unconfigured, upstream_error, query_failed, spend_table_missing, stale_spend_data,
            mixed_currencies.'
          content:
            application/json:
              schema:
                type: object
                properties:
                  error:
                    type: string
                    description: Human-readable message. For an upstream taxonomy failure the leading
                      '[code] ' is stripped out into `code`.
                  code:
                    type: string
                    description: Mythic's machine-readable code, e.g. dataset_missing, scan_limit_exceeded,
                      insufficient_data, invalid_definition, not_found, rate_limited.
                    example: scan_limit_exceeded
                  retryAfter:
                    type: string
                    description: Seconds to wait; present on rate_limited. Also sent as the Retry-After
                      header.
                required:
                - error
        '429':
          description: 'Rate limited. Either the gateway''s own limiter (body {error, message, retryAfter}
            plus X-RateLimit-* headers) or Mythic''s 120 req/10s per-location limit (code: rate_limited).
            Retry-After is forwarded in both cases.'
          headers:
            Retry-After:
              description: Seconds to wait before retrying.
              schema:
                type: string
          content:
            application/json:
              schema:
                type: object
                properties:
                  error:
                    type: string
                    description: Human-readable message. For an upstream taxonomy failure the leading
                      '[code] ' is stripped out into `code`.
                  code:
                    type: string
                    description: Mythic's machine-readable code, e.g. dataset_missing, scan_limit_exceeded,
                      insufficient_data, invalid_definition, not_found, rate_limited.
                    example: scan_limit_exceeded
                  retryAfter:
                    type: string
                    description: Seconds to wait; present on rate_limited. Also sent as the Retry-After
                      header.
                required:
                - error
    post:
      operationId: createGeoExperiment
      tags:
      - incrementality
      summary: Create a geo-holdout experiment
      description: 'Requires scope `incrementality:write` AND an agency-scoped API key: a client-bound
        key is rejected with 403 `This operation requires an agency-scoped API key` before the scope check
        is reached. Writes change real Meta ad targeting.


        Mythic validates the definition at create time by building the compute query, so an uncomputable
        definition comes back as 400 invalid_definition: there is no dry-run or rollback step. There is
        no edit endpoint either, so a typo is fixed by delete + recreate.


        The body is parsed with a fallback to an empty object on malformed JSON, so a broken body surfaces
        as 400 (name is required) rather than a parse error. This route has no Zod validator.'
      parameters:
      - name: X-Client-Id
        in: header
        required: false
        description: 'REQUIRED for agency-scoped API keys: names the target client by id, slug or name.
          Ignored for client-bound keys. Omitting it on an agency key returns 400; no match returns 404;
          an ambiguous name returns 409.'
        schema:
          type: string
      requestBody:
        required: true
        content:
          application/json:
            schema:
              type: object
              properties:
                name:
                  type: string
                  description: Trimmed by the core; empty or missing gives 400 (name is required).
                channel:
                  type: string
                  description: Optional channel label, e.g. meta. Stringified when present, omitted from
                    the upstream body when absent.
                definition:
                  type: object
                  additionalProperties: true
                  description: Geo-holdout definition. Must be a non-array JSON object or the request
                    fails with 400 (definition must be a JSON object). Field-level shape is validated
                    by Mythic, not the gateway.
              required:
              - name
              - definition
      responses:
        '200':
          description: Created. The route returns 200, not 201.
          content:
            application/json:
              schema:
                type: object
                properties:
                  success:
                    type: boolean
                    enum:
                    - true
                  experiment:
                    type: object
                    additionalProperties: true
                required:
                - success
                - experiment
        '400':
          description: 'The caller''s request is wrong, or an unmapped failure (400 is this route''s fallback
            status). Mapped codes: invalid_config, invalid_definition, unsupported_channel, scope_rejected.
            Also `Incrementality is not configured for this client (Mythic Analytics not linked)`.'
          content:
            application/json:
              schema:
                type: object
                properties:
                  error:
                    type: string
                    description: Human-readable message. For an upstream taxonomy failure the leading
                      '[code] ' is stripped out into `code`.
                  code:
                    type: string
                    description: Mythic's machine-readable code, e.g. dataset_missing, scan_limit_exceeded,
                      insufficient_data, invalid_definition, not_found, rate_limited.
                    example: scan_limit_exceeded
                  retryAfter:
                    type: string
                    description: Seconds to wait; present on rate_limited. Also sent as the Retry-After
                      header.
                required:
                - error
        '401':
          description: Missing, invalid, revoked or expired API key, or a key that resolves to neither
            a client nor an agency.
          content:
            application/json:
              schema:
                type: object
                properties:
                  error:
                    type: string
                  message:
                    type: string
                required:
                - error
        '403':
          description: 'Permission denied: key missing the required scope, its run-as user deactivated,
            or (on writes) a non-agency key. Body is {error, message} from the middleware, or {error,
            code} when Mythic refused (agency_required, hipaa_restricted).'
          content:
            application/json:
              schema:
                type: object
                properties:
                  error:
                    type: string
                  message:
                    type: string
                required:
                - error
        '409':
          description: '`overlapping_experiments`: another experiment already covers these regions/window.'
          content:
            application/json:
              schema:
                type: object
                properties:
                  error:
                    type: string
                    description: Human-readable message. For an upstream taxonomy failure the leading
                      '[code] ' is stripped out into `code`.
                  code:
                    type: string
                    description: Mythic's machine-readable code, e.g. dataset_missing, scan_limit_exceeded,
                      insufficient_data, invalid_definition, not_found, rate_limited.
                    example: scan_limit_exceeded
                  retryAfter:
                    type: string
                    description: Seconds to wait; present on rate_limited. Also sent as the Retry-After
                      header.
                required:
                - error
        '422':
          description: 'Well-formed but the data cannot answer it: insufficient_data, result_truncated.
            Also a Mythic 2xx carrying success:false.'
          content:
            application/json:
              schema:
                type: object
                properties:
                  error:
                    type: string
                    description: Human-readable message. For an upstream taxonomy failure the leading
                      '[code] ' is stripped out into `code`.
                  code:
                    type: string
                    description: Mythic's machine-readable code, e.g. dataset_missing, scan_limit_exceeded,
                      insufficient_data, invalid_definition, not_found, rate_limited.
                    example: scan_limit_exceeded
                  retryAfter:
                    type: string
                    description: Seconds to wait; present on rate_limited. Also sent as the Retry-After
                      header.
                required:
                - error
        '424':
          description: 'Upstream fault, deliberately reported as 424 rather than a 5xx of ours. Codes:
            dataset_missing, bq_unconfigured, upstream_error, query_failed, spend_table_missing, stale_spend_data,
            mixed_currencies.'
          content:
            application/json:
              schema:
                type: object
                properties:
                  error:
                    type: string
                    description: Human-readable message. For an upstream taxonomy failure the leading
                      '[code] ' is stripped out into `code`.
                  code:
                    type: string
                    description: Mythic's machine-readable code, e.g. dataset_missing, scan_limit_exceeded,
                      insufficient_data, invalid_definition, not_found, rate_limited.
                    example: scan_limit_exceeded
                  retryAfter:
                    type: string
                    description: Seconds to wait; present on rate_limited. Also sent as the Retry-After
                      header.
                required:
                - error
        '429':
          description: 'Rate limited. Either the gateway''s own limiter (body {error, message, retryAfter}
            plus X-RateLimit-* headers) or Mythic''s 120 req/10s per-location limit (code: rate_limited).
            Retry-After is forwarded in both cases.'
          headers:
            Retry-After:
              description: Seconds to wait before retrying.
              schema:
                type: string
          content:
            application/json:
              schema:
                type: object
                properties:
                  error:
                    type: string
                    description: Human-readable message. For an upstream taxonomy failure the leading
                      '[code] ' is stripped out into `code`.
                  code:
                    type: string
                    description: Mythic's machine-readable code, e.g. dataset_missing, scan_limit_exceeded,
                      insufficient_data, invalid_definition, not_found, rate_limited.
                    example: scan_limit_exceeded
                  retryAfter:
                    type: string
                    description: Seconds to wait; present on rate_limited. Also sent as the Retry-After
                      header.
                required:
                - error
  /v1/incrementality/experiments/{id}:
    get:
      operationId: getGeoExperiment
      tags:
      - incrementality
      summary: Get one geo-holdout experiment
      description: 'Requires scope `incrementality:read`.


        This route''s fallback error status is 404, so an unmapped failure on an unknown id surfaces as
        404 (Experiment not found).'
      parameters:
      - name: X-Client-Id
        in: header
        required: false
        description: 'REQUIRED for agency-scoped API keys: names the target client by id, slug or name.
          Ignored for client-bound keys. Omitting it on an agency key returns 400; no match returns 404;
          an ambiguous name returns 409.'
        schema:
          type: string
      - name: id
        in: path
        required: true
        description: Experiment id.
        schema:
          type: string
      responses:
        '200':
          description: The experiment. Field-level shape owned by Mythic.
          content:
            application/json:
              schema:
                type: object
                properties:
                  experiment:
                    type: object
                    additionalProperties: true
                required:
                - experiment
        '400':
          description: 'The caller''s request is wrong: invalid_config, invalid_definition, unsupported_channel,
            scope_rejected. Also `Incrementality is not configured for this client (Mythic Analytics not
            linked)`.'
          content:
            application/json:
              schema:
                type: object
                properties:
                  error:
                    type: string
                    description: Human-readable message. For an upstream taxonomy failure the leading
                      '[code] ' is stripped out into `code`.
                  code:
                    type: string
                    description: Mythic's machine-readable code, e.g. dataset_missing, scan_limit_exceeded,
                      insufficient_data, invalid_definition, not_found, rate_limited.
                    example: scan_limit_exceeded
                  retryAfter:
                    type: string
                    description: Seconds to wait; present on rate_limited. Also sent as the Retry-After
                      header.
                required:
                - error
        '401':
          description: Missing, invalid, revoked or expired API key, or a key that resolves to neither
            a client nor an agency.
          content:
            application/json:
              schema:
                type: object
                properties:
                  error:
                    type: string
                  message:
                    type: string
                required:
                - error
        '403':
          description: 'Permission denied: key missing the required scope, its run-as user deactivated,
            or (on writes) a non-agency key. Body is {error, message} from the middleware, or {error,
            code} when Mythic refused (agency_required, hipaa_restricted).'
          content:
            application/json:
              schema:
                type: object
                properties:
                  error:
                    type: string
                  message:
                    type: string
                required:
                - error
        '404':
          description: '`not_found` from Mythic, or this route''s 404 fallback (`Experiment not found`)
            when the failure carries no mapped code.'
          content:
            application/json:
              schema:
                type: object
                properties:
                  error:
                    type: string
                    description: Human-readable message. For an upstream taxonomy failure the leading
                      '[code] ' is stripped out into `code`.
                  code:
                    type: string
                    description: Mythic's machine-readable code, e.g. dataset_missing, scan_limit_exceeded,
                      insufficient_data, invalid_definition, not_found, rate_limited.
                    example: scan_limit_exceeded
                  retryAfter:
                    type: string
                    description: Seconds to wait; present on rate_limited. Also sent as the Retry-After
                      header.
                required:
                - error
        '422':
          description: 'Well-formed but the data cannot answer it: insufficient_data, result_truncated.
            Also a Mythic 2xx carrying success:false.'
          content:
            application/json:
              schema:
                type: object
                properties:
                  error:
                    type: string
                    description: Human-readable message. For an upstream taxonomy failure the leading
                      '[code] ' is stripped out into `code`.
                  code:
                    type: string
                    description: Mythic's machine-readable code, e.g. dataset_missing, scan_limit_exceeded,
                      insufficient_data, invalid_definition, not_found, rate_limited.
                    example: scan_limit_exceeded
                  retryAfter:
                    type: string
                    description: Seconds to wait; present on rate_limited. Also sent as the Retry-After
                      header.
                required:
                - error
        '424':
          description: 'Upstream fault, deliberately reported as 424 rather than a 5xx of ours. Codes:
            dataset_missing, bq_unconfigured, upstream_error, query_failed, spend_table_missing, stale_spend_data,
            mixed_currencies.'
          content:
            application/json:
              schema:
                type: object
                properties:
                  error:
                    type: string
                    description: Human-readable message. For an upstream taxonomy failure the leading
                      '[code] ' is stripped out into `code`.
                  code:
                    type: string
                    description: Mythic's machine-readable code, e.g. dataset_missing, scan_limit_exceeded,
                      insufficient_data, invalid_definition, not_found, rate_limited.
                    example: scan_limit_exceeded
                  retryAfter:
                    type: string
                    description: Seconds to wait; present on rate_limited. Also sent as the Retry-After
                      header.
                required:
                - error
        '429':
          description: 'Rate limited. Either the gateway''s own limiter (body {error, message, retryAfter}
            plus X-RateLimit-* headers) or Mythic''s 120 req/10s per-location limit (code: rate_limited).
            Retry-After is forwarded in both cases.'
          headers:
            Retry-After:
              description: Seconds to wait before retrying.
              schema:
                type: string
          content:
            application/json:
              schema:
                type: object
                properties:
                  error:
                    type: string
                    description: Human-readable message. For an upstream taxonomy failure the leading
                      '[code] ' is stripped out into `code`.
                  code:
                    type: string
                    description: Mythic's machine-readable code, e.g. dataset_missing, scan_limit_exceeded,
                      insufficient_data, invalid_definition, not_found, rate_limited.
                    example: scan_limit_exceeded
                  retryAfter:
                    type: string
                    description: Seconds to wait; present on rate_limited. Also sent as the Retry-After
                      header.
                required:
                - error
    delete:
      operationId: deleteGeoExperiment
      tags:
      - incrementality
      summary: Delete a geo-holdout experiment
      description: 'Requires scope `incrementality:write` AND an agency-scoped API key: a client-bound
        key is rejected with 403 `This operation requires an agency-scoped API key` before the scope check
        is reached. Writes change real Meta ad targeting.


        Mythic has no edit endpoint, so delete + recreate is the update path. Deleting a computed experiment
        also removes its coefficient from the progressive-truth median.


        Unlike the GET reads, this route''s fallback error status is 400, so an unmapped failure surfaces
        as 400 unless Mythic returns not_found.'
      parameters:
      - name: X-Client-Id
        in: header
        required: false
        description: 'REQUIRED for agency-scoped API keys: names the target client by id, slug or name.
          Ignored for client-bound keys. Omitting it on an agency key returns 400; no match returns 404;
          an ambiguous name returns 409.'
        schema:
          type: string
      - name: id
        in: path
        required: true
        description: Experiment id.
        schema:
          type: string
      responses:
        '200':
          description: Deleted. The id is echoed back by the core, not read from the upstream body.
          content:
            application/json:
              schema:
                type: object
                properties:
                  id:
                    type: string
                  deleted:
                    type: boolean
                    enum:
                    - true
                required:
                - id
                - deleted
        '400':
          description: 'The caller''s request is wrong, or an unmapped failure (400 is this route''s fallback
            status). Mapped codes: invalid_config, invalid_definition, unsupported_channel, scope_rejected.
            Also `Incrementality is not configured for this client (Mythic Analytics not linked)`.'
          content:
            application/json:
              schema:
                type: object
                properties:
                  error:
                    type: string
                    description: Human-readable message. For an upstream taxonomy failure the leading
                      '[code] ' is stripped out into `code`.
                  code:
                    type: string
                    description: Mythic's machine-readable code, e.g. dataset_missing, scan_limit_exceeded,
                      insufficient_data, invalid_definition, not_found, rate_limited.
                    example: scan_limit_exceeded
                  retryAfter:
                    type: string
                    description: Seconds to wait; present on rate_limited. Also sent as the Retry-After
                      header.
                required:
                - error
        '401':
          description: Missing, invalid, revoked or expired API key, or a key that resolves to neither
            a client nor an agency.
          content:
            application/json:
              schema:
                type: object
                properties:
                  error:
                    type: string
                  message:
                    type: string
                required:
                - error
        '403':
          description: 'Permission denied: key missing the required scope, its run-as user deactivated,
            or (on writes) a non-agency key. Body is {error, message} from the middleware, or {error,
            code} when Mythic refused (agency_required, hipaa_restricted).'
          content:
            application/json:
              schema:
                type: object
                properties:
                  error:
                    type: string
                  message:
                    type: string
                required:
                - error
        '404':
          description: '`not_found` from Mythic, or this route''s 404 fallback (`Experiment not found`)
            when the failure carries no mapped code.'
          content:
            application/json:
              schema:
                type: object
                properties:
                  error:
                    type: string
                    description: Human-readable message. For an upstream taxonomy failure the leading
                      '[code] ' is stripped out into `code`.
                  code:
                    type: string
                    description: Mythic's machine-readable code, e.g. dataset_missing, scan_limit_exceeded,
                      insufficient_data, invalid_definition, not_found, rate_limited.
                    example: scan_limit_exceeded
                  retryAfter:
                    type: string
                    description: Seconds to wait; present on rate_limited. Also sent as the Retry-After
                      header.
                required:
                - error
        '422':
          description: 'Well-formed but the data cannot answer it: insufficient_data, result_truncated.
            Also a Mythic 2xx carrying success:false.'
          content:
            application/json:
              schema:
                type: object
                properties:
                  error:
                    type: string
                    description: Human-readable message. For an upstream taxonomy failure the leading
                      '[code] ' is stripped out into `code`.
                  code:
                    type: string
                    description: Mythic's machine-readable code, e.g. dataset_missing, scan_limit_exceeded,
                      insufficient_data, invalid_definition, not_found, rate_limited.
                    example: scan_limit_exceeded
                  retryAfter:
                    type: string
                    description: Seconds to wait; present on rate_limited. Also sent as the Retry-After
                      header.
                required:
                - error
        '424':
          description: 'Upstream fault, deliberately reported as 424 rather than a 5xx of ours. Codes:
            dataset_missing, bq_unconfigured, upstream_error, query_failed, spend_table_missing, stale_spend_data,
            mixed_currencies.'
          content:
            application/json:
              schema:
                type: object
                properties:
                  error:
                    type: string
                    description: Human-readable message. For an upstream taxonomy failure the leading
                      '[code] ' is stripped out into `code`.
                  code:
                    type: string
                    description: Mythic's machine-readable code, e.g. dataset_missing, scan_limit_exceeded,
                      insufficient_data, invalid_definition, not_found, rate_limited.
                    example: scan_limit_exceeded
                  retryAfter:
                    type: string
                    description: Seconds to wait; present on rate_limited. Also sent as the Retry-After
                      header.
                required:
                - error
        '429':
          description: 'Rate limited. Either the gateway''s own limiter (body {error, message, retryAfter}
            plus X-RateLimit-* headers) or Mythic''s 120 req/10s per-location limit (code: rate_limited).
            Retry-After is forwarded in both cases.'
          headers:
            Retry-After:
              description: Seconds to wait before retrying.
              schema:
                type: string
          content:
            application/json:
              schema:
                type: object
                properties:
                  error:
                    type: string
                    description: Human-readable message. For an upstream taxonomy failure the leading
                      '[code] ' is stripped out into `code`.
                  code:
                    type: string
                    description: Mythic's machine-readable code, e.g. dataset_missing, scan_limit_exceeded,
                      insufficient_data, invalid_definition, not_found, rate_limited.
                    example: scan_limit_exceeded
                  retryAfter:
                    type: string
                    description: Seconds to wait; present on rate_limited. Also sent as the Retry-After
                      header.
                required:
                - error
  /v1/incrementality/experiments/{id}/compute:
    post:
      operationId: computeGeoExperiment
      tags:
      - incrementality
      summary: Compute an experiment's incrementality result
      description: 'Requires scope `incrementality:write` AND an agency-scoped API key: a client-bound
        key is rejected with 403 `This operation requires an agency-scoped API key` before the scope check
        is reached. Writes change real Meta ad targeting.


        Runs the weighted-synthetic-control difference-in-differences. Gated on the measured window: before
        it closes Mythic returns 409 window_not_complete. Pass allow_partial:true for a provisional mid-window
        read, which is stamped partial on the result and excluded from /coefficients.


        The body is effectively optional: a malformed or absent body falls back to an empty object, which
        means allow_partial:false. No Zod validator on this route.'
      parameters:
      - name: X-Client-Id
        in: header
        required: false
        description: 'REQUIRED for agency-scoped API keys: names the target client by id, slug or name.
          Ignored for client-bound keys. Omitting it on an agency key returns 400; no match returns 404;
          an ambiguous name returns 409.'
        schema:
          type: string
      - name: id
        in: path
        required: true
        description: Experiment id.
        schema:
          type: string
      requestBody:
        required: false
        content:
          application/json:
            schema:
              type: object
              properties:
                allow_partial:
                  type: boolean
                  default: false
                  description: Coerced with a double negation. True returns a provisional mid-window result.
      responses:
        '200':
          description: Compute result. Field-level shape owned by Mythic.
          content:
            application/json:
              schema:
                type: object
                properties:
                  success:
                    type: boolean
                    enum:
                    - true
                  result:
                    type: object
                    additionalProperties: true
                required:
                - success
                - result
        '400':
          description: 'The caller''s request is wrong, or an unmapped failure (400 is this route''s fallback
            status). Mapped codes: invalid_config, invalid_definition, unsupported_channel, scope_rejected.
            Also `Incrementality is not configured for this client (Mythic Analytics not linked)`.'
          content:
            application/json:
              schema:
                type: object
                properties:
                  error:
                    type: string
                    description: Human-readable message. For an upstream taxonomy failure the leading
                      '[code] ' is stripped out into `code`.
                  code:
                    type: string
                    description: Mythic's machine-readable code, e.g. dataset_missing, scan_limit_exceeded,
                      insufficient_data, invalid_definition, not_found, rate_limited.
                    example: scan_limit_exceeded
                  retryAfter:
                    type: string
                    description: Seconds to wait; present on rate_limited. Also sent as the Retry-After
                      header.
                required:
                - error
        '401':
          description: Missing, invalid, revoked or expired API key, or a key that resolves to neither
            a client nor an agency.
          content:
            application/json:
              schema:
                type: object
                properties:
                  error:
                    type: string
                  message:
                    type: string
                required:
                - error
        '403':
          description: 'Permission denied: key missing the required scope, its run-as user deactivated,
            or (on writes) a non-agency key. Body is {error, message} from the middleware, or {error,
            code} when Mythic refused (agency_required, hipaa_restricted).'
          content:
            application/json:
              schema:
                type: object
                properties:
                  error:
                    type: string
                  message:
                    type: string
                required:
                - error
        '404':
          description: '`not_found` from Mythic, or this route''s 404 fallback (`Experiment not found`)
            when the failure carries no mapped code.'
          content:
            application/json:
              schema:
                type: object
                properties:
                  error:
                    type: string
                    description: Human-readable message. For an upstream taxonomy failure the leading
                      '[code] ' is stripped out into `code`.
                  code:
                    type: string
                    description: Mythic's machine-readable code, e.g. dataset_missing, scan_limit_exceeded,
                      insufficient_data, invalid_definition, not_found, rate_limited.
                    example: scan_limit_exceeded
                  retryAfter:
                    type: string
                    description: Seconds to wait; present on rate_limited. Also sent as the Retry-After
                      header.
                required:
                - error
        '409':
          description: '`window_not_complete`: the measured window has not closed. Retry with allow_partial:true
            for a provisional read.'
          content:
            application/json:
              schema:
                type: object
                properties:
                  error:
                    type: string
                    description: Human-readable message. For an upstream taxonomy failure the leading
                      '[code] ' is stripped out into `code`.
                  code:
                    type: string
                    description: Mythic's machine-readable code, e.g. dataset_missing, scan_limit_exceeded,
                      insufficient_data, invalid_definition, not_found, rate_limited.
                    example: scan_limit_exceeded
                  retryAfter:
                    type: string
                    description: Seconds to wait; present on rate_limited. Also sent as the Retry-After
                      header.
                required:
                - error
        '413':
          description: '`scan_limit_exceeded`: the query would scan too much data. Retry with a shorter
            lookback_days.'
          content:
            application/json:
              schema:
                type: object
                properties:
                  error:
                    type: string
                    description: Human-readable message. For an upstream taxonomy failure the leading
                      '[code] ' is stripped out into `code`.
                  code:
                    type: string
                    description: Mythic's machine-readable code, e.g. dataset_missing, scan_limit_exceeded,
                      insufficient_data, invalid_definition, not_found, rate_limited.
                    example: scan_limit_exceeded
                  retryAfter:
                    type: string
                    description: Seconds to wait; present on rate_limited. Also sent as the Retry-After
                      header.
                required:
                - error
        '422':
          description: 'Well-formed but the data cannot answer it: insufficient_data, result_truncated.
            Also a Mythic 2xx carrying success:false.'
          content:
            application/json:
              schema:
                type: object
                properties:
                  error:
                    type: string
                    description: Human-readable message. For an upstream taxonomy failure the leading
                      '[code] ' is stripped out into `code`.
                  code:
                    type: string
                    description: Mythic's machine-readable code, e.g. dataset_missing, scan_limit_exceeded,
                      insufficient_data, invalid_definition, not_found, rate_limited.
                    example: scan_limit_exceeded
                  retryAfter:
                    type: string
                    description: Seconds to wait; present on rate_limited. Also sent as the Retry-After
                      header.
                required:
                - error
        '424':
          description: 'Upstream fault, deliberately reported as 424 rather than a 5xx of ours. Codes:
            dataset_missing, bq_unconfigured, upstream_error, query_failed, spend_table_missing, stale_spend_data,
            mixed_currencies.'
          content:
            application/json:
              schema:
                type: object
                properties:
                  error:
                    type: string
                    description: Human-readable message. For an upstream taxonomy failure the leading
                      '[code] ' is stripped out into `code`.
                  code:
                    type: string
                    description: Mythic's machine-readable code, e.g. dataset_missing, scan_limit_exceeded,
                      insufficient_data, invalid_definition, not_found, rate_limited.
                    example: scan_limit_exceeded
                  retryAfter:
                    type: string
                    description: Seconds to wait; present on rate_limited. Also sent as the Retry-After
                      header.
                required:
                - error
        '429':
          description: 'Rate limited. Either the gateway''s own limiter (body {error, message, retryAfter}
            plus X-RateLimit-* headers) or Mythic''s 120 req/10s per-location limit (code: rate_limited).
            Retry-After is forwarded in both cases.'
          headers:
            Retry-After:
              description: Seconds to wait before retrying.
              schema:
                type: string
          content:
            application/json:
              schema:
                type: object
                properties:
                  error:
                    type: string
                    description: Human-readable message. For an upstream taxonomy failure the leading
                      '[code] ' is stripped out into `code`.
                  code:
                    type: string
                    description: Mythic's machine-readable code, e.g. dataset_missing, scan_limit_exceeded,
                      insufficient_data, invalid_definition, not_found, rate_limited.
                    example: scan_limit_exceeded
                  retryAfter:
                    type: string
                    description: Seconds to wait; present on rate_limited. Also sent as the Retry-After
                      header.
                required:
                - error
  /v1/incrementality/experiments/{id}/launch-plan:
    get:
      operationId: getGeoLaunchPlan
      tags:
      - incrementality
      summary: Get the Meta launch plan for an experiment
      description: 'Requires scope `incrementality:read`.


        The operator-facing plan for putting the experiment live on Meta: which geos to target for the
        test cell and which to hold out. Fallback error status is 404 (Experiment not found). The 200
        body is Mythic''s `data` passed through.'
      parameters:
      - name: X-Client-Id
        in: header
        required: false
        description: 'REQUIRED for agency-scoped API keys: names the target client by id, slug or name.
          Ignored for client-bound keys. Omitting it on an agency key returns 400; no match returns 404;
          an ambiguous name returns 409.'
        schema:
          type: string
      - name: id
        in: path
        required: true
        description: Experiment id.
        schema:
          type: string
      responses:
        '200':
          description: Mythic's launch-plan payload. Field-level shape owned by Mythic.
          content:
            application/json:
              schema:
                type: object
                additionalProperties: true
        '400':
          description: 'The caller''s request is wrong: invalid_config, invalid_definition, unsupported_channel,
            scope_rejected. Also `Incrementality is not configured for this client (Mythic Analytics not
            linked)`.'
          content:
            application/json:
              schema:
                type: object
                properties:
                  error:
                    type: string
                    description: Human-readable message. For an upstream taxonomy failure the leading
                      '[code] ' is stripped out into `code`.
                  code:
                    type: string
                    description: Mythic's machine-readable code, e.g. dataset_missing, scan_limit_exceeded,
                      insufficient_data, invalid_definition, not_found, rate_limited.
                    example: scan_limit_exceeded
                  retryAfter:
                    type: string
                    description: Seconds to wait; present on rate_limited. Also sent as the Retry-After
                      header.
                required:
                - error
        '401':
          description: Missing, invalid, revoked or expired API key, or a key that resolves to neither
            a client nor an agency.
          content:
            application/json:
              schema:
                type: object
                properties:
                  error:
                    type: string
                  message:
                    type: string
                required:
                - error
        '403':
          description: 'Permission denied: key missing the required scope, its run-as user deactivated,
            or (on writes) a non-agency key. Body is {error, message} from the middleware, or {error,
            code} when Mythic refused (agency_required, hipaa_restricted).'
          content:
            application/json:
              schema:
                type: object
                properties:
                  error:
                    type: string
                  message:
                    type: string
                required:
                - error
        '404':
          description: '`not_found` from Mythic, or this route''s 404 fallback (`Experiment not found`)
            when the failure carries no mapped code.'
          content:
            application/json:
              schema:
                type: object
                properties:
                  error:
                    type: string
                    description: Human-readable message. For an upstream taxonomy failure the leading
                      '[code] ' is stripped out into `code`.
                  code:
                    type: string
                    description: Mythic's machine-readable code, e.g. dataset_missing, scan_limit_exceeded,
                      insufficient_data, invalid_definition, not_found, rate_limited.
                    example: scan_limit_exceeded
                  retryAfter:
                    type: string
                    description: Seconds to wait; present on rate_limited. Also sent as the Retry-After
                      header.
                required:
                - error
        '422':
          description: 'Well-formed but the data cannot answer it: insufficient_data, result_truncated.
            Also a Mythic 2xx carrying success:false.'
          content:
            application/json:
              schema:
                type: object
                properties:
                  error:
                    type: string
                    description: Human-readable message. For an upstream taxonomy failure the leading
                      '[code] ' is stripped out into `code`.
                  code:
                    type: string
                    description: Mythic's machine-readable code, e.g. dataset_missing, scan_limit_exceeded,
                      insufficient_data, invalid_definition, not_found, rate_limited.
                    example: scan_limit_exceeded
                  retryAfter:
                    type: string
                    description: Seconds to wait; present on rate_limited. Also sent as the Retry-After
                      header.
                required:
                - error
        '424':
          description: 'Upstream fault, deliberately reported as 424 rather than a 5xx of ours. Codes:
            dataset_missing, bq_unconfigured, upstream_error, query_failed, spend_table_missing, stale_spend_data,
            mixed_currencies.'
          content:
            application/json:
              schema:
                type: object
                properties:
                  error:
                    type: string
                    description: Human-readable message. For an upstream taxonomy failure the leading
                      '[code] ' is stripped out into `code`.
                  code:
                    type: string
                    description: Mythic's machine-readable code, e.g. dataset_missing, scan_limit_exceeded,
                      insufficient_data, invalid_definition, not_found, rate_limited.
                    example: scan_limit_exceeded
                  retryAfter:
                    type: string
                    description: Seconds to wait; present on rate_limited. Also sent as the Retry-After
                      header.
                required:
                - error
        '429':
          description: 'Rate limited. Either the gateway''s own limiter (body {error, message, retryAfter}
            plus X-RateLimit-* headers) or Mythic''s 120 req/10s per-location limit (code: rate_limited).
            Retry-After is forwarded in both cases.'
          headers:
            Retry-After:
              description: Seconds to wait before retrying.
              schema:
                type: string
          content:
            application/json:
              schema:
                type: object
                properties:
                  error:
                    type: string
                    description: Human-readable message. For an upstream taxonomy failure the leading
                      '[code] ' is stripped out into `code`.
                  code:
                    type: string
                    description: Mythic's machine-readable code, e.g. dataset_missing, scan_limit_exceeded,
                      insufficient_data, invalid_definition, not_found, rate_limited.
                    example: scan_limit_exceeded
                  retryAfter:
                    type: string
                    description: Seconds to wait; present on rate_limited. Also sent as the Retry-After
                      header.
                required:
                - error
  /v1/incrementality/experiments/{id}/validate:
    get:
      operationId: validateGeoExperiment
      tags:
      - incrementality
      summary: Run the A/A placebo or drift check on an experiment
      description: 'Requires scope `incrementality:read`.


        An A/A placebo check before launch, or an interim drift check while the experiment runs. Fallback
        error status is 404 (Experiment not found). The 200 body is Mythic''s `data` passed through.'
      parameters:
      - name: X-Client-Id
        in: header
        required: false
        description: 'REQUIRED for agency-scoped API keys: names the target client by id, slug or name.
          Ignored for client-bound keys. Omitting it on an agency key returns 400; no match returns 404;
          an ambiguous name returns 409.'
        schema:
          type: string
      - name: id
        in: path
        required: true
        description: Experiment id.
        schema:
          type: string
      responses:
        '200':
          description: Mythic's validation payload. Field-level shape owned by Mythic.
          content:
            application/json:
              schema:
                type: object
                additionalProperties: true
        '400':
          description: 'The caller''s request is wrong: invalid_config, invalid_definition, unsupported_channel,
            scope_rejected. Also `Incrementality is not configured for this client (Mythic Analytics not
            linked)`.'
          content:
            application/json:
              schema:
                type: object
                properties:
                  error:
                    type: string
                    description: Human-readable message. For an upstream taxonomy failure the leading
                      '[code] ' is stripped out into `code`.
                  code:
                    type: string
                    description: Mythic's machine-readable code, e.g. dataset_missing, scan_limit_exceeded,
                      insufficient_data, invalid_definition, not_found, rate_limited.
                    example: scan_limit_exceeded
                  retryAfter:
                    type: string
                    description: Seconds to wait; present on rate_limited. Also sent as the Retry-After
                      header.
                required:
                - error
        '401':
          description: Missing, invalid, revoked or expired API key, or a key that resolves to neither
            a client nor an agency.
          content:
            application/json:
              schema:
                type: object
                properties:
                  error:
                    type: string
                  message:
                    type: string
                required:
                - error
        '403':
          description: 'Permission denied: key missing the required scope, its run-as user deactivated,
            or (on writes) a non-agency key. Body is {error, message} from the middleware, or {error,
            code} when Mythic refused (agency_required, hipaa_restricted).'
          content:
            application/json:
              schema:
                type: object
                properties:
                  error:
                    type: string
                  message:
                    type: string
                required:
                - error
        '404':
          description: '`not_found` from Mythic, or this route''s 404 fallback (`Experiment not found`)
            when the failure carries no mapped code.'
          content:
            application/json:
              schema:
                type: object
                properties:
                  error:
                    type: string
                    description: Human-readable message. For an upstream taxonomy failure the leading
                      '[code] ' is stripped out into `code`.
                  code:
                    type: string
                    description: Mythic's machine-readable code, e.g. dataset_missing, scan_limit_exceeded,
                      insufficient_data, invalid_definition, not_found, rate_limited.
                    example: scan_limit_exceeded
                  retryAfter:
                    type: string
                    description: Seconds to wait; present on rate_limited. Also sent as the Retry-After
                      header.
                required:
                - error
        '422':
          description: 'Well-formed but the data cannot answer it: insufficient_data, result_truncated.
            Also a Mythic 2xx carrying success:false.'
          content:
            application/json:
              schema:
                type: object
                properties:
                  error:
                    type: string
                    description: Human-readable message. For an upstream taxonomy failure the leading
                      '[code] ' is stripped out into `code`.
                  code:
                    type: string
                    description: Mythic's machine-readable code, e.g. dataset_missing, scan_limit_exceeded,
                      insufficient_data, invalid_definition, not_found, rate_limited.
                    example: scan_limit_exceeded
                  retryAfter:
                    type: string
                    description: Seconds to wait; present on rate_limited. Also sent as the Retry-After
                      header.
                required:
                - error
        '424':
          description: 'Upstream fault, deliberately reported as 424 rather than a 5xx of ours. Codes:
            dataset_missing, bq_unconfigured, upstream_error, query_failed, spend_table_missing, stale_spend_data,
            mixed_currencies.'
          content:
            application/json:
              schema:
                type: object
                properties:
                  error:
                    type: string
                    description: Human-readable message. For an upstream taxonomy failure the leading
                      '[code] ' is stripped out into `code`.
                  code:
                    type: string
                    description: Mythic's machine-readable code, e.g. dataset_missing, scan_limit_exceeded,
                      insufficient_data, invalid_definition, not_found, rate_limited.
                    example: scan_limit_exceeded
                  retryAfter:
                    type: string
                    description: Seconds to wait; present on rate_limited. Also sent as the Retry-After
                      header.
                required:
                - error
        '429':
          description: 'Rate limited. Either the gateway''s own limiter (body {error, message, retryAfter}
            plus X-RateLimit-* headers) or Mythic''s 120 req/10s per-location limit (code: rate_limited).
            Retry-After is forwarded in both cases.'
          headers:
            Retry-After:
              description: Seconds to wait before retrying.
              schema:
                type: string
          content:
            application/json:
              schema:
                type: object
                properties:
                  error:
                    type: string
                    description: Human-readable message. For an upstream taxonomy failure the leading
                      '[code] ' is stripped out into `code`.
                  code:
                    type: string
                    description: Mythic's machine-readable code, e.g. dataset_missing, scan_limit_exceeded,
                      insufficient_data, invalid_definition, not_found, rate_limited.
                    example: scan_limit_exceeded
                  retryAfter:
                    type: string
                    description: Seconds to wait; present on rate_limited. Also sent as the Retry-After
                      header.
                required:
                - error
  /v1/incrementality/markets:
    get:
      operationId: suggestControlMarkets
      tags:
      - incrementality
      summary: Rank control-market candidates for a geo-holdout test
      description: 'Requires scope `incrementality:read`.


        Ranks candidate control markets by how closely their daily conversions track the test set. Either
        seed `test_regions`, or supply `event_name` and let Mythic rank markets for that event. With neither,
        the request fails with 400 (test_regions or event_name is required).


        `test_regions` arrives as a comma-separated string; the route splits, trims and drops empties
        before calling the core. The 200 body is Mythic''s `data` passed through.'
      parameters:
      - name: X-Client-Id
        in: header
        required: false
        description: 'REQUIRED for agency-scoped API keys: names the target client by id, slug or name.
          Ignored for client-bound keys. Omitting it on an agency key returns 400; no match returns 404;
          an ambiguous name returns 409.'
        schema:
          type: string
      - name: test_regions
        in: query
        required: false
        description: Comma-separated test regions, e.g. CA,NV,AZ. Required unless event_name is supplied.
        schema:
          type: string
      - name: measure
        in: query
        required: false
        description: '`revenue` (monetary value, Mythic''s default) or `count` for lead-gen events that
          carry no value.'
        schema:
          type: string
          enum:
          - revenue
          - count
      - name: event_name
        in: query
        required: false
        description: Conversion event to measure. Mythic defaults to the literal `purchase` event.
        schema:
          type: string
      - name: json_path
        in: query
        required: false
        description: Where the monetary value lives in the event properties, e.g. $.meta_amount. Mythic
          default $.value.
        schema:
          type: string
      - name: lookback_days
        in: query
        required: false
        description: 'Window in days, clamped by the core to 1..365. This is the lever for a scan_limit_exceeded
          failure: narrow the window.'
        schema:
          type: integer
          minimum: 1
          maximum: 365
      responses:
        '200':
          description: Mythic's market-suggestion payload, plus `notice` when the window was narrowed.
          content:
            application/json:
              schema:
                type: object
                additionalProperties: true
                properties:
                  notice:
                    type: string
                    description: Present only when Mythic silently shortened the lookback window. Verdicts
                      are then NOT comparable with a full-window result.
                  window_narrowed:
                    type: object
                    additionalProperties: true
                    properties:
                      requested_lookback_days:
                        type: integer
                      used_lookback_days:
                        type: integer
                      reason:
                        type: string
        '400':
          description: 'The caller''s request is wrong, or an unmapped failure (400 is this route''s fallback
            status). Mapped codes: invalid_config, invalid_definition, unsupported_channel, scope_rejected.
            Also `Incrementality is not configured for this client (Mythic Analytics not linked)`.'
          content:
            application/json:
              schema:
                type: object
                properties:
                  error:
                    type: string
                    description: Human-readable message. For an upstream taxonomy failure the leading
                      '[code] ' is stripped out into `code`.
                  code:
                    type: string
                    description: Mythic's machine-readable code, e.g. dataset_missing, scan_limit_exceeded,
                      insufficient_data, invalid_definition, not_found, rate_limited.
                    example: scan_limit_exceeded
                  retryAfter:
                    type: string
                    description: Seconds to wait; present on rate_limited. Also sent as the Retry-After
                      header.
                required:
                - error
        '401':
          description: Missing, invalid, revoked or expired API key, or a key that resolves to neither
            a client nor an agency.
          content:
            application/json:
              schema:
                type: object
                properties:
                  error:
                    type: string
                  message:
                    type: string
                required:
                - error
        '403':
          description: 'Permission denied: key missing the required scope, its run-as user deactivated,
            or (on writes) a non-agency key. Body is {error, message} from the middleware, or {error,
            code} when Mythic refused (agency_required, hipaa_restricted).'
          content:
            application/json:
              schema:
                type: object
                properties:
                  error:
                    type: string
                  message:
                    type: string
                required:
                - error
        '404':
          description: '`not_found` from Mythic, or this route''s 404 fallback (`Experiment not found`)
            when the failure carries no mapped code.'
          content:
            application/json:
              schema:
                type: object
                properties:
                  error:
                    type: string
                    description: Human-readable message. For an upstream taxonomy failure the leading
                      '[code] ' is stripped out into `code`.
                  code:
                    type: string
                    description: Mythic's machine-readable code, e.g. dataset_missing, scan_limit_exceeded,
                      insufficient_data, invalid_definition, not_found, rate_limited.
                    example: scan_limit_exceeded
                  retryAfter:
                    type: string
                    description: Seconds to wait; present on rate_limited. Also sent as the Retry-After
                      header.
                required:
                - error
        '413':
          description: '`scan_limit_exceeded`: the query would scan too much data. Retry with a shorter
            lookback_days.'
          content:
            application/json:
              schema:
                type: object
                properties:
                  error:
                    type: string
                    description: Human-readable message. For an upstream taxonomy failure the leading
                      '[code] ' is stripped out into `code`.
                  code:
                    type: string
                    description: Mythic's machine-readable code, e.g. dataset_missing, scan_limit_exceeded,
                      insufficient_data, invalid_definition, not_found, rate_limited.
                    example: scan_limit_exceeded
                  retryAfter:
                    type: string
                    description: Seconds to wait; present on rate_limited. Also sent as the Retry-After
                      header.
                required:
                - error
        '422':
          description: 'Well-formed but the data cannot answer it: insufficient_data, result_truncated.
            Also a Mythic 2xx carrying success:false.'
          content:
            application/json:
              schema:
                type: object
                properties:
                  error:
                    type: string
                    description: Human-readable message. For an upstream taxonomy failure the leading
                      '[code] ' is stripped out into `code`.
                  code:
                    type: string
                    description: Mythic's machine-readable code, e.g. dataset_missing, scan_limit_exceeded,
                      insufficient_data, invalid_definition, not_found, rate_limited.
                    example: scan_limit_exceeded
                  retryAfter:
                    type: string
                    description: Seconds to wait; present on rate_limited. Also sent as the Retry-After
                      header.
                required:
                - error
        '424':
          description: 'Upstream fault, deliberately reported as 424 rather than a 5xx of ours. Codes:
            dataset_missing, bq_unconfigured, upstream_error, query_failed, spend_table_missing, stale_spend_data,
            mixed_currencies.'
          content:
            application/json:
              schema:
                type: object
                properties:
                  error:
                    type: string
                    description: Human-readable message. For an upstream taxonomy failure the leading
                      '[code] ' is stripped out into `code`.
                  code:
                    type: string
                    description: Mythic's machine-readable code, e.g. dataset_missing, scan_limit_exceeded,
                      insufficient_data, invalid_definition, not_found, rate_limited.
                    example: scan_limit_exceeded
                  retryAfter:
                    type: string
                    description: Seconds to wait; present on rate_limited. Also sent as the Retry-After
                      header.
                required:
                - error
        '429':
          description: 'Rate limited. Either the gateway''s own limiter (body {error, message, retryAfter}
            plus X-RateLimit-* headers) or Mythic''s 120 req/10s per-location limit (code: rate_limited).
            Retry-After is forwarded in both cases.'
          headers:
            Retry-After:
              description: Seconds to wait before retrying.
              schema:
                type: string
          content:
            application/json:
              schema:
                type: object
                properties:
                  error:
                    type: string
                    description: Human-readable message. For an upstream taxonomy failure the leading
                      '[code] ' is stripped out into `code`.
                  code:
                    type: string
                    description: Mythic's machine-readable code, e.g. dataset_missing, scan_limit_exceeded,
                      insufficient_data, invalid_definition, not_found, rate_limited.
                    example: scan_limit_exceeded
                  retryAfter:
                    type: string
                    description: Seconds to wait; present on rate_limited. Also sent as the Retry-After
                      header.
                required:
                - error
  /v1/memories:
    get:
      operationId: listMemories
      tags:
      - memories
      summary: List memories for the resolved client
      description: 'Requires scope `memories:read` OR `memories:write` (requireAnyScope). Returns the
        API key user''s personal memories plus client-shared ones, newest first (core default limit 50).
        A key with no associated user sees SHARED memories only. Agency-internal (`agencyOnly`) memories
        are included only for agency-scoped keys; client keys are fail-closed.

        '
      parameters:
      - name: category
        in: query
        required: false
        description: 'Filter by category. Values other than `daily` / `long_term` are ignored (treated
          as no filter).

          '
        schema:
          type: string
          enum:
          - long_term
          - daily
      - name: X-Client-Id
        in: header
        required: false
        description: 'Required for agency-wide API keys — target client id, slug, or name. Ignored for
          client-scoped keys.

          '
        schema:
          type: string
      responses:
        '200':
          description: The visible memories
          content:
            application/json:
              schema:
                type: object
                required:
                - memories
                properties:
                  memories:
                    type: array
                    items:
                      type: object
                      required:
                      - id
                      - agencyId
                      - clientId
                      - userId
                      - agentId
                      - category
                      - title
                      - content
                      - tags
                      - shared
                      - agencyOnly
                      - createdAt
                      - updatedAt
                      properties:
                        id:
                          type: string
                          format: uuid
                        agencyId:
                          type: string
                        clientId:
                          type: string
                        userId:
                          type: string
                          nullable: true
                        agentId:
                          type: string
                          nullable: true
                        category:
                          type: string
                          enum:
                          - long_term
                          - daily
                        title:
                          type: string
                        content:
                          type: string
                        tags:
                          type: array
                          items:
                            type: string
                        shared:
                          type: boolean
                        agencyOnly:
                          type: boolean
                        createdAt:
                          type: string
                          format: date-time
                        updatedAt:
                          type: string
                          format: date-time
        '400':
          description: Agency-wide key did not send the `X-Client-Id` header
          content:
            application/json:
              schema:
                type: object
                properties:
                  error:
                    type: string
                  message:
                    type: string
        '401':
          description: Missing, malformed, revoked, or inactive API key
          content:
            application/json:
              schema:
                type: object
                properties:
                  error:
                    type: string
                  message:
                    type: string
        '403':
          description: Permission denied — key lacks memories:read / memories:write
          content:
            application/json:
              schema:
                type: object
                properties:
                  error:
                    type: string
                  message:
                    type: string
        '404':
          description: No client in this agency matches `X-Client-Id`
          content:
            application/json:
              schema:
                type: object
                properties:
                  error:
                    type: string
                  message:
                    type: string
        '409':
          description: '`X-Client-Id` name is ambiguous — send an id instead'
          content:
            application/json:
              schema:
                type: object
                properties:
                  error:
                    type: string
                  message:
                    type: string
        '429':
          description: Rate limit exceeded for this API key
          content:
            application/json:
              schema:
                type: object
                properties:
                  error:
                    type: string
                  message:
                    type: string
    post:
      operationId: createMemory
      tags:
      - memories
      summary: Create a memory
      description: 'Requires scope `memories:write`. The memory is authored by the API key''s associated
        user; a key with no user always creates a client-SHARED memory (the request''s `shared` value
        is forced to true). `agencyOnly: true` requires an agency-scoped key, otherwise 403. `category`
        defaults to `long_term`, `tags` to `[]`.

        '
      parameters:
      - name: X-Client-Id
        in: header
        required: false
        description: Required for agency-wide API keys — target client id, slug, or name.
        schema:
          type: string
      requestBody:
        required: true
        content:
          application/json:
            schema:
              type: object
              required:
              - title
              - content
              properties:
                title:
                  type: string
                  minLength: 1
                content:
                  type: string
                  minLength: 1
                category:
                  type: string
                  enum:
                  - long_term
                  - daily
                tags:
                  type: array
                  items:
                    type: string
                shared:
                  type: boolean
                  description: 'false = personal to the key''s user, true = shared with everyone on the
                    client. Forced to true for keys with no associated user.

                    '
                agencyOnly:
                  type: boolean
                  description: Agency-internal, hidden from client users. Agency-scoped keys only.
      responses:
        '201':
          description: The created memory
          content:
            application/json:
              schema:
                type: object
                required:
                - memory
                properties:
                  memory:
                    type: object
                    required:
                    - id
                    - agencyId
                    - clientId
                    - userId
                    - agentId
                    - category
                    - title
                    - content
                    - tags
                    - shared
                    - agencyOnly
                    - createdAt
                    - updatedAt
                    properties:
                      id:
                        type: string
                        format: uuid
                      agencyId:
                        type: string
                      clientId:
                        type: string
                      userId:
                        type: string
                        nullable: true
                      agentId:
                        type: string
                        nullable: true
                      category:
                        type: string
                        enum:
                        - long_term
                        - daily
                      title:
                        type: string
                      content:
                        type: string
                      tags:
                        type: array
                        items:
                          type: string
                      shared:
                        type: boolean
                      agencyOnly:
                        type: boolean
                      createdAt:
                        type: string
                        format: date-time
                      updatedAt:
                        type: string
                        format: date-time
        '400':
          description: 'Request body failed validation (zValidator), or an agency-wide key did not send
            `X-Client-Id`.

            '
          content:
            application/json:
              schema:
                type: object
                properties:
                  error:
                    type: string
                  message:
                    type: string
        '401':
          description: Missing, malformed, revoked, or inactive API key
          content:
            application/json:
              schema:
                type: object
                properties:
                  error:
                    type: string
                  message:
                    type: string
        '403':
          description: 'Key lacks `memories:write`, or requested `agencyOnly: true` without an agency-scoped
            key ("agencyOnly memories require an agency-scoped key").

            '
          content:
            application/json:
              schema:
                type: object
                properties:
                  error:
                    type: string
                  message:
                    type: string
        '404':
          description: No client in this agency matches `X-Client-Id`
          content:
            application/json:
              schema:
                type: object
                properties:
                  error:
                    type: string
                  message:
                    type: string
        '409':
          description: '`X-Client-Id` name is ambiguous — send an id instead'
          content:
            application/json:
              schema:
                type: object
                properties:
                  error:
                    type: string
                  message:
                    type: string
        '429':
          description: Rate limit exceeded for this API key
          content:
            application/json:
              schema:
                type: object
                properties:
                  error:
                    type: string
                  message:
                    type: string
  /v1/memories/search:
    get:
      operationId: searchMemories
      tags:
      - memories
      summary: Hybrid semantic + keyword search over visible memories
      description: 'Requires scope `memories:read` OR `memories:write` (requireAnyScope). Searches the
        key user''s personal memories plus client-shared ones (shared-only for keys with no user); agency-internal
        memories are included only for agency-scoped keys. Ranking blends pgvector cosine similarity (0.7)
        with Postgres ts_rank_cd (0.3), falling back to keyword-only when no embedding is available. Core
        default cap is 10 results; an empty or missing `q` returns an empty list.

        '
      parameters:
      - name: q
        in: query
        required: false
        description: Search query. Empty or absent returns an empty results array.
        schema:
          type: string
      - name: X-Client-Id
        in: header
        required: false
        description: Required for agency-wide API keys — target client id, slug, or name.
        schema:
          type: string
      responses:
        '200':
          description: Ranked matches
          content:
            application/json:
              schema:
                type: object
                required:
                - results
                properties:
                  results:
                    type: array
                    items:
                      type: object
                      required:
                      - id
                      - agencyId
                      - clientId
                      - userId
                      - agentId
                      - category
                      - title
                      - content
                      - tags
                      - shared
                      - agencyOnly
                      - createdAt
                      - updatedAt
                      - score
                      - matchType
                      properties:
                        id:
                          type: string
                          format: uuid
                        agencyId:
                          type: string
                        clientId:
                          type: string
                        userId:
                          type: string
                          nullable: true
                        agentId:
                          type: string
                          nullable: true
                        category:
                          type: string
                          enum:
                          - long_term
                          - daily
                        title:
                          type: string
                        content:
                          type: string
                        tags:
                          type: array
                          items:
                            type: string
                        shared:
                          type: boolean
                        agencyOnly:
                          type: boolean
                        createdAt:
                          type: string
                          format: date-time
                        updatedAt:
                          type: string
                          format: date-time
                        score:
                          type: number
                          description: Blended relevance score (higher is better).
                        matchType:
                          type: string
                          enum:
                          - hybrid
                          - semantic
                          - keyword
        '400':
          description: Agency-wide key did not send the `X-Client-Id` header
          content:
            application/json:
              schema:
                type: object
                properties:
                  error:
                    type: string
                  message:
                    type: string
        '401':
          description: Missing, malformed, revoked, or inactive API key
          content:
            application/json:
              schema:
                type: object
                properties:
                  error:
                    type: string
                  message:
                    type: string
        '403':
          description: Permission denied — key lacks memories:read / memories:write
          content:
            application/json:
              schema:
                type: object
                properties:
                  error:
                    type: string
                  message:
                    type: string
        '404':
          description: No client in this agency matches `X-Client-Id`
          content:
            application/json:
              schema:
                type: object
                properties:
                  error:
                    type: string
                  message:
                    type: string
        '409':
          description: '`X-Client-Id` name is ambiguous — send an id instead'
          content:
            application/json:
              schema:
                type: object
                properties:
                  error:
                    type: string
                  message:
                    type: string
        '429':
          description: Rate limit exceeded for this API key
          content:
            application/json:
              schema:
                type: object
                properties:
                  error:
                    type: string
                  message:
                    type: string
  /v1/memories/{id}:
    get:
      operationId: getMemory
      tags:
      - memories
      summary: Get one memory by id
      description: 'Requires scope `memories:read` OR `memories:write` (requireAnyScope). Resolves within
        the resolved client only: shared memories, or the key user''s own personal memory. Agency-internal
        memories resolve only for agency-scoped keys — otherwise the route reports 404.

        '
      parameters:
      - name: id
        in: path
        required: true
        schema:
          type: string
      - name: X-Client-Id
        in: header
        required: false
        description: Required for agency-wide API keys — target client id, slug, or name.
        schema:
          type: string
      responses:
        '200':
          description: The memory
          content:
            application/json:
              schema:
                type: object
                required:
                - memory
                properties:
                  memory:
                    type: object
                    required:
                    - id
                    - agencyId
                    - clientId
                    - userId
                    - agentId
                    - category
                    - title
                    - content
                    - tags
                    - shared
                    - agencyOnly
                    - createdAt
                    - updatedAt
                    properties:
                      id:
                        type: string
                        format: uuid
                      agencyId:
                        type: string
                      clientId:
                        type: string
                      userId:
                        type: string
                        nullable: true
                      agentId:
                        type: string
                        nullable: true
                      category:
                        type: string
                        enum:
                        - long_term
                        - daily
                      title:
                        type: string
                      content:
                        type: string
                      tags:
                        type: array
                        items:
                          type: string
                      shared:
                        type: boolean
                      agencyOnly:
                        type: boolean
                      createdAt:
                        type: string
                        format: date-time
                      updatedAt:
                        type: string
                        format: date-time
        '400':
          description: Agency-wide key did not send the `X-Client-Id` header
          content:
            application/json:
              schema:
                type: object
                properties:
                  error:
                    type: string
                  message:
                    type: string
        '401':
          description: Missing, malformed, revoked, or inactive API key
          content:
            application/json:
              schema:
                type: object
                properties:
                  error:
                    type: string
                  message:
                    type: string
        '403':
          description: Permission denied — key lacks memories:read / memories:write
          content:
            application/json:
              schema:
                type: object
                properties:
                  error:
                    type: string
                  message:
                    type: string
        '404':
          description: '`{"error": "Memory not found"}` when the id does not exist or is not visible to
            this key; also returned when `X-Client-Id` matches no client in the agency.

            '
          content:
            application/json:
              schema:
                type: object
                properties:
                  error:
                    type: string
                  message:
                    type: string
        '409':
          description: '`X-Client-Id` name is ambiguous — send an id instead'
          content:
            application/json:
              schema:
                type: object
                properties:
                  error:
                    type: string
                  message:
                    type: string
        '429':
          description: Rate limit exceeded for this API key
          content:
            application/json:
              schema:
                type: object
                properties:
                  error:
                    type: string
                  message:
                    type: string
    patch:
      operationId: updateMemory
      tags:
      - memories
      summary: Update a memory
      description: 'Requires scope `memories:write`. A key with an associated user may edit that user''s
        own memories plus any shared memory on the client; a key with no user may edit SHARED memories
        only. Setting `agencyOnly: true` requires an agency-scoped key, otherwise 403. Title/content changes
        trigger re-embedding.

        '
      parameters:
      - name: id
        in: path
        required: true
        schema:
          type: string
      - name: X-Client-Id
        in: header
        required: false
        description: Required for agency-wide API keys — target client id, slug, or name.
        schema:
          type: string
      requestBody:
        required: true
        content:
          application/json:
            schema:
              type: object
              description: All fields optional; only supplied fields are written.
              properties:
                title:
                  type: string
                  minLength: 1
                content:
                  type: string
                  minLength: 1
                category:
                  type: string
                  enum:
                  - long_term
                  - daily
                tags:
                  type: array
                  items:
                    type: string
                shared:
                  type: boolean
                agencyOnly:
                  type: boolean
                  description: Agency-scoped keys only; setting true with a client key returns 403.
      responses:
        '200':
          description: The updated memory
          content:
            application/json:
              schema:
                type: object
                required:
                - memory
                properties:
                  memory:
                    type: object
                    required:
                    - id
                    - agencyId
                    - clientId
                    - userId
                    - agentId
                    - category
                    - title
                    - content
                    - tags
                    - shared
                    - agencyOnly
                    - createdAt
                    - updatedAt
                    properties:
                      id:
                        type: string
                        format: uuid
                      agencyId:
                        type: string
                      clientId:
                        type: string
                      userId:
                        type: string
                        nullable: true
                      agentId:
                        type: string
                        nullable: true
                      category:
                        type: string
                        enum:
                        - long_term
                        - daily
                      title:
                        type: string
                      content:
                        type: string
                      tags:
                        type: array
                        items:
                          type: string
                      shared:
                        type: boolean
                      agencyOnly:
                        type: boolean
                      createdAt:
                        type: string
                        format: date-time
                      updatedAt:
                        type: string
                        format: date-time
        '400':
          description: 'Request body failed validation (zValidator), or an agency-wide key did not send
            `X-Client-Id`.

            '
          content:
            application/json:
              schema:
                type: object
                properties:
                  error:
                    type: string
                  message:
                    type: string
        '401':
          description: Missing, malformed, revoked, or inactive API key
          content:
            application/json:
              schema:
                type: object
                properties:
                  error:
                    type: string
                  message:
                    type: string
        '403':
          description: 'Key lacks `memories:write`, or attempted `agencyOnly: true` without an agency-scoped
            key ("Setting agencyOnly requires an agency-scoped key").

            '
          content:
            application/json:
              schema:
                type: object
                properties:
                  error:
                    type: string
                  message:
                    type: string
        '404':
          description: '`{"error": "Memory not found"}` when the id does not exist or is not editable
            by this key; also returned when `X-Client-Id` matches no client in the agency.

            '
          content:
            application/json:
              schema:
                type: object
                properties:
                  error:
                    type: string
                  message:
                    type: string
        '409':
          description: '`X-Client-Id` name is ambiguous — send an id instead'
          content:
            application/json:
              schema:
                type: object
                properties:
                  error:
                    type: string
                  message:
                    type: string
        '429':
          description: Rate limit exceeded for this API key
          content:
            application/json:
              schema:
                type: object
                properties:
                  error:
                    type: string
                  message:
                    type: string
    delete:
      operationId: deleteMemory
      tags:
      - memories
      summary: Delete a memory
      description: 'Requires scope `memories:write`. A key with an associated user may delete that user''s
        own memories plus any shared memory on the client; a key with no user may delete SHARED memories
        only. Agency-internal memories are deletable only by agency-scoped keys.

        '
      parameters:
      - name: id
        in: path
        required: true
        schema:
          type: string
      - name: X-Client-Id
        in: header
        required: false
        description: Required for agency-wide API keys — target client id, slug, or name.
        schema:
          type: string
      responses:
        '200':
          description: Deleted
          content:
            application/json:
              schema:
                type: object
                required:
                - success
                properties:
                  success:
                    type: boolean
                    enum:
                    - true
        '400':
          description: Agency-wide key did not send the `X-Client-Id` header
          content:
            application/json:
              schema:
                type: object
                properties:
                  error:
                    type: string
                  message:
                    type: string
        '401':
          description: Missing, malformed, revoked, or inactive API key
          content:
            application/json:
              schema:
                type: object
                properties:
                  error:
                    type: string
                  message:
                    type: string
        '403':
          description: Permission denied — key lacks memories:write
          content:
            application/json:
              schema:
                type: object
                properties:
                  error:
                    type: string
                  message:
                    type: string
        '404':
          description: '`{"error": "Memory not found"}` when the id does not exist or is not deletable
            by this key; also returned when `X-Client-Id` matches no client in the agency.

            '
          content:
            application/json:
              schema:
                type: object
                properties:
                  error:
                    type: string
                  message:
                    type: string
        '409':
          description: '`X-Client-Id` name is ambiguous — send an id instead'
          content:
            application/json:
              schema:
                type: object
                properties:
                  error:
                    type: string
                  message:
                    type: string
        '429':
          description: Rate limit exceeded for this API key
          content:
            application/json:
              schema:
                type: object
                properties:
                  error:
                    type: string
                  message:
                    type: string
  /v1/metrics:
    get:
      operationId: listMetrics
      tags:
      - metrics
      summary: List this client's metrics
      description: 'Lists every metric belonging to the API key''s resolved client, each rendered through
        the shared `buildMetricView` read model (current source-grain period summary, plus a derived fiscal-year
        grid or flat period list when `year` is given). Required scope: `metrics:read` OR `metrics:write`
        (requireAnyScope). Agency-wide keys must also send `X-Client-Id`.'
      parameters:
      - name: year
        in: query
        required: false
        description: Fiscal year. When a valid integer, each metric gains `grid` (monthly, quarterly,
          annual grains) or `periodList` (weekly, daily grains). Non-integer values are ignored rather
          than rejected.
        schema:
          type: integer
      - name: X-Client-Id
        in: header
        required: false
        description: Target client id, slug, or name. Required for agency-wide API keys (400 when missing);
          ignored for client-scoped keys.
        schema:
          type: string
      responses:
        '200':
          description: The client's metrics.
          content:
            application/json:
              schema:
                type: object
                required:
                - metrics
                properties:
                  metrics:
                    type: array
                    items:
                      type: object
                      required:
                      - id
                      - name
                      - definition
                      - calculation
                      - governed
                      - format
                      - unit
                      - sourceGrain
                      - direction
                      - displayOrder
                      - periods
                      - currentLabel
                      - current
                      - reportIds
                      properties:
                        id:
                          type: string
                        name:
                          type: string
                        definition:
                          type: string
                          nullable: true
                        calculation:
                          type: string
                          nullable: true
                        governed:
                          type: boolean
                          description: True when the metric has governed SQL. The SQL itself is never
                            returned.
                        format:
                          type: string
                        unit:
                          type: string
                          nullable: true
                        sourceGrain:
                          type: string
                          enum:
                          - daily
                          - weekly
                          - monthly
                          - quarterly
                          - annual
                        direction:
                          type: string
                          enum:
                          - higher_is_better
                          - lower_is_better
                        displayOrder:
                          type: integer
                        periods:
                          type: array
                          items:
                            type: object
                            required:
                            - periodLabel
                            - periodStart
                            - periodEnd
                            - target
                            - actual
                            properties:
                              periodLabel:
                                type: string
                              periodStart:
                                type: string
                                nullable: true
                              periodEnd:
                                type: string
                                nullable: true
                              target:
                                type: number
                                nullable: true
                              actual:
                                type: number
                                nullable: true
                        currentLabel:
                          type: string
                          description: Label of the metric's current source-grain period; always set,
                            even with no stored row.
                        current:
                          type: object
                          nullable: true
                          required:
                          - periodLabel
                          - target
                          - actual
                          - percent
                          - status
                          properties:
                            periodLabel:
                              type: string
                            target:
                              type: number
                              nullable: true
                            actual:
                              type: number
                              nullable: true
                            percent:
                              type: number
                              nullable: true
                            status:
                              type: string
                              nullable: true
                              enum:
                              - good
                              - warn
                              - bad
                        reportIds:
                          type: array
                          items:
                            type: string
                        grid:
                          type: array
                          description: Present only when `year` is supplied and the grain is monthly,
                            quarterly, or annual.
                          items:
                            type: object
                            required:
                            - label
                            - grain
                            - target
                            - actual
                            - mode
                            - percent
                            - status
                            properties:
                              label:
                                type: string
                              grain:
                                type: string
                                enum:
                                - daily
                                - weekly
                                - monthly
                                - quarterly
                                - annual
                              target:
                                type: number
                                nullable: true
                              actual:
                                type: number
                                nullable: true
                              mode:
                                type: string
                                enum:
                                - source
                                - rollup
                                - amortize
                                - empty
                              percent:
                                type: number
                                nullable: true
                              status:
                                type: string
                                nullable: true
                                enum:
                                - good
                                - warn
                                - bad
                        periodList:
                          type: array
                          description: Present only when `year` is supplied and the grain is weekly or
                            daily.
                          items:
                            type: object
                            required:
                            - label
                            - target
                            - actual
                            - percent
                            - status
                            properties:
                              label:
                                type: string
                              target:
                                type: number
                                nullable: true
                              actual:
                                type: number
                                nullable: true
                              percent:
                                type: number
                                nullable: true
                              status:
                                type: string
                                nullable: true
                                enum:
                                - good
                                - warn
                                - bad
        '400':
          description: Agency-wide key without an `X-Client-Id` header.
          content:
            application/json:
              schema:
                type: object
                properties:
                  error:
                    type: string
                  message:
                    type: string
        '401':
          description: Missing or invalid API key.
          content:
            application/json:
              schema:
                type: object
                properties:
                  error:
                    type: string
                  message:
                    type: string
        '403':
          description: Key lacks both `metrics:read` and `metrics:write`.
          content:
            application/json:
              schema:
                type: object
                properties:
                  error:
                    type: string
                    example: Permission denied
                  message:
                    type: string
        '404':
          description: '`X-Client-Id` matched no client in this agency.'
          content:
            application/json:
              schema:
                type: object
                properties:
                  error:
                    type: string
                  message:
                    type: string
        '409':
          description: '`X-Client-Id` name matched more than one client.'
          content:
            application/json:
              schema:
                type: object
                properties:
                  error:
                    type: string
                  message:
                    type: string
        '429':
          description: Rate limit exceeded.
          content:
            application/json:
              schema:
                type: object
                properties:
                  error:
                    type: string
                  message:
                    type: string
                  retryAfter:
                    type: integer
    post:
      operationId: createMetric
      tags:
      - metrics
      summary: Create a metric
      description: 'Creates a metric owned by the API key''s resolved client (clientId and agencyId are
        taken from the key, never the request body). `reportIds`, when supplied, is linked after insert:
        a bad report id returns 400 while the metric stays created. Required scope: `metrics:write`.'
      parameters:
      - name: X-Client-Id
        in: header
        required: false
        description: Target client id, slug, or name. Required for agency-wide API keys.
        schema:
          type: string
      requestBody:
        required: true
        content:
          application/json:
            schema:
              type: object
              additionalProperties: false
              required:
              - name
              properties:
                name:
                  type: string
                  minLength: 1
                definition:
                  type: string
                  nullable: true
                calculation:
                  type: string
                  nullable: true
                query:
                  type: string
                  nullable: true
                  description: Governed BigQuery SQL used by `get_metric_value` / GET /v1/metrics/{id}/value.
                format:
                  type: string
                  enum:
                  - number
                  - currency
                  - percentage
                unit:
                  type: string
                  nullable: true
                sourceGrain:
                  type: string
                  enum:
                  - daily
                  - weekly
                  - monthly
                  - quarterly
                  - annual
                direction:
                  type: string
                  enum:
                  - higher_is_better
                  - lower_is_better
                displayOrder:
                  type: integer
                reportIds:
                  type: array
                  items:
                    type: string
      responses:
        '201':
          description: Metric created (returned as the metric read model, with no periods yet).
          content:
            application/json:
              schema:
                type: object
                required:
                - metric
                properties:
                  metric:
                    type: object
                    required:
                    - id
                    - name
                    - definition
                    - calculation
                    - governed
                    - format
                    - unit
                    - sourceGrain
                    - direction
                    - displayOrder
                    - periods
                    - currentLabel
                    - current
                    - reportIds
                    properties:
                      id:
                        type: string
                      name:
                        type: string
                      definition:
                        type: string
                        nullable: true
                      calculation:
                        type: string
                        nullable: true
                      governed:
                        type: boolean
                      format:
                        type: string
                      unit:
                        type: string
                        nullable: true
                      sourceGrain:
                        type: string
                        enum:
                        - daily
                        - weekly
                        - monthly
                        - quarterly
                        - annual
                      direction:
                        type: string
                        enum:
                        - higher_is_better
                        - lower_is_better
                      displayOrder:
                        type: integer
                      periods:
                        type: array
                        items:
                          type: object
                          properties:
                            periodLabel:
                              type: string
                            periodStart:
                              type: string
                              nullable: true
                            periodEnd:
                              type: string
                              nullable: true
                            target:
                              type: number
                              nullable: true
                            actual:
                              type: number
                              nullable: true
                      currentLabel:
                        type: string
                      current:
                        type: object
                        nullable: true
                        properties:
                          periodLabel:
                            type: string
                          target:
                            type: number
                            nullable: true
                          actual:
                            type: number
                            nullable: true
                          percent:
                            type: number
                            nullable: true
                          status:
                            type: string
                            nullable: true
                            enum:
                            - good
                            - warn
                            - bad
                      reportIds:
                        type: array
                        items:
                          type: string
        '400':
          description: Body failed validation (unknown keys are rejected, not stripped), or the metric
            was created but `reportIds` contained ids that are not this client's reports — in which case
            `metricId` names the created metric.
          content:
            application/json:
              schema:
                type: object
                properties:
                  error:
                    type: string
                  metricId:
                    type: string
                    description: Present when the metric was created but linking failed.
        '401':
          description: Missing or invalid API key.
          content:
            application/json:
              schema:
                type: object
                properties:
                  error:
                    type: string
                  message:
                    type: string
        '403':
          description: Key lacks `metrics:write`.
          content:
            application/json:
              schema:
                type: object
                properties:
                  error:
                    type: string
                  message:
                    type: string
        '429':
          description: Rate limit exceeded.
          content:
            application/json:
              schema:
                type: object
                properties:
                  error:
                    type: string
                  message:
                    type: string
                  retryAfter:
                    type: integer
        '500':
          description: Create failed.
          content:
            application/json:
              schema:
                type: object
                properties:
                  error:
                    type: string
                    example: Failed to create metric
  /v1/metrics/{id}:
    parameters:
    - name: id
      in: path
      required: true
      description: Metric id.
      schema:
        type: string
    get:
      operationId: getMetric
      tags:
      - metrics
      summary: Get one metric with its periods
      description: 'Returns a single metric of the API key''s resolved client through `buildMetricView`,
        including all stored periods and optionally the fiscal-year grid. Required scope: `metrics:read`
        OR `metrics:write` (requireAnyScope).'
      parameters:
      - name: year
        in: query
        required: false
        description: Fiscal year; adds `grid` (monthly/quarterly/annual) or `periodList` (weekly/daily).
        schema:
          type: integer
      - name: X-Client-Id
        in: header
        required: false
        description: Target client id, slug, or name. Required for agency-wide API keys.
        schema:
          type: string
      responses:
        '200':
          description: The metric.
          content:
            application/json:
              schema:
                type: object
                required:
                - metric
                properties:
                  metric:
                    type: object
                    required:
                    - id
                    - name
                    - definition
                    - calculation
                    - governed
                    - format
                    - unit
                    - sourceGrain
                    - direction
                    - displayOrder
                    - periods
                    - currentLabel
                    - current
                    - reportIds
                    properties:
                      id:
                        type: string
                      name:
                        type: string
                      definition:
                        type: string
                        nullable: true
                      calculation:
                        type: string
                        nullable: true
                      governed:
                        type: boolean
                      format:
                        type: string
                      unit:
                        type: string
                        nullable: true
                      sourceGrain:
                        type: string
                        enum:
                        - daily
                        - weekly
                        - monthly
                        - quarterly
                        - annual
                      direction:
                        type: string
                        enum:
                        - higher_is_better
                        - lower_is_better
                      displayOrder:
                        type: integer
                      periods:
                        type: array
                        items:
                          type: object
                          properties:
                            periodLabel:
                              type: string
                            periodStart:
                              type: string
                              nullable: true
                            periodEnd:
                              type: string
                              nullable: true
                            target:
                              type: number
                              nullable: true
                            actual:
                              type: number
                              nullable: true
                      currentLabel:
                        type: string
                      current:
                        type: object
                        nullable: true
                        properties:
                          periodLabel:
                            type: string
                          target:
                            type: number
                            nullable: true
                          actual:
                            type: number
                            nullable: true
                          percent:
                            type: number
                            nullable: true
                          status:
                            type: string
                            nullable: true
                            enum:
                            - good
                            - warn
                            - bad
                      reportIds:
                        type: array
                        items:
                          type: string
                      grid:
                        type: array
                        description: Present only with `year` at monthly, quarterly, or annual grain.
                        items:
                          type: object
                          properties:
                            label:
                              type: string
                            grain:
                              type: string
                              enum:
                              - daily
                              - weekly
                              - monthly
                              - quarterly
                              - annual
                            target:
                              type: number
                              nullable: true
                            actual:
                              type: number
                              nullable: true
                            mode:
                              type: string
                              enum:
                              - source
                              - rollup
                              - amortize
                              - empty
                            percent:
                              type: number
                              nullable: true
                            status:
                              type: string
                              nullable: true
                              enum:
                              - good
                              - warn
                              - bad
                      periodList:
                        type: array
                        description: Present only with `year` at weekly or daily grain.
                        items:
                          type: object
                          properties:
                            label:
                              type: string
                            target:
                              type: number
                              nullable: true
                            actual:
                              type: number
                              nullable: true
                            percent:
                              type: number
                              nullable: true
                            status:
                              type: string
                              nullable: true
                              enum:
                              - good
                              - warn
                              - bad
        '401':
          description: Missing or invalid API key.
          content:
            application/json:
              schema:
                type: object
                properties:
                  error:
                    type: string
                  message:
                    type: string
        '403':
          description: Key lacks both `metrics:read` and `metrics:write`.
          content:
            application/json:
              schema:
                type: object
                properties:
                  error:
                    type: string
                  message:
                    type: string
        '404':
          description: No such metric for this client.
          content:
            application/json:
              schema:
                type: object
                properties:
                  error:
                    type: string
                    example: Metric not found
        '429':
          description: Rate limit exceeded.
          content:
            application/json:
              schema:
                type: object
                properties:
                  error:
                    type: string
                  message:
                    type: string
                  retryAfter:
                    type: integer
    put:
      operationId: updateMetric
      tags:
      - metrics
      summary: Update a metric
      description: 'Partially updates a metric of the API key''s resolved client. Unknown body keys are
        rejected rather than silently dropped. When `reportIds` is present the report links are replaced.
        The returned read model is built with no periods, so `periods` is empty and `current` is null
        even if periods exist. Required scope: `metrics:write`.'
      parameters:
      - name: X-Client-Id
        in: header
        required: false
        description: Target client id, slug, or name. Required for agency-wide API keys.
        schema:
          type: string
      requestBody:
        required: true
        content:
          application/json:
            schema:
              type: object
              additionalProperties: false
              description: All fields optional; unknown fields are rejected with 400.
              properties:
                name:
                  type: string
                  minLength: 1
                definition:
                  type: string
                  nullable: true
                calculation:
                  type: string
                  nullable: true
                query:
                  type: string
                  nullable: true
                format:
                  type: string
                  enum:
                  - number
                  - currency
                  - percentage
                unit:
                  type: string
                  nullable: true
                sourceGrain:
                  type: string
                  enum:
                  - daily
                  - weekly
                  - monthly
                  - quarterly
                  - annual
                direction:
                  type: string
                  enum:
                  - higher_is_better
                  - lower_is_better
                displayOrder:
                  type: integer
                reportIds:
                  type: array
                  items:
                    type: string
      responses:
        '200':
          description: Metric updated.
          content:
            application/json:
              schema:
                type: object
                required:
                - metric
                properties:
                  metric:
                    type: object
                    required:
                    - id
                    - name
                    - definition
                    - calculation
                    - governed
                    - format
                    - unit
                    - sourceGrain
                    - direction
                    - displayOrder
                    - periods
                    - currentLabel
                    - current
                    - reportIds
                    properties:
                      id:
                        type: string
                      name:
                        type: string
                      definition:
                        type: string
                        nullable: true
                      calculation:
                        type: string
                        nullable: true
                      governed:
                        type: boolean
                      format:
                        type: string
                      unit:
                        type: string
                        nullable: true
                      sourceGrain:
                        type: string
                        enum:
                        - daily
                        - weekly
                        - monthly
                        - quarterly
                        - annual
                      direction:
                        type: string
                        enum:
                        - higher_is_better
                        - lower_is_better
                      displayOrder:
                        type: integer
                      periods:
                        type: array
                        description: Always empty on this response.
                        items:
                          type: object
                      currentLabel:
                        type: string
                      current:
                        type: object
                        nullable: true
                        description: Always null on this response (built without periods).
                      reportIds:
                        type: array
                        items:
                          type: string
        '400':
          description: Body failed validation, or `reportIds` referenced reports that are not this client's.
          content:
            application/json:
              schema:
                type: object
                properties:
                  error:
                    type: string
        '401':
          description: Missing or invalid API key.
          content:
            application/json:
              schema:
                type: object
                properties:
                  error:
                    type: string
                  message:
                    type: string
        '403':
          description: Key lacks `metrics:write`.
          content:
            application/json:
              schema:
                type: object
                properties:
                  error:
                    type: string
                  message:
                    type: string
        '404':
          description: No such metric for this client.
          content:
            application/json:
              schema:
                type: object
                properties:
                  error:
                    type: string
                    example: Metric not found
        '429':
          description: Rate limit exceeded.
          content:
            application/json:
              schema:
                type: object
                properties:
                  error:
                    type: string
                  message:
                    type: string
                  retryAfter:
                    type: integer
        '500':
          description: Update failed.
          content:
            application/json:
              schema:
                type: object
                properties:
                  error:
                    type: string
                    example: Failed to update metric
    delete:
      operationId: deleteMetric
      tags:
      - metrics
      summary: Delete a metric
      description: 'Deletes a metric of the API key''s resolved client. Required scope: `metrics:write`.'
      parameters:
      - name: X-Client-Id
        in: header
        required: false
        description: Target client id, slug, or name. Required for agency-wide API keys.
        schema:
          type: string
      responses:
        '200':
          description: Metric deleted.
          content:
            application/json:
              schema:
                type: object
                required:
                - success
                properties:
                  success:
                    type: boolean
                    enum:
                    - true
        '401':
          description: Missing or invalid API key.
          content:
            application/json:
              schema:
                type: object
                properties:
                  error:
                    type: string
                  message:
                    type: string
        '403':
          description: Key lacks `metrics:write`.
          content:
            application/json:
              schema:
                type: object
                properties:
                  error:
                    type: string
                  message:
                    type: string
        '404':
          description: No such metric for this client.
          content:
            application/json:
              schema:
                type: object
                properties:
                  error:
                    type: string
                    example: Metric not found
        '429':
          description: Rate limit exceeded.
          content:
            application/json:
              schema:
                type: object
                properties:
                  error:
                    type: string
                  message:
                    type: string
                  retryAfter:
                    type: integer
  /v1/metrics/{id}/periods/{label}:
    parameters:
    - name: id
      in: path
      required: true
      description: Metric id.
      schema:
        type: string
    - name: label
      in: path
      required: true
      description: Period label; must parse and must match the metric's source grain.
      schema:
        type: string
    put:
      operationId: setMetricPeriod
      tags:
      - metrics
      summary: Upsert a source-grain period's target and actual
      description: 'Upserts one period row (keyed by metric id + period label) with its target and actual.
        The label must parse and must be at the metric''s own source grain. Required scope: `metrics:write`.'
      parameters:
      - name: X-Client-Id
        in: header
        required: false
        description: Target client id, slug, or name. Required for agency-wide API keys.
        schema:
          type: string
      requestBody:
        required: true
        content:
          application/json:
            schema:
              type: object
              properties:
                targetValue:
                  type: number
                  nullable: true
                actualValue:
                  type: number
                  nullable: true
      responses:
        '200':
          description: The upserted period row as stored.
          content:
            application/json:
              schema:
                type: object
                required:
                - period
                properties:
                  period:
                    type: object
                    description: The raw metric_periods row returned by the insert/upsert. Numeric columns
                      come back as strings from the driver.
                    properties:
                      id:
                        type: string
                      metricId:
                        type: string
                      periodLabel:
                        type: string
                      periodStart:
                        type: string
                        format: date
                      periodEnd:
                        type: string
                        format: date
                      targetValue:
                        type: string
                        nullable: true
                      actualValue:
                        type: string
                        nullable: true
                      createdAt:
                        type: string
                        format: date-time
                      updatedAt:
                        type: string
                        format: date-time
        '400':
          description: Body failed validation, the label does not parse ("Invalid period label"), or the
            label's grain does not match the metric's source grain.
          content:
            application/json:
              schema:
                type: object
                properties:
                  error:
                    type: string
        '401':
          description: Missing or invalid API key.
          content:
            application/json:
              schema:
                type: object
                properties:
                  error:
                    type: string
                  message:
                    type: string
        '403':
          description: Key lacks `metrics:write`.
          content:
            application/json:
              schema:
                type: object
                properties:
                  error:
                    type: string
                  message:
                    type: string
        '404':
          description: No such metric for this client.
          content:
            application/json:
              schema:
                type: object
                properties:
                  error:
                    type: string
                    example: Metric not found
        '429':
          description: Rate limit exceeded.
          content:
            application/json:
              schema:
                type: object
                properties:
                  error:
                    type: string
                  message:
                    type: string
                  retryAfter:
                    type: integer
    delete:
      operationId: deleteMetricPeriod
      tags:
      - metrics
      summary: Remove a period
      description: 'Deletes the period row for this metric and label. Required scope: `metrics:write`.'
      parameters:
      - name: X-Client-Id
        in: header
        required: false
        description: Target client id, slug, or name. Required for agency-wide API keys.
        schema:
          type: string
      responses:
        '200':
          description: Deleted. Returns success whenever the metric belongs to this client, even if no
            period row existed for that label.
          content:
            application/json:
              schema:
                type: object
                required:
                - success
                properties:
                  success:
                    type: boolean
                    enum:
                    - true
        '401':
          description: Missing or invalid API key.
          content:
            application/json:
              schema:
                type: object
                properties:
                  error:
                    type: string
                  message:
                    type: string
        '403':
          description: Key lacks `metrics:write`.
          content:
            application/json:
              schema:
                type: object
                properties:
                  error:
                    type: string
                  message:
                    type: string
        '404':
          description: No such metric for this client.
          content:
            application/json:
              schema:
                type: object
                properties:
                  error:
                    type: string
                    example: Metric not found
        '429':
          description: Rate limit exceeded.
          content:
            application/json:
              schema:
                type: object
                properties:
                  error:
                    type: string
                  message:
                    type: string
                  retryAfter:
                    type: integer
  /v1/metrics/{id}/value:
    parameters:
    - name: id
      in: path
      required: true
      description: Metric id.
      schema:
        type: string
    get:
      operationId: getMetricValue
      tags:
      - metrics
      summary: Resolve a metric's value for a period
      description: 'Resolves the metric''s value by running its OWN governed query through the same executor
        MCP and the chat surfaces use, so every caller gets the same number. Falls back to the hand-entered
        actual when the metric has no governed SQL (`source: stored_actual`). If the query groups without
        a grand-total row the response carries the slices and `source: query_breakdown_only` with a null
        `value` — do not sum the slices. Required scope: `metrics:read` OR `metrics:write` (requireAnyScope).'
      parameters:
      - name: period
        in: query
        required: false
        description: Period label at the metric's own source grain (e.g. `2026-M03`). Defaults to the
          current period at that grain. A label at a different grain, or an unparseable label, is a 400.
        schema:
          type: string
      - name: X-Client-Id
        in: header
        required: false
        description: Target client id, slug, or name. Required for agency-wide API keys.
        schema:
          type: string
      responses:
        '200':
          description: The resolved value plus the citation fields for the metric and period.
          content:
            application/json:
              schema:
                type: object
                required:
                - metric_id
                - metric
                - definition
                - calculation
                - format
                - unit
                - period
                - period_start
                - period_end
                - value
                - target
                - source
                properties:
                  metric_id:
                    type: string
                  metric:
                    type: string
                    description: Metric name.
                  definition:
                    type: string
                    nullable: true
                  calculation:
                    type: string
                    nullable: true
                  format:
                    type: string
                  unit:
                    type: string
                    nullable: true
                  period:
                    type: string
                  period_start:
                    type: string
                    format: date
                  period_end:
                    type: string
                    format: date
                  value:
                    type: number
                    nullable: true
                    description: Null for `stored_actual` with no entered actual, and for `query_breakdown_only`.
                  value_column:
                    type: string
                    description: 'Name of the column the value came from. Only on `source: query`.'
                  target:
                    type: number
                    nullable: true
                  percent:
                    type: number
                    nullable: true
                    description: Absent on `query_breakdown_only`.
                  status:
                    type: string
                    nullable: true
                    enum:
                    - good
                    - warn
                    - bad
                    description: Absent on `query_breakdown_only`.
                  source:
                    type: string
                    enum:
                    - query
                    - stored_actual
                    - query_breakdown_only
                  breakdown_by:
                    type: string
                    description: Dimension column name, when the query is grouped.
                  breakdown:
                    type: array
                    description: Per-slice values, biggest first, when the query is grouped.
                    items:
                      type: object
                      required:
                      - label
                      - value
                      properties:
                        label:
                          type: string
                        value:
                          type: number
                          nullable: true
                  truncated:
                    type: boolean
                    description: True when the row cap (200) was hit, so the breakdown (and possibly the
                      total) is incomplete.
                  note:
                    type: string
                    description: Guidance for the caller; present for stored_actual, breakdown-only, and
                      truncated results.
                  sql:
                    type: string
                    description: 'The governed SQL actually run, with the period bound. Only on `source:
                      query`.'
        '400':
          description: The metric tool returned an error - metric not found for this client, a `period`
            at the wrong grain or unparseable, the governed query not allowed for this client, the query
            returning no numeric value, or the query failing.
          content:
            application/json:
              schema:
                type: object
                required:
                - error
                properties:
                  error:
                    type: string
                    description: 'Tool error text with the leading "Error: " stripped.'
        '401':
          description: Missing or invalid API key.
          content:
            application/json:
              schema:
                type: object
                properties:
                  error:
                    type: string
                  message:
                    type: string
        '403':
          description: Key lacks both `metrics:read` and `metrics:write`.
          content:
            application/json:
              schema:
                type: object
                properties:
                  error:
                    type: string
                  message:
                    type: string
        '429':
          description: Rate limit exceeded.
          content:
            application/json:
              schema:
                type: object
                properties:
                  error:
                    type: string
                  message:
                    type: string
                  retryAfter:
                    type: integer
  /v1/models:
    get:
      operationId: listModels
      tags:
      - models
      summary: List available AI models
      description: 'Lists the AI models available for `ai_analysis` alert `aiModel` overrides, sourced
        from the shared model registry (retired entries excluded). Scope: none required — any valid API
        key may read it (reference data, no client scope needed). Auth is the route-group middleware (`apiKeyAuth`:
        `Authorization: Bearer secret_xxx`) plus per-key `rateLimit`; there is no `requireScope` / `requireAnyScope`
        on this route.'
      responses:
        '200':
          description: The offered models, flat and grouped by provider.
          headers:
            X-RateLimit-Limit:
              description: Requests allowed per minute for this API key.
              schema:
                type: string
            X-RateLimit-Remaining:
              description: Requests remaining in the current window.
              schema:
                type: string
            X-RateLimit-Reset:
              description: Unix seconds when the current window resets.
              schema:
                type: string
          content:
            application/json:
              schema:
                type: object
                required:
                - models
                - grouped
                properties:
                  models:
                    type: array
                    items:
                      type: object
                      required:
                      - id
                      - name
                      - provider
                      - contextWindow
                      - costTier
                      properties:
                        id:
                          type: string
                          description: Friendly model id, used as an alert's `aiModel` value.
                        name:
                          type: string
                          description: Display name.
                        provider:
                          type: string
                          enum:
                          - anthropic
                          - google
                          - openrouter
                          - cloudflare
                          - other
                        contextWindow:
                          type: integer
                        costTier:
                          type: string
                          enum:
                          - $
                          - $$
                          - $$$
                          - $$$$
                  grouped:
                    type: object
                    description: 'The same models bucketed by provider. Keys are always present: `anthropic`,
                      `google`, `openrouter`, `cloudflare`, and `other` (`other` is always an empty array).'
                    additionalProperties:
                      type: array
                      items:
                        type: object
                        required:
                        - id
                        - name
                        - provider
                        - contextWindow
                        - costTier
                        properties:
                          id:
                            type: string
                          name:
                            type: string
                          provider:
                            type: string
                            enum:
                            - anthropic
                            - google
                            - openrouter
                            - cloudflare
                            - other
                          contextWindow:
                            type: integer
                          costTier:
                            type: string
                            enum:
                            - $
                            - $$
                            - $$$
                            - $$$$
        '401':
          description: Missing or invalid API key (from `apiKeyAuth` middleware).
          content:
            application/json:
              schema:
                type: object
                properties:
                  error:
                    type: string
                    example: Missing or invalid credentials
                  message:
                    type: string
        '429':
          description: Per-API-key rate limit exceeded (from `rateLimit` middleware).
          content:
            application/json:
              schema:
                type: object
                properties:
                  error:
                    type: string
                    example: Rate limit exceeded
                  message:
                    type: string
                  retryAfter:
                    type: integer
                    description: Seconds until the rate-limit window resets.
        '502':
          description: The model registry could not be read (handler catch branch).
          content:
            application/json:
              schema:
                type: object
                properties:
                  error:
                    type: string
                    description: The thrown error's message.
  /v1/prompts:
    get:
      operationId: listPrompts
      summary: List the authenticated client's prompt-library entries
      description: Returns the `agent_prompts` rows owned by the client resolved from the API key, ordered
        by name ascending. Requires scope `prompts:read` OR `prompts:write` (requireAnyScope).
      tags:
      - prompts
      parameters:
      - name: X-Client-Id
        in: header
        required: false
        description: 'Required for agency-wide API keys: the target client''s id, slug, or name. Ignored
          for client-scoped keys.'
        schema:
          type: string
      responses:
        '200':
          description: The client's prompt-library entries
          content:
            application/json:
              schema:
                type: object
                required:
                - prompts
                properties:
                  prompts:
                    type: array
                    items:
                      type: object
                      required:
                      - id
                      - name
                      - description
                      - content
                      - type
                      - isDefault
                      - updatedAt
                      properties:
                        id:
                          type: string
                        name:
                          type: string
                        description:
                          type: string
                          nullable: true
                        content:
                          type: string
                        type:
                          type: string
                          enum:
                          - system
                          - user
                          - context
                        isDefault:
                          type: boolean
                        updatedAt:
                          type: string
                          format: date-time
        '400':
          description: Agency-wide key sent without an `X-Client-Id` header
          content:
            application/json:
              schema:
                type: object
                properties:
                  error:
                    type: string
                  message:
                    type: string
        '401':
          description: Missing, invalid, or expired API key
          content:
            application/json:
              schema:
                type: object
                properties:
                  error:
                    type: string
                  message:
                    type: string
        '403':
          description: Key lacks `prompts:read`/`prompts:write`, or the associated user is deactivated
          content:
            application/json:
              schema:
                type: object
                properties:
                  error:
                    type: string
                  message:
                    type: string
        '404':
          description: No client in this agency matches `X-Client-Id`
          content:
            application/json:
              schema:
                type: object
                properties:
                  error:
                    type: string
                  message:
                    type: string
        '409':
          description: Ambiguous `X-Client-Id` — several clients share that name
          content:
            application/json:
              schema:
                type: object
                properties:
                  error:
                    type: string
                  message:
                    type: string
        '429':
          description: Rate limit exceeded
          content:
            application/json:
              schema:
                type: object
                properties:
                  error:
                    type: string
                  message:
                    type: string
                  retryAfter:
                    type: integer
                    description: Seconds until the window resets
    post:
      operationId: createPrompt
      summary: Create a prompt-library entry for the authenticated client
      description: Creates a client-owned prompt (`agencyId` is always null). `type` defaults to `system`,
        `isDefault` to `false`. Requires scope `prompts:write` (requireScope).
      tags:
      - prompts
      parameters:
      - name: X-Client-Id
        in: header
        required: false
        description: Required for agency-wide API keys — target client id, slug, or name.
        schema:
          type: string
      requestBody:
        required: true
        content:
          application/json:
            schema:
              type: object
              required:
              - name
              - content
              properties:
                name:
                  type: string
                  minLength: 1
                content:
                  type: string
                  minLength: 1
                description:
                  type: string
                  nullable: true
                type:
                  type: string
                  enum:
                  - system
                  - user
                  - context
                  default: system
                isDefault:
                  type: boolean
                  default: false
      responses:
        '201':
          description: Prompt created
          content:
            application/json:
              schema:
                type: object
                required:
                - prompt
                properties:
                  prompt:
                    type: object
                    required:
                    - id
                    - name
                    - description
                    - content
                    - type
                    - isDefault
                    - updatedAt
                    properties:
                      id:
                        type: string
                      name:
                        type: string
                      description:
                        type: string
                        nullable: true
                      content:
                        type: string
                      type:
                        type: string
                        enum:
                        - system
                        - user
                        - context
                      isDefault:
                        type: boolean
                      updatedAt:
                        type: string
                        format: date-time
        '400':
          description: 'Zod body validation failure, a core validation error returned as `{ "error": "<message>"
            }` (blank name/content, invalid type, non-string description), or an agency key sent without
            `X-Client-Id`.'
          content:
            application/json:
              schema:
                type: object
                properties:
                  error:
                    type: string
                  message:
                    type: string
        '401':
          description: Missing, invalid, or expired API key
          content:
            application/json:
              schema:
                type: object
                properties:
                  error:
                    type: string
                  message:
                    type: string
        '403':
          description: Key lacks `prompts:write`, or the associated user is deactivated
          content:
            application/json:
              schema:
                type: object
                properties:
                  error:
                    type: string
                  message:
                    type: string
        '404':
          description: No client in this agency matches `X-Client-Id`
          content:
            application/json:
              schema:
                type: object
                properties:
                  error:
                    type: string
                  message:
                    type: string
        '409':
          description: Ambiguous `X-Client-Id` — several clients share that name
          content:
            application/json:
              schema:
                type: object
                properties:
                  error:
                    type: string
                  message:
                    type: string
        '429':
          description: Rate limit exceeded
          content:
            application/json:
              schema:
                type: object
                properties:
                  error:
                    type: string
                  message:
                    type: string
                  retryAfter:
                    type: integer
  /v1/prompts/{id}:
    parameters:
    - name: id
      in: path
      required: true
      description: Prompt id (uuid). All lookups are scoped to the authenticated client.
      schema:
        type: string
    - name: X-Client-Id
      in: header
      required: false
      description: Required for agency-wide API keys — target client id, slug, or name.
      schema:
        type: string
    get:
      operationId: getPrompt
      summary: Get one prompt-library entry including its full content
      description: Requires scope `prompts:read` OR `prompts:write` (requireAnyScope).
      tags:
      - prompts
      responses:
        '200':
          description: The prompt
          content:
            application/json:
              schema:
                type: object
                required:
                - prompt
                properties:
                  prompt:
                    type: object
                    required:
                    - id
                    - name
                    - description
                    - content
                    - type
                    - isDefault
                    - updatedAt
                    properties:
                      id:
                        type: string
                      name:
                        type: string
                      description:
                        type: string
                        nullable: true
                      content:
                        type: string
                      type:
                        type: string
                        enum:
                        - system
                        - user
                        - context
                      isDefault:
                        type: boolean
                      updatedAt:
                        type: string
                        format: date-time
        '400':
          description: Agency-wide key sent without an `X-Client-Id` header
          content:
            application/json:
              schema:
                type: object
                properties:
                  error:
                    type: string
                  message:
                    type: string
        '401':
          description: Missing, invalid, or expired API key
          content:
            application/json:
              schema:
                type: object
                properties:
                  error:
                    type: string
                  message:
                    type: string
        '403':
          description: Key lacks `prompts:read`/`prompts:write`, or the associated user is deactivated
          content:
            application/json:
              schema:
                type: object
                properties:
                  error:
                    type: string
                  message:
                    type: string
        '404':
          description: '`{ "error": "Prompt not found" }` when no prompt with that id belongs to the client,
            or the client-resolution 404 for an unmatched `X-Client-Id`.'
          content:
            application/json:
              schema:
                type: object
                properties:
                  error:
                    type: string
                  message:
                    type: string
        '409':
          description: Ambiguous `X-Client-Id` — several clients share that name
          content:
            application/json:
              schema:
                type: object
                properties:
                  error:
                    type: string
                  message:
                    type: string
        '429':
          description: Rate limit exceeded
          content:
            application/json:
              schema:
                type: object
                properties:
                  error:
                    type: string
                  message:
                    type: string
                  retryAfter:
                    type: integer
    patch:
      operationId: updatePrompt
      summary: Sparse-patch a prompt-library entry
      description: Updates any subset of name / content / description / type / isDefault, bumps `updatedAt`,
        and snapshots a new version. Requires scope `prompts:write` (requireScope).
      tags:
      - prompts
      requestBody:
        required: true
        content:
          application/json:
            schema:
              type: object
              properties:
                name:
                  type: string
                  minLength: 1
                content:
                  type: string
                  minLength: 1
                description:
                  type: string
                  nullable: true
                type:
                  type: string
                  enum:
                  - system
                  - user
                  - context
                isDefault:
                  type: boolean
      responses:
        '200':
          description: The updated prompt
          content:
            application/json:
              schema:
                type: object
                required:
                - prompt
                properties:
                  prompt:
                    type: object
                    required:
                    - id
                    - name
                    - description
                    - content
                    - type
                    - isDefault
                    - updatedAt
                    properties:
                      id:
                        type: string
                      name:
                        type: string
                      description:
                        type: string
                        nullable: true
                      content:
                        type: string
                      type:
                        type: string
                        enum:
                        - system
                        - user
                        - context
                      isDefault:
                        type: boolean
                      updatedAt:
                        type: string
                        format: date-time
        '400':
          description: 'Zod body validation failure, a core validation error returned as `{ "error": "<message>"
            }` (blank name/content, invalid type, non-string description), or an agency key sent without
            `X-Client-Id`.'
          content:
            application/json:
              schema:
                type: object
                properties:
                  error:
                    type: string
                  message:
                    type: string
        '401':
          description: Missing, invalid, or expired API key
          content:
            application/json:
              schema:
                type: object
                properties:
                  error:
                    type: string
                  message:
                    type: string
        '403':
          description: Key lacks `prompts:write`, or the associated user is deactivated
          content:
            application/json:
              schema:
                type: object
                properties:
                  error:
                    type: string
                  message:
                    type: string
        '404':
          description: '`{ "error": "Prompt not found" }`, or the client-resolution 404 for an unmatched
            `X-Client-Id`.'
          content:
            application/json:
              schema:
                type: object
                properties:
                  error:
                    type: string
                  message:
                    type: string
        '409':
          description: Ambiguous `X-Client-Id` — several clients share that name
          content:
            application/json:
              schema:
                type: object
                properties:
                  error:
                    type: string
                  message:
                    type: string
        '429':
          description: Rate limit exceeded
          content:
            application/json:
              schema:
                type: object
                properties:
                  error:
                    type: string
                  message:
                    type: string
                  retryAfter:
                    type: integer
    delete:
      operationId: deletePrompt
      summary: Delete a prompt-library entry
      description: Refuses with 400 if any agent still references the prompt via `systemPromptId`. Requires
        scope `prompts:write` (requireScope).
      tags:
      - prompts
      responses:
        '200':
          description: Prompt deleted
          content:
            application/json:
              schema:
                type: object
                required:
                - success
                properties:
                  success:
                    type: boolean
        '400':
          description: '`{ "error": "Cannot delete prompt: it is referenced by the following agents: ..."
            }`, or an agency key sent without `X-Client-Id`.'
          content:
            application/json:
              schema:
                type: object
                properties:
                  error:
                    type: string
                  message:
                    type: string
        '401':
          description: Missing, invalid, or expired API key
          content:
            application/json:
              schema:
                type: object
                properties:
                  error:
                    type: string
                  message:
                    type: string
        '403':
          description: Key lacks `prompts:write`, or the associated user is deactivated
          content:
            application/json:
              schema:
                type: object
                properties:
                  error:
                    type: string
                  message:
                    type: string
        '404':
          description: '`{ "error": "Prompt not found" }`, or the client-resolution 404 for an unmatched
            `X-Client-Id`.'
          content:
            application/json:
              schema:
                type: object
                properties:
                  error:
                    type: string
                  message:
                    type: string
        '409':
          description: Ambiguous `X-Client-Id` — several clients share that name
          content:
            application/json:
              schema:
                type: object
                properties:
                  error:
                    type: string
                  message:
                    type: string
        '429':
          description: Rate limit exceeded
          content:
            application/json:
              schema:
                type: object
                properties:
                  error:
                    type: string
                  message:
                    type: string
                  retryAfter:
                    type: integer
  /v1/report-authoring/client-schema:
    get:
      operationId: getReportAuthoringClientSchema
      tags:
      - report-authoring
      summary: The client's authorized tables and columns
      description: 'Returns the BigQuery tables and columns the resolved client is authorized to query,
        keyed by the client''s MONOREPO slug (resolved server-side from `client_configs.config.portal_client_id`,
        never from the caller).


        Auth: API key plus a resolved client.

        Required scope: `reports:read` OR `reports:write` (requireAnyScope).

        Agency-wide keys must name the target client via `X-Client-Id`.'
      parameters:
      - name: X-Client-Id
        in: header
        required: false
        description: Target client (id, slug, or name). Required for agency-wide API keys; ignored for
          client-scoped keys.
        schema:
          type: string
      responses:
        '200':
          description: The client schema, proxied verbatim from the report server's `GET /api/authoring/client-schema/{client}`.
            The gateway types it as `unknown`, so no field-level schema is asserted here.
          content:
            application/json:
              schema:
                type: object
                additionalProperties: true
        '400':
          description: Executor error envelope.
          content:
            application/json:
              schema:
                type: object
                properties:
                  error:
                    type: string
        '401':
          description: Missing or invalid API key.
          content:
            application/json:
              schema:
                type: object
                properties:
                  error:
                    type: string
        '403':
          description: The API key lacks `reports:read` / `reports:write`.
          content:
            application/json:
              schema:
                type: object
                properties:
                  error:
                    type: string
        '404':
          description: No client matching `X-Client-Id` exists in this agency.
          content:
            application/json:
              schema:
                type: object
                properties:
                  error:
                    type: string
                  message:
                    type: string
        '429':
          description: Rate limit exceeded.
          content:
            application/json:
              schema:
                type: object
                properties:
                  error:
                    type: string
  /v1/report-authoring/components:
    get:
      operationId: listReportComponents
      tags:
      - report-authoring
      summary: Component library manifest
      description: 'Returns the trusted report component library manifest (the index every authoring conversation
        starts from). Composer-only manifest keys (templateHtml, entrySignature, entryFunction, slots,
        cssPrefix, dependencies, produces, exampleRef, exampleDataKey) are stripped before the response
        is returned; configSchema, dataShape, transformContract, whenToUse/whenNotToUse and acceptsRawHtml
        are retained.


        Auth: API key plus a resolved client.

        Required scope: `reports:read` OR `reports:write` (requireAnyScope).

        Agency-wide keys must additionally name the target client via the `X-Client-Id` header (requireResolvedClient).'
      parameters:
      - name: X-Client-Id
        in: header
        required: false
        description: Target client (id, slug, or name). Required for agency-wide API keys; ignored for
          client-scoped keys, which are pinned to their own client.
        schema:
          type: string
      responses:
        '200':
          description: The component manifest, proxied from the report server's `GET /api/authoring/components`
            and passed through untyped by the gateway. Shape is owned upstream; documented here as the
            observed envelope only.
          content:
            application/json:
              schema:
                type: object
                properties:
                  components:
                    type: array
                    description: One entry per library component (lean manifest).
                    items:
                      type: object
                      properties:
                        configSchema:
                          type: object
                          description: Schema for the component's config block.
                        dataShape:
                          type: object
                          description: Shape the component's bound data must take.
                        transformContract:
                          type: object
                          description: Inputs the agent must supply in dataBindings.
                        whenToUse:
                          type: string
                        whenNotToUse:
                          type: string
                        acceptsRawHtml:
                          type: boolean
                          description: Trust-gated; only trusted (agency) callers may use raw HTML.
                      additionalProperties: true
                additionalProperties: true
        '400':
          description: Executor error envelope.
          content:
            application/json:
              schema:
                type: object
                properties:
                  error:
                    type: string
        '401':
          description: Missing or invalid API key.
          content:
            application/json:
              schema:
                type: object
                properties:
                  error:
                    type: string
        '403':
          description: The API key lacks `reports:read` / `reports:write`.
          content:
            application/json:
              schema:
                type: object
                properties:
                  error:
                    type: string
        '404':
          description: No client matching `X-Client-Id` exists in this agency.
          content:
            application/json:
              schema:
                type: object
                properties:
                  error:
                    type: string
                  message:
                    type: string
        '429':
          description: Rate limit exceeded.
          content:
            application/json:
              schema:
                type: object
                properties:
                  error:
                    type: string
  /v1/report-authoring/save:
    post:
      operationId: saveReportSpec
      tags:
      - report-authoring
      summary: Compose and save a report from a ReportSpec
      description: 'Composes a report from a ReportSpec using the trusted server-side component library
        (the caller never writes HTML) and saves it. Without `report_id` the server composes a preview
        first and returns a live, token-gated preview URL plus lint. With `report_id` it updates the existing
        report.


        Trust: only trusted agency keys may set `publish: true`; every other caller''s save lands as a
        draft. Untrusted (client-scoped) keys must have a per-client `mcpGuardrails` policy configured
        (`requireClientGuardrails`) or the save is rejected.


        Auth: API key plus a resolved client.

        Required scope: `reports:write` (requireScope).

        Agency-wide keys must name the target client via `X-Client-Id`.'
      parameters:
      - name: X-Client-Id
        in: header
        required: false
        description: Target client (id, slug, or name). Required for agency-wide API keys; ignored for
          client-scoped keys.
        schema:
          type: string
      requestBody:
        required: true
        content:
          application/json:
            schema:
              type: object
              required:
              - spec
              properties:
                spec:
                  type: object
                  additionalProperties: true
                  description: The ReportSpec (component ids + config + dataBindings + queries). Validated
                    as a free-form object at the route; the executor rejects an empty object.
                report_id:
                  type: string
                  description: Update this existing report instead of creating one.
                title:
                  type: string
                  description: Overrides `spec.title`.
                description:
                  type: string
                  description: Overrides `spec.description`.
                publish:
                  type: boolean
                  description: Publish immediately. Honoured only for trusted agency keys; otherwise the
                    save stays a draft.
      responses:
        '200':
          description: 'Saved. Three observed shapes from `executeReportAuthoringTool`: update (`{saved,
            report}`), trusted publish (`{saved, published, report, previewUrl, lint}`), and draft (`{saved,
            status, previewUrl, draftSlug, version, title, lint}`).'
          content:
            application/json:
              schema:
                type: object
                properties:
                  saved:
                    type: boolean
                  published:
                    type: boolean
                    description: Present only on the trusted publish path.
                  status:
                    type: string
                    enum:
                    - draft
                    description: Present only on the draft path.
                  report:
                    type: object
                    additionalProperties: true
                    description: The persisted report as returned by the report server's create/update
                      endpoint (untyped in the gateway).
                  previewUrl:
                    type: string
                    description: Token-gated live preview URL.
                  draftSlug:
                    type: string
                  version:
                    type: string
                  title:
                    type: string
                  lint:
                    type: object
                    additionalProperties: true
                    properties:
                      errors:
                        type: array
                        items:
                          type: string
                additionalProperties: true
        '400':
          description: 'Zod validation failure, or an executor error envelope: missing / empty `spec`,
            invalid `spec_json`, a failed preview (`Report preview failed: <detail>`), or a guardrails
            rejection for an untrusted key.'
          content:
            application/json:
              schema:
                type: object
                properties:
                  error:
                    type: string
        '401':
          description: Missing or invalid API key.
          content:
            application/json:
              schema:
                type: object
                properties:
                  error:
                    type: string
        '403':
          description: The API key lacks `reports:write`.
          content:
            application/json:
              schema:
                type: object
                properties:
                  error:
                    type: string
        '404':
          description: No client matching `X-Client-Id` exists in this agency.
          content:
            application/json:
              schema:
                type: object
                properties:
                  error:
                    type: string
                  message:
                    type: string
        '429':
          description: Rate limit exceeded.
          content:
            application/json:
              schema:
                type: object
                properties:
                  error:
                    type: string
  /v1/report-authoring/sync:
    post:
      operationId: syncAuthoredReports
      tags:
      - report-authoring
      summary: File-based repo sync of a client's built reports
      description: 'Syncs an agency authoring repo''s reports for ONE client into the portal (the external
        equivalent of the monorepo''s CI sync). The payload carries BUILT report content; the portal owns
        rendering. Conflicts are resolved by `decideInboundReportSync`: portal-edited rows are flagged
        (`sync_status = ''conflict''`, losing version stashed in `sync_pending`), never clobbered, and
        portal-native rows are never touched. Slugs listed in `deleted` are archived (never portal-origin
        rows). Draft syncs mint a token-gated `previewUrl` on a best-effort basis.


        Tenancy: the target client comes from the resolved key context, never from the payload; `portal_client_id`
        is forced onto the written `client_configs` row and only whitelisted config fields are accepted
        (`service_account_key_path` and `mcp_servers` are dropped).


        Auth: API key plus a resolved client.

        Required scope: `reports:write` (requireScope) AND an agency-type key — client-scoped keys are
        rejected with 403.

        Agency-wide keys must name the target client via `X-Client-Id`.


        Body limit: 50 MB, enforced before validation.'
      parameters:
      - name: X-Client-Id
        in: header
        required: false
        description: Target client (id, slug, or name). Required for agency-wide API keys; ignored for
          client-scoped keys.
        schema:
          type: string
      requestBody:
        required: true
        content:
          application/json:
            schema:
              type: object
              required:
              - client
              properties:
                client:
                  type: string
                  pattern: ^[a-z0-9][a-z0-9-]{0,80}$
                  description: The caller repo's client slug (their `clients/<slug>/` folder). Keys `client_configs`
                    and the embed paths.
                client_config:
                  type: object
                  description: Optional client.config.json content — whitelisted fields only.
                  properties:
                    name:
                      type: string
                      maxLength: 200
                    gcp_project_id:
                      type: string
                      maxLength: 100
                    datasets:
                      type: object
                      additionalProperties:
                        type: string
                        maxLength: 200
                    branding:
                      type: object
                      properties:
                        company_name:
                          type: string
                          maxLength: 200
                reports:
                  type: array
                  default: []
                  maxItems: 100
                  items:
                    type: object
                    required:
                    - slug
                    - name
                    - spec_hash
                    properties:
                      slug:
                        type: string
                        pattern: ^[a-z0-9][a-z0-9-]{0,80}$
                      name:
                        type: string
                        minLength: 1
                        maxLength: 200
                      description:
                        type: string
                        maxLength: 4000
                        nullable: true
                      category:
                        type: string
                        maxLength: 100
                        nullable: true
                      status:
                        type: string
                        enum:
                        - draft
                        - published
                        default: published
                      report_type:
                        type: string
                        maxLength: 40
                        nullable: true
                      bigquery_tables:
                        type: array
                        maxItems: 100
                        nullable: true
                        items:
                          type: string
                          maxLength: 300
                      report_meta:
                        type: object
                        nullable: true
                        additionalProperties: true
                        description: Arbitrary metadata. `report_meta.queries`, if present, is stored
                          separately as `report_queries` and feeds the report-sidebar AI prompt.
                      html_content:
                        type: string
                        maxLength: 2000000
                        nullable: true
                        description: Built HTML for the report (max 2MB).
                      spec_hash:
                        type: string
                        minLength: 8
                        maxLength: 128
                deleted:
                  type: array
                  default: []
                  maxItems: 100
                  description: Report slugs removed from the repo, archived in the portal.
                  items:
                    type: string
                    pattern: ^[a-z0-9][a-z0-9-]{0,80}$
      responses:
        '200':
          description: Sync completed; per-report outcomes in `results`.
          content:
            application/json:
              schema:
                type: object
                properties:
                  ok:
                    type: boolean
                  client:
                    type: string
                    description: Echo of the request's repo client slug.
                  reportHost:
                    type: string
                    description: The host embed URLs were written against (legacy host for the home agency,
                      otherwise the agency's reports domain).
                  syncedBy:
                    type: string
                    description: Provenance string, `api_key:<id>`.
                  results:
                    type: array
                    items:
                      type: object
                      properties:
                        slug:
                          type: string
                        action:
                          type: string
                          enum:
                          - create
                          - apply
                          - conflict
                          - skip
                          - archive
                          - error
                        reason:
                          type: string
                          description: Present when the sync decision carried one (e.g. `already-in-sync`).
                        previewUrl:
                          type: string
                          description: Minted for draft reports that were created, applied, or already
                            in sync.
                        error:
                          type: string
                          description: Present only when `action` is `error`.
        '400':
          description: Zod validation failure on the request body, or a missing `X-Client-Id` for an agency-wide
            key.
          content:
            application/json:
              schema:
                type: object
                properties:
                  error:
                    type: string
                  message:
                    type: string
        '401':
          description: Missing or invalid API key.
          content:
            application/json:
              schema:
                type: object
                properties:
                  error:
                    type: string
        '403':
          description: The key is not agency-scoped (`Report sync requires an agency-scoped API key.`),
            or it lacks `reports:write`.
          content:
            application/json:
              schema:
                type: object
                properties:
                  error:
                    type: string
        '404':
          description: No client matching `X-Client-Id` exists in this agency.
          content:
            application/json:
              schema:
                type: object
                properties:
                  error:
                    type: string
                  message:
                    type: string
        '409':
          description: Slug/tenancy conflict — the repo slug is bound to another client, the slug exists
            but is unbound to any portal client, this client is already synced under a different slug,
            or a non-home agency has no reports domain configured.
          content:
            application/json:
              schema:
                type: object
                properties:
                  error:
                    type: string
        '413':
          description: Sync payload too large (max 50MB). Split the sync into fewer reports.
          content:
            application/json:
              schema:
                type: object
                properties:
                  error:
                    type: string
        '429':
          description: Rate limit exceeded.
          content:
            application/json:
              schema:
                type: object
                properties:
                  error:
                    type: string
  /v1/report-authoring/validate-query:
    post:
      operationId: validateReportQuery
      tags:
      - report-authoring
      summary: Dry-run a report query
      description: 'Dry-runs the supplied SQL against the client''s authorized data and returns the resulting
        columns and estimated bytes, or a rejection. The SQL is never persisted; the call is audited via
        `trackAuthoringToolCall`.


        Auth: API key plus a resolved client.

        Required scope: `reports:read` OR `reports:write` (requireAnyScope).

        Agency-wide keys must name the target client via `X-Client-Id`.'
      parameters:
      - name: X-Client-Id
        in: header
        required: false
        description: Target client (id, slug, or name). Required for agency-wide API keys; ignored for
          client-scoped keys.
        schema:
          type: string
      requestBody:
        required: true
        content:
          application/json:
            schema:
              type: object
              required:
              - sql
              properties:
                sql:
                  type: string
                  minLength: 1
                  description: The query to dry-run.
      responses:
        '200':
          description: The dry-run result, proxied from the report server's `POST /api/authoring/validate-query`.
            Typed `unknown` in the gateway; per the route comment it carries columns + bytes, or a rejection.
          content:
            application/json:
              schema:
                type: object
                additionalProperties: true
        '400':
          description: Zod validation failure on the request body, or the executor's error envelope (e.g.
            `validate_query requires a 'sql' string.`).
          content:
            application/json:
              schema:
                type: object
                properties:
                  error:
                    type: string
        '401':
          description: Missing or invalid API key.
          content:
            application/json:
              schema:
                type: object
                properties:
                  error:
                    type: string
        '403':
          description: The API key lacks `reports:read` / `reports:write`.
          content:
            application/json:
              schema:
                type: object
                properties:
                  error:
                    type: string
        '404':
          description: No client matching `X-Client-Id` exists in this agency.
          content:
            application/json:
              schema:
                type: object
                properties:
                  error:
                    type: string
                  message:
                    type: string
        '429':
          description: Rate limit exceeded.
          content:
            application/json:
              schema:
                type: object
                properties:
                  error:
                    type: string
  /v1/reports/authoring/ensure-draft-preview:
    post:
      operationId: ensureReportDraftPreview
      tags:
      - reports
      summary: Seed a session draft from an existing report and return a preview URL
      description: 'Edit-flow bootstrap for the Studio: seeds an authoring draft for `sessionId`

        (optionally copied from `editReportId`) and returns a token-gated preview URL.


        Required scope: NONE declared on the route (no requireScope/requireAnyScope).

        Auth is `dualAuth` + `requireResolvedClient` + `rateLimit` from the file''s

        `.use("*", ...)` lines; the handler additionally calls

        assertCallerClientAccess(c, clientId), so a caller may only target a client

        it owns (API key: same client, or same agency for an agency key; JWT:

        platform_admin any, agency_admin own agency, client user own client).

        '
      requestBody:
        required: true
        content:
          application/json:
            schema:
              type: object
              required:
              - clientId
              - sessionId
              properties:
                clientId:
                  type: string
                  description: Portal clients.id the draft belongs to.
                sessionId:
                  type: string
                  description: Chat session id the draft is keyed by (`__draft-<sessionId>`).
                editReportId:
                  type: string
                  description: Existing report to seed the draft from (edit flow).
      responses:
        '200':
          description: Draft ensured.
          content:
            application/json:
              schema:
                type: object
                properties:
                  ok:
                    type: boolean
                  draftSlug:
                    type: string
                  previewUrl:
                    type: string
                    nullable: true
                  handEdited:
                    type: boolean
        '400':
          description: clientId and sessionId are required, or the caller supplied no client.
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ReportsError'
        '401':
          description: Authentication required (JWT caller with no user on the context).
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ReportsError'
        '403':
          description: Caller is not authorized for this client.
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ReportsError'
        '404':
          description: Client not found, or the client has no synced monorepo slug.
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ReportsError'
        '500':
          description: ensure-draft-preview failed.
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ReportsError'
  /v1/reports/authoring/promote:
    post:
      operationId: promoteReportToMonorepo
      tags:
      - reports
      summary: Promote a published portal report into the monorepo as one commit
      description: 'Composes the report''s source and commits it to the client''s monorepo, then

        flips the portal row''s origin to `promoted`. Refuses drafts (`__draft-` ids)

        and refuses reports whose `sync_status` is `conflict`.


        Required scope: NONE declared on the route. Beyond the inherited

        dualAuth / requireResolvedClient / rateLimit middleware, the handler is

        STAFF-GATED via isTrustedAuthor({ keyType, userRole }) and then calls

        assertCallerClientAccess(c, clientId).

        '
      requestBody:
        required: true
        content:
          application/json:
            schema:
              type: object
              required:
              - clientId
              - reportId
              properties:
                clientId:
                  type: string
                  description: Portal clients.id.
                reportId:
                  type: string
                  description: The report SLUG (not a uuid, and never a `__draft-` id).
      responses:
        '200':
          description: 'Committed, or not configured (`reason: "no_token"`, returned as 200 so the

            Studio can surface the message).

            '
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/PromoteResult'
        '400':
          description: clientId and reportId are required, or the report is still a draft.
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ReportsError'
        '401':
          description: Authentication required.
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ReportsError'
        '403':
          description: Promoting a report requires staff access, or the client is out of scope.
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ReportsError'
        '404':
          description: Client not found, or no synced monorepo slug for the client.
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ReportsError'
        '502':
          description: Compose or commit failed (a PromoteResult with committed=false).
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/PromoteResult'
        '500':
          description: Promote failed.
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ReportsError'
  /v1/reports/authoring/resolve-conflict:
    post:
      operationId: resolveReportSyncConflict
      tags:
      - reports
      summary: Resolve a report's portal/monorepo sync conflict by picking a winner
      description: '`keep-portal` force-promotes the portal version (and clears the stashed

        monorepo version); `pull-monorepo` discards the portal edit and re-applies the

        stashed monorepo version (falling back to re-reading monorepo HEAD).


        Required scope: NONE declared on the route. Inherited dualAuth /

        requireResolvedClient / rateLimit, plus a STAFF gate (isTrustedAuthor) and

        assertCallerClientAccess(c, clientId) in the handler.

        '
      requestBody:
        required: true
        content:
          application/json:
            schema:
              type: object
              required:
              - clientId
              - reportId
              - resolution
              properties:
                clientId:
                  type: string
                reportId:
                  type: string
                  description: The report SLUG.
                resolution:
                  type: string
                  enum:
                  - keep-portal
                  - pull-monorepo
      responses:
        '200':
          description: 'Conflict resolved. `keep-portal` returns the PromoteResult with `resolution`

            merged in; `pull-monorepo` returns the applied-version summary.

            '
          content:
            application/json:
              schema:
                oneOf:
                - allOf:
                  - type: object
                    properties:
                      resolution:
                        type: string
                        enum:
                        - keep-portal
                  - $ref: '#/components/schemas/PromoteResult'
                - type: object
                  properties:
                    resolution:
                      type: string
                      enum:
                      - pull-monorepo
                    ok:
                      type: boolean
                    specHash:
                      type: string
                      nullable: true
                    source:
                      type: string
                      enum:
                      - stashed
                      - head
                      description: Whether the applied version came from the stash or from monorepo HEAD.
        '400':
          description: clientId, reportId, and a valid resolution are required.
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ReportsError'
        '401':
          description: Authentication required.
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ReportsError'
        '403':
          description: Resolving a conflict requires staff access, or the client is out of scope.
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ReportsError'
        '404':
          description: Client not found, or no synced monorepo slug for the client.
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ReportsError'
        '502':
          description: 'keep-portal: the force-promote failed. pull-monorepo: the monorepo version

            could not be loaded (no stash and the report server''s checkout is stale).

            '
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ReportsError'
        '500':
          description: Resolve failed.
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ReportsError'
  /v1/reports/{reportId}/chat/debug:
    get:
      operationId: getReportChatDebug
      tags:
      - reports
      summary: Inspect the compiled system prompt, tools, and agent config for a report chat
      description: 'Mirrors the stream handler''s prompt assembly (agent template + grounding

        section + report-layout summary + skills section) without running the model.


        Required scope: NONE declared on the route — dualAuth /

        requireResolvedClient / rateLimit only. Returns 500 with an `error` body when

        the client''s agency has no AI agent configured.

        '
      parameters:
      - $ref: '#/components/parameters/ReportIdPath'
      - name: includeDiscussions
        in: query
        required: false
        schema:
          type: string
          enum:
          - 'true'
        description: Compared to the literal string "true"; when set, a `discussions` slot is added to
          the runtime context.
      responses:
        '200':
          description: Debug snapshot.
          content:
            application/json:
              schema:
                type: object
                properties:
                  systemPrompt:
                    type: string
                  reportType:
                    type: string
                    description: Defaults to "looker_studio" when the report row has none.
                  embedUrl:
                    type: string
                    nullable: true
                  posthogProjectId:
                    type: string
                    nullable: true
                  contextMessageCount:
                    type: integer
                    description: system_settings.chatContextMessages, default 10.
                  tools:
                    type: array
                    items:
                      type: object
                      properties:
                        name:
                          type: string
                        displayName:
                          type: string
                        description:
                          type: string
                          nullable: true
                  reportQueries:
                    type: string
                    nullable: true
                  bigqueryTables:
                    type: array
                    items:
                      type: string
                    description: Empty for posthog reports (the ids move to posthogDashboardIds).
                  posthogDashboardIds:
                    type: array
                    items:
                      type: string
                  posthogDashboardSummary:
                    type: string
                    nullable: true
                  posthogInsightsData:
                    nullable: true
                    description: Parsed from the stored JSON string; null when absent or unparseable.
                  glossaryTerms:
                    type: array
                    items:
                      type: object
                      properties:
                        id:
                          type: string
                        name:
                          type: string
                        definition:
                          type: string
                          description: Client override applied when one exists.
                  agentName:
                    type: string
                    nullable: true
                  agentSlug:
                    type: string
                    nullable: true
                  agentModelId:
                    type: string
                    nullable: true
                  agentThinkingLevel:
                    type: string
                    nullable: true
                  agentMaxTurns:
                    type: integer
                    nullable: true
                  agentTimeoutMs:
                    type: integer
                    nullable: true
                  promptTemplate:
                    type: string
                    nullable: true
                  promptTemplateId:
                    type: string
                    nullable: true
                  runtimeContext:
                    type: object
                    description: Template slots (agency_name, client_name, client_description, report_name,
                      report_description, bigquery_tables, report_queries, glossary_terms, discussions);
                      values are strings or undefined.
                    additionalProperties:
                      type: string
                  skills:
                    type: array
                    items:
                      type: object
                      properties:
                        slug:
                          type: string
                        name:
                          type: string
                        description:
                          type: string
                          nullable: true
        '404':
          description: Report not found.
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ReportsError'
        '500':
          description: No AI agent configured for this agency, or failed to get debug info.
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ReportsError'
  /v1/reports/{reportId}/chat/messages/{messageId}/feedback:
    post:
      operationId: submitReportChatMessageFeedback
      tags:
      - reports
      summary: Submit or update thumbs up/down on an assistant message
      description: 'Upserts one feedback row per (messageId, userId). The message must exist and

        belong to the caller; reportId is deliberately NOT part of that lookup.

        JWT-only: an API-key caller gets 403.


        Required scope: NONE declared on the route — dualAuth /

        requireResolvedClient / rateLimit only, plus the in-handler `authType === "jwt"` check.

        '
      parameters:
      - $ref: '#/components/parameters/ReportIdPath'
      - $ref: '#/components/parameters/MessageIdPath'
      requestBody:
        required: true
        description: Validated by zValidator("json", feedbackSchema).
        content:
          application/json:
            schema:
              type: object
              required:
              - rating
              properties:
                rating:
                  type: string
                  enum:
                  - positive
                  - negative
                comment:
                  type: string
      responses:
        '200':
          description: The stored feedback row (Drizzle `.returning()` on chat_message_feedback).
          content:
            application/json:
              schema:
                type: object
                properties:
                  feedback:
                    type: object
                    properties:
                      id:
                        type: string
                      messageId:
                        type: string
                      userId:
                        type: string
                        nullable: true
                      reportId:
                        type: string
                        nullable: true
                        description: Mirrors the MESSAGE's report (null for Studio chats), not the URL
                          param.
                      sessionId:
                        type: string
                        nullable: true
                      rating:
                        type: string
                        enum:
                        - positive
                        - negative
                      comment:
                        type: string
                        nullable: true
                      createdAt:
                        type: string
                        format: date-time
                      updatedAt:
                        type: string
                        format: date-time
        '400':
          description: Request body failed feedbackSchema validation (zValidator).
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ReportsError'
        '403':
          description: JWT auth required.
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ReportsError'
        '404':
          description: Message not found, or not owned by the caller.
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ReportsError'
        '500':
          description: Failed to submit feedback.
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ReportsError'
    delete:
      operationId: deleteReportChatMessageFeedback
      tags:
      - reports
      summary: Remove the caller's feedback on a message
      description: 'Deletes the (messageId, userId) feedback row. Returns success even when no row

        existed. JWT-only: an API-key caller gets 403.


        Required scope: NONE declared on the route — dualAuth /

        requireResolvedClient / rateLimit only, plus the in-handler `authType === "jwt"` check.

        '
      parameters:
      - $ref: '#/components/parameters/ReportIdPath'
      - $ref: '#/components/parameters/MessageIdPath'
      responses:
        '200':
          description: Deleted.
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ReportsSuccess'
        '403':
          description: JWT auth required.
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ReportsError'
        '500':
          description: Failed to delete feedback.
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ReportsError'
  /v1/reports/{reportId}/chat/sessions:
    get:
      operationId: listReportChatSessions
      tags:
      - reports
      summary: List the caller's chat sessions for a report
      description: 'JWT callers get their own sessions, aggregated from `chat_messages` and sorted

        by last message descending. API-key callers always get an empty list

        (API sessions are tracked at client level).


        Required scope: NONE declared on the route — dualAuth /

        requireResolvedClient / rateLimit only.

        '
      parameters:
      - $ref: '#/components/parameters/ReportIdPath'
      responses:
        '200':
          description: Session list (an empty array for API-key auth).
          content:
            application/json:
              schema:
                type: object
                properties:
                  sessions:
                    type: array
                    items:
                      type: object
                      properties:
                        sessionId:
                          type: string
                          description: '"default" for messages stored without a session id.'
                        createdAt:
                          type: string
                          format: date-time
                        lastMessageAt:
                          type: string
                          format: date-time
                        messageCount:
                          type: integer
                        preview:
                          type: string
                          description: First 100 chars, preferring a user message.
        '404':
          description: Report not found.
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ReportsError'
        '500':
          description: Failed to list sessions.
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ReportsError'
  /v1/reports/{reportId}/chat/sessions/{sessionId}:
    get:
      operationId: getReportChatSession
      tags:
      - reports
      summary: Get the messages of one chat session
      description: 'Filtered by sessionId + the caller''s own userId, NOT by reportId (Studio chats

        carry report_id = null). Exception: a thread its owner shared is also readable

        by platform_admin, or by an agency_admin of the owning agency

        (isSharedThreadReadableBy). API-key callers always get 404.


        Required scope: NONE declared on the route — dualAuth /

        requireResolvedClient / rateLimit only.

        '
      parameters:
      - $ref: '#/components/parameters/ReportIdPath'
      - $ref: '#/components/parameters/SessionIdPath'
      responses:
        '200':
          description: The session's messages.
          content:
            application/json:
              schema:
                type: object
                properties:
                  session:
                    type: object
                    properties:
                      id:
                        type: string
                  messages:
                    type: array
                    items:
                      type: object
                      properties:
                        id:
                          type: string
                        role:
                          type: string
                          description: e.g. "user" | "assistant".
                        content:
                          type: string
                        sessionId:
                          type: string
                          nullable: true
                        metadata:
                          nullable: true
                          description: Stored message metadata, passed through verbatim.
                        createdAt:
                          type: string
                          format: date-time
                        inputTokens:
                          type: integer
                          nullable: true
                        outputTokens:
                          type: integer
                          nullable: true
                        feedback:
                          nullable: true
                          description: Present only for assistant messages; omitted otherwise.
                          type: object
                          properties:
                            rating:
                              type: string
                              enum:
                              - positive
                              - negative
                            comment:
                              type: string
                              nullable: true
        '404':
          description: Session not found (returned for every API-key caller).
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ReportsError'
        '500':
          description: Failed to get session.
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ReportsError'
    delete:
      operationId: deleteReportChatSession
      tags:
      - reports
      summary: Delete the caller's messages for a chat session
      description: 'Deletes `chat_messages` matching reportId + sessionId + the caller''s userId.

        API-key callers get 404.


        Required scope: NONE declared on the route — dualAuth /

        requireResolvedClient / rateLimit only.

        '
      parameters:
      - $ref: '#/components/parameters/ReportIdPath'
      - $ref: '#/components/parameters/SessionIdPath'
      responses:
        '200':
          description: Deleted.
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ReportsSuccess'
        '404':
          description: Session not found (returned for every API-key caller).
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ReportsError'
        '500':
          description: Failed to delete session.
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ReportsError'
  /v1/reports/{reportId}/chat/stream:
    post:
      operationId: streamReportChat
      tags:
      - reports
      summary: Streaming AI chat for a report (Server-Sent Events)
      description: 'Report-centric entry point into the shared `handleChatStream`. On success the

        response is an SSE stream (`streamSSE`), not JSON: each frame is an

        `event: message` whose `data` is a JSON object with a `type` discriminator

        (including `type: "error"` with `{ message }` for a mid-stream failure).

        `reportId` may be a real report id or a synthetic `__draft-<sessionId>` Studio

        id; a draft requires `clientId` in the body and re-checks tenant access.


        Required scope: NONE declared on the route — dualAuth /

        requireResolvedClient / rateLimit only. Scope enforcement is PER TOOL for

        API-key callers via chatBuiltinScopeAllowed(): family reads accept

        `<family>:read` or `<family>:write` (glossary, reports, memories,

        bigquery_tables, alerts, metrics, documents, tasks, prompts, skills,

        incrementality), writes require `<family>:write`, Slack reads/sends require

        `slack:read` / `slack:write`, web tools `web:read`, discussion writes

        `discussions:write`, task-proposal decisions `task_proposals:write`, and

        `author_dataform_model` requires `dataform:author`. Unmapped tools are always-on.

        '
      parameters:
      - name: reportId
        in: path
        required: true
        schema:
          type: string
        description: A report id, or a synthetic `__draft-<sessionId>` Studio authoring id.
      requestBody:
        required: true
        description: Validated by zValidator("json", chatMessageSchema).
        content:
          application/json:
            schema:
              $ref: '#/components/schemas/ChatStreamRequest'
      responses:
        '200':
          description: SSE stream of chat events.
          content:
            text/event-stream:
              schema:
                type: string
                description: 'SSE frames, each `event: message` with a JSON `data` payload carrying a

                  `type` field. Individual event payload shapes are produced by

                  lib/pi-sse-bridge.ts (streamAgentToSSE) and are not enumerated here.

                  '
        '400':
          description: Request body failed chatMessageSchema validation (zValidator).
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ReportsError'
        '401':
          description: Authentication required for the draft's client.
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ReportsError'
        '403':
          description: Caller is not authorized for the resolved client.
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ReportsError'
        '404':
          description: Report not found.
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ReportsError'
        '500':
          description: Setup failed before the stream opened.
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ReportsError'
  /v1/reports/{reportId}/chat/tools:
    get:
      operationId: listReportChatTools
      tags:
      - reports
      summary: List the AI tools available to this report's chat
      description: 'Returns the client''s DB tools (webhook + MCP-server tools, agency/client

        filtered) plus the role-appropriate built-in tools.


        Required scope: NONE declared on the route (dualAuth /

        requireResolvedClient / rateLimit only). For an API-key caller the built-in

        tools are filtered by the key''s effective scopes via

        chatBuiltinScopeAllowed() — each family read needs `<family>:read` OR

        `<family>:write` and each write needs `<family>:write` (e.g. metrics:read /

        metrics:write for list_metrics, reports:write for save_report,

        task_proposals:write for the proposal decisions, dataform:author for

        author_dataform_model). Tools with no scope mapping are always-on.

        '
      parameters:
      - $ref: '#/components/parameters/ReportIdPath'
      responses:
        '200':
          description: The tool list.
          content:
            application/json:
              schema:
                type: object
                properties:
                  tools:
                    type: array
                    items:
                      type: object
                      properties:
                        name:
                          type: string
                        displayName:
                          type: string
                        description:
                          type: string
                          nullable: true
                        inputSchema:
                          type: object
                          nullable: true
                          description: JSON Schema for the tool's arguments (parsed from storage).
                          additionalProperties: true
        '404':
          description: Report not found.
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ReportsError'
        '500':
          description: Failed to fetch tools.
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ReportsError'
  /v1/reports/{reportId}/publish:
    post:
      operationId: publishReportDraft
      tags:
      - reports
      summary: Publish an authoring draft as a client-visible report
      description: 'Deterministic (no agent): reads the draft''s stored ReportSpec from

        `report_meta.report_spec` and re-composes it published with a real slug.

        `reportId` MUST be a `__draft-<sessionId>` id. If the target report''s origin is

        `promoted`, the regenerated source is re-promoted to the monorepo best-effort

        and reported under `repoSync`.


        Required scope: NONE declared on the route. Beyond the inherited dualAuth /

        requireResolvedClient / rateLimit middleware, the handler is STAFF-GATED via

        isTrustedAuthor({ keyType, userRole }) and then calls

        assertCallerClientAccess(c, <draft''s client_id>).

        '
      parameters:
      - name: reportId
        in: path
        required: true
        schema:
          type: string
        description: The authoring draft id — must start with `__draft-`.
      requestBody:
        required: false
        description: Parsed with `c.req.json().catch(() => ({}))`; an absent or invalid body is treated
          as `{}`.
        content:
          application/json:
            schema:
              type: object
              properties:
                targetReportId:
                  type: string
                  description: Edit save-back — overwrite this existing report in place instead of creating
                    a new slug.
                title:
                  type: string
                  description: Rename on publish; trimmed, and applied to the spec title.
      responses:
        '200':
          description: Published.
          content:
            application/json:
              schema:
                type: object
                properties:
                  published:
                    type: boolean
                  reportId:
                    type: string
                    description: The published report's slug.
                  client:
                    type: string
                    description: Monorepo client slug.
                  embedPath:
                    type: string
                    description: '`/embed/{client}/{reportId}`.'
                  repoSync:
                    allOf:
                    - $ref: '#/components/schemas/PromoteResult'
                    description: Present only when the published report's origin is `promoted`.
        '400':
          description: Not a `__draft-` id, or the draft has no composed report yet.
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ReportsError'
        '401':
          description: Authentication required.
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ReportsError'
        '403':
          description: Publishing requires staff access, or the draft's client is out of scope.
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ReportsError'
        '404':
          description: Draft not found, or its client not found.
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ReportsError'
        '422':
          description: The report server rejected the compose.
          content:
            application/json:
              schema:
                type: object
                properties:
                  error:
                    type: string
                  lint:
                    description: Lint diagnostics from the authoring service, passed through verbatim.
                    nullable: true
        '500':
          description: Could not resolve the client slug for this draft, or publish failed.
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ReportsError'
  /v1/scheduled-tasks:
    get:
      operationId: listScheduledTasks
      summary: List this client's scheduled tasks
      description: 'Lists the authenticated key''s client''s OWN scheduled tasks (agency-owned tasks are

        excluded), newest first, each augmented with `runCount` and `totalCostUsd`.

        Required scope: `tasks:read` OR `tasks:write` (requireAnyScope).

        `clientId` is always derived from the API key, never from the caller.

        '
      tags:
      - scheduled-tasks
      responses:
        '200':
          description: The client's tasks
          content:
            application/json:
              schema:
                type: object
                required:
                - tasks
                properties:
                  tasks:
                    type: array
                    items:
                      allOf:
                      - $ref: '#/paths/~1v1~1scheduled-tasks/get/x-schemas/ScheduledTask'
                      - type: object
                        properties:
                          runCount:
                            type: integer
                          totalCostUsd:
                            type: number
        '400':
          $ref: '#/paths/~1v1~1scheduled-tasks/get/x-schemas/MissingTargetClient'
        '401':
          $ref: '#/paths/~1v1~1scheduled-tasks/get/x-schemas/Unauthorized'
        '403':
          $ref: '#/paths/~1v1~1scheduled-tasks/get/x-schemas/Forbidden'
        '404':
          $ref: '#/paths/~1v1~1scheduled-tasks/get/x-schemas/ClientNotFound'
        '409':
          $ref: '#/paths/~1v1~1scheduled-tasks/get/x-schemas/AmbiguousClient'
        '429':
          $ref: '#/paths/~1v1~1scheduled-tasks/get/x-schemas/RateLimited'
      x-schemas:
        ScheduledTask:
          type: object
          description: A row of `scheduled_tasks` (Drizzle `select()` / `returning()`, so all columns).
          properties:
            id:
              type: string
            clientId:
              type: string
              nullable: true
            agencyId:
              type: string
              nullable: true
            targetClientIds:
              type: array
              nullable: true
              items:
                type: string
            name:
              type: string
            instruction:
              type: string
              nullable: true
            commands:
              type: array
              nullable: true
              items:
                type: object
                required:
                - tool
                properties:
                  tool:
                    type: string
                  args:
                    type: object
                    additionalProperties: true
                  autoRun:
                    type: boolean
                    description: Staff-only; forced off for non-agency API keys.
            scheduleType:
              type: string
              default: cron
            cronExpression:
              type: string
              nullable: true
            timezone:
              type: string
              default: UTC
            enabledTools:
              description: Free-form JSON.
              nullable: true
            modelId:
              type: string
              nullable: true
            agentId:
              type: string
              nullable: true
            status:
              type: string
              enum:
              - active
              - paused
            pausedReason:
              type: string
              nullable: true
            monthlyBudgetUsd:
              type: string
              nullable: true
              description: Numeric stored as text.
            createdBy:
              type: string
              nullable: true
            lastRunAt:
              type: string
              format: date-time
              nullable: true
            nextRunAt:
              type: string
              format: date-time
              nullable: true
            lastError:
              type: string
              nullable: true
            consecutiveErrors:
              type: integer
            createdAt:
              type: string
              format: date-time
            updatedAt:
              type: string
              format: date-time
        ScheduledTaskRun:
          type: object
          description: A row of `scheduled_task_runs`.
          properties:
            id:
              type: string
            taskId:
              type: string
            clientId:
              type: string
              nullable: true
            startedAt:
              type: string
              format: date-time
            finishedAt:
              type: string
              format: date-time
              nullable: true
            status:
              type: string
              description: e.g. running, success, error (default `running`).
            summary:
              type: string
              nullable: true
            proposals:
              type: array
              nullable: true
              description: Free-form proposal objects; indexed by `proposalIndex` in the decision routes.
              items:
                type: object
                additionalProperties: true
            reviewStatus:
              type: string
              description: Default `pending`.
            reviewedBy:
              type: string
              nullable: true
            reviewedAt:
              type: string
              format: date-time
              nullable: true
            reviewSlackMessage:
              type: object
              nullable: true
              properties:
                connectionId:
                  type: string
                  nullable: true
                channelId:
                  type: string
                ts:
                  type: string
            toolCalls:
              description: Free-form JSON.
              nullable: true
            costUsd:
              type: string
              nullable: true
            errorMessage:
              type: string
              nullable: true
            createdAt:
              type: string
              format: date-time
        Error:
          type: object
          properties:
            error:
              type: string
            message:
              type: string
        Unauthorized:
          description: Missing or invalid API key (apiKeyAuth middleware).
          content:
            application/json:
              schema:
                $ref: '#/paths/~1v1~1scheduled-tasks/get/x-schemas/Error'
        Forbidden:
          description: Permission denied — the key lacks the required scope.
          content:
            application/json:
              schema:
                $ref: '#/paths/~1v1~1scheduled-tasks/get/x-schemas/Error'
        MissingTargetClient:
          description: 'Agency-wide key without the `X-Client-Id` header (requireResolvedClient), or a

            request-body/validation failure on write routes.

            '
          content:
            application/json:
              schema:
                $ref: '#/paths/~1v1~1scheduled-tasks/get/x-schemas/Error'
        ClientNotFound:
          description: No client matching `X-Client-Id` exists in this agency.
          content:
            application/json:
              schema:
                $ref: '#/paths/~1v1~1scheduled-tasks/get/x-schemas/Error'
        AmbiguousClient:
          description: Multiple clients share that `X-Client-Id` name — send an id.
          content:
            application/json:
              schema:
                $ref: '#/paths/~1v1~1scheduled-tasks/get/x-schemas/Error'
        RateLimited:
          description: Rate limit exceeded (rateLimit middleware).
          content:
            application/json:
              schema:
                type: object
                properties:
                  error:
                    type: string
                  message:
                    type: string
                  retryAfter:
                    type: integer
                    description: Seconds until the window resets.
    post:
      operationId: createScheduledTask
      summary: Create a scheduled task
      description: 'Creates a scheduled task owned by the API key''s client. Required scope: `tasks:write`.

        A task needs `instruction`, `commands`, or both — "neither" is a 400. `commands[].autoRun`

        is only honored for agency-type keys. Per-client task-count and budget caps are enforced

        by the core and surface as 400s.

        '
      tags:
      - scheduled-tasks
      requestBody:
        required: true
        content:
          application/json:
            schema:
              type: object
              required:
              - name
              - cronExpression
              properties:
                name:
                  type: string
                  minLength: 1
                instruction:
                  type: string
                commands:
                  type: array
                  nullable: true
                  items:
                    type: object
                    required:
                    - tool
                    properties:
                      tool:
                        type: string
                        minLength: 1
                      args:
                        type: object
                        additionalProperties: true
                      autoRun:
                        type: boolean
                cronExpression:
                  type: string
                  minLength: 1
                timezone:
                  type: string
                  description: Defaults to UTC.
                enabledTools:
                  description: Free-form JSON.
                modelId:
                  type: string
                  nullable: true
                agentId:
                  type: string
                  nullable: true
                monthlyBudgetUsd:
                  nullable: true
                  oneOf:
                  - type: string
                  - type: number
      responses:
        '201':
          description: Task created
          content:
            application/json:
              schema:
                type: object
                required:
                - task
                properties:
                  task:
                    $ref: '#/paths/~1v1~1scheduled-tasks/get/x-schemas/ScheduledTask'
        '400':
          description: Zod validation failure, or a ScheduledTaskValidationError (task cap, bad agentId,
            no instruction and no commands).
          content:
            application/json:
              schema:
                $ref: '#/paths/~1v1~1scheduled-tasks/get/x-schemas/Error'
        '401':
          $ref: '#/paths/~1v1~1scheduled-tasks/get/x-schemas/Unauthorized'
        '403':
          $ref: '#/paths/~1v1~1scheduled-tasks/get/x-schemas/Forbidden'
        '404':
          $ref: '#/paths/~1v1~1scheduled-tasks/get/x-schemas/ClientNotFound'
        '409':
          $ref: '#/paths/~1v1~1scheduled-tasks/get/x-schemas/AmbiguousClient'
        '429':
          $ref: '#/paths/~1v1~1scheduled-tasks/get/x-schemas/RateLimited'
        '500':
          description: Failed to create scheduled task
          content:
            application/json:
              schema:
                $ref: '#/paths/~1v1~1scheduled-tasks/get/x-schemas/Error'
  /v1/scheduled-tasks/runs:
    get:
      operationId: listAllScheduledTaskRuns
      summary: List runs across all of this client's tasks
      description: 'Runs across all of the client''s OWN tasks, newest first, capped at 100, each row

        carrying its task''s name as `taskName`. Required scope: `tasks:read` OR `tasks:write`.

        '
      tags:
      - scheduled-tasks
      parameters:
      - name: pending
        in: query
        required: false
        schema:
          type: string
          enum:
          - '1'
        description: Exactly `1` restricts the list to runs with `reviewStatus = "pending"` (the review
          inbox). Any other value is ignored.
      responses:
        '200':
          description: Runs
          content:
            application/json:
              schema:
                type: object
                required:
                - runs
                properties:
                  runs:
                    type: array
                    items:
                      allOf:
                      - $ref: '#/paths/~1v1~1scheduled-tasks/get/x-schemas/ScheduledTaskRun'
                      - type: object
                        properties:
                          taskName:
                            type: string
        '400':
          $ref: '#/paths/~1v1~1scheduled-tasks/get/x-schemas/MissingTargetClient'
        '401':
          $ref: '#/paths/~1v1~1scheduled-tasks/get/x-schemas/Unauthorized'
        '403':
          $ref: '#/paths/~1v1~1scheduled-tasks/get/x-schemas/Forbidden'
        '404':
          $ref: '#/paths/~1v1~1scheduled-tasks/get/x-schemas/ClientNotFound'
        '409':
          $ref: '#/paths/~1v1~1scheduled-tasks/get/x-schemas/AmbiguousClient'
        '429':
          $ref: '#/paths/~1v1~1scheduled-tasks/get/x-schemas/RateLimited'
  /v1/scheduled-tasks/runs/{runId}:
    parameters:
    - name: runId
      in: path
      required: true
      schema:
        type: string
    get:
      operationId: getScheduledTaskRun
      summary: Get one run with its proposals
      description: 'Returns the run and its parent task. Scoped to the run''s effective client (the run''s
        own

        `clientId`, falling back to the task''s), so a fan-out run can be addressed by the target

        client it ran against. Required scope: `tasks:read` OR `tasks:write`.

        '
      tags:
      - scheduled-tasks
      responses:
        '200':
          description: The run and its task
          content:
            application/json:
              schema:
                type: object
                required:
                - run
                - task
                properties:
                  run:
                    $ref: '#/paths/~1v1~1scheduled-tasks/get/x-schemas/ScheduledTaskRun'
                  task:
                    $ref: '#/paths/~1v1~1scheduled-tasks/get/x-schemas/ScheduledTask'
        '400':
          $ref: '#/paths/~1v1~1scheduled-tasks/get/x-schemas/MissingTargetClient'
        '401':
          $ref: '#/paths/~1v1~1scheduled-tasks/get/x-schemas/Unauthorized'
        '403':
          $ref: '#/paths/~1v1~1scheduled-tasks/get/x-schemas/Forbidden'
        '404':
          description: '`{"error":"Run not found"}` — or no client matching `X-Client-Id`.'
          content:
            application/json:
              schema:
                $ref: '#/paths/~1v1~1scheduled-tasks/get/x-schemas/Error'
        '409':
          $ref: '#/paths/~1v1~1scheduled-tasks/get/x-schemas/AmbiguousClient'
        '429':
          $ref: '#/paths/~1v1~1scheduled-tasks/get/x-schemas/RateLimited'
  /v1/scheduled-tasks/runs/{runId}/approve:
    parameters:
    - name: runId
      in: path
      required: true
      schema:
        type: string
    post:
      operationId: approveScheduledTaskProposal
      summary: Approve one proposal in a run
      description: 'Approves the proposal at `proposalIndex` and routes it to its send target. Optionally

        replaces the proposal body with `body` before sending. Required scope: `task_proposals:write`

        (deliberately separate from `tasks:write` — approving can send a real message).

        '
      tags:
      - scheduled-tasks
      requestBody:
        required: true
        content:
          application/json:
            schema:
              type: object
              required:
              - proposalIndex
              properties:
                proposalIndex:
                  type: integer
                  minimum: 0
                body:
                  type: string
                  minLength: 1
                  description: Edited proposal body to send instead of the generated one (trimmed, non-empty).
      responses:
        '200':
          description: Approved
          content:
            application/json:
              schema:
                type: object
                properties:
                  success:
                    type: boolean
                    enum:
                    - true
        '400':
          description: Zod validation failure, or a decision failure that is not "not found" (e.g. `Proposal
            not found`, send failure).
          content:
            application/json:
              schema:
                $ref: '#/paths/~1v1~1scheduled-tasks/get/x-schemas/Error'
        '401':
          $ref: '#/paths/~1v1~1scheduled-tasks/get/x-schemas/Unauthorized'
        '403':
          $ref: '#/paths/~1v1~1scheduled-tasks/get/x-schemas/Forbidden'
        '404':
          description: Run not found (or no client matching `X-Client-Id`).
          content:
            application/json:
              schema:
                $ref: '#/paths/~1v1~1scheduled-tasks/get/x-schemas/Error'
        '409':
          $ref: '#/paths/~1v1~1scheduled-tasks/get/x-schemas/AmbiguousClient'
        '429':
          $ref: '#/paths/~1v1~1scheduled-tasks/get/x-schemas/RateLimited'
  /v1/scheduled-tasks/runs/{runId}/dismiss:
    parameters:
    - name: runId
      in: path
      required: true
      schema:
        type: string
    post:
      operationId: dismissScheduledTaskProposal
      summary: Dismiss one proposal, or the whole run
      description: 'Omitting `proposalIndex` dismisses the entire run; supplying it dismisses just that

        proposal. Required scope: `task_proposals:write`.

        '
      tags:
      - scheduled-tasks
      requestBody:
        required: true
        content:
          application/json:
            schema:
              type: object
              properties:
                proposalIndex:
                  type: integer
                  minimum: 0
                  description: Omit to dismiss the whole run.
      responses:
        '200':
          description: Dismissed
          content:
            application/json:
              schema:
                type: object
                properties:
                  success:
                    type: boolean
                    enum:
                    - true
        '400':
          description: Zod validation failure, or a dismissal failure that is not "not found".
          content:
            application/json:
              schema:
                $ref: '#/paths/~1v1~1scheduled-tasks/get/x-schemas/Error'
        '401':
          $ref: '#/paths/~1v1~1scheduled-tasks/get/x-schemas/Unauthorized'
        '403':
          $ref: '#/paths/~1v1~1scheduled-tasks/get/x-schemas/Forbidden'
        '404':
          description: Run not found (or no client matching `X-Client-Id`).
          content:
            application/json:
              schema:
                $ref: '#/paths/~1v1~1scheduled-tasks/get/x-schemas/Error'
        '409':
          $ref: '#/paths/~1v1~1scheduled-tasks/get/x-schemas/AmbiguousClient'
        '429':
          $ref: '#/paths/~1v1~1scheduled-tasks/get/x-schemas/RateLimited'
  /v1/scheduled-tasks/runs/{runId}/restore:
    parameters:
    - name: runId
      in: path
      required: true
      schema:
        type: string
    post:
      operationId: restoreScheduledTaskProposal
      summary: Undo a dismissal
      description: 'Restores a dismissed run, or a single dismissed proposal when `proposalIndex` is given

        (omitted is passed to the core as `null`). Required scope: `task_proposals:write`.

        '
      tags:
      - scheduled-tasks
      requestBody:
        required: true
        content:
          application/json:
            schema:
              type: object
              properties:
                proposalIndex:
                  type: integer
                  minimum: 0
                  description: Omit to restore the whole run.
      responses:
        '200':
          description: Restored
          content:
            application/json:
              schema:
                type: object
                properties:
                  success:
                    type: boolean
                    enum:
                    - true
        '400':
          description: Zod validation failure, or a restore failure that is not "not found".
          content:
            application/json:
              schema:
                $ref: '#/paths/~1v1~1scheduled-tasks/get/x-schemas/Error'
        '401':
          $ref: '#/paths/~1v1~1scheduled-tasks/get/x-schemas/Unauthorized'
        '403':
          $ref: '#/paths/~1v1~1scheduled-tasks/get/x-schemas/Forbidden'
        '404':
          description: Run not found (or no client matching `X-Client-Id`).
          content:
            application/json:
              schema:
                $ref: '#/paths/~1v1~1scheduled-tasks/get/x-schemas/Error'
        '409':
          $ref: '#/paths/~1v1~1scheduled-tasks/get/x-schemas/AmbiguousClient'
        '429':
          $ref: '#/paths/~1v1~1scheduled-tasks/get/x-schemas/RateLimited'
  /v1/scheduled-tasks/{id}:
    parameters:
    - name: id
      in: path
      required: true
      schema:
        type: string
    get:
      operationId: getScheduledTask
      summary: Get one scheduled task
      description: 'Required scope: `tasks:read` OR `tasks:write`. Scoped to the API key''s client.'
      tags:
      - scheduled-tasks
      responses:
        '200':
          description: The task
          content:
            application/json:
              schema:
                type: object
                required:
                - task
                properties:
                  task:
                    $ref: '#/paths/~1v1~1scheduled-tasks/get/x-schemas/ScheduledTask'
        '400':
          $ref: '#/paths/~1v1~1scheduled-tasks/get/x-schemas/MissingTargetClient'
        '401':
          $ref: '#/paths/~1v1~1scheduled-tasks/get/x-schemas/Unauthorized'
        '403':
          $ref: '#/paths/~1v1~1scheduled-tasks/get/x-schemas/Forbidden'
        '404':
          description: Task not found (or no client matching `X-Client-Id`).
          content:
            application/json:
              schema:
                $ref: '#/paths/~1v1~1scheduled-tasks/get/x-schemas/Error'
        '409':
          $ref: '#/paths/~1v1~1scheduled-tasks/get/x-schemas/AmbiguousClient'
        '429':
          $ref: '#/paths/~1v1~1scheduled-tasks/get/x-schemas/RateLimited'
    put:
      operationId: updateScheduledTask
      summary: Update a scheduled task
      description: 'Partial update — only supplied keys are written. An empty-string `instruction` clears
        it

        (turning the task into a pure-commands task). `commands[].autoRun` is only honored for

        agency-type keys. Required scope: `tasks:write`.

        '
      tags:
      - scheduled-tasks
      requestBody:
        required: true
        content:
          application/json:
            schema:
              type: object
              properties:
                name:
                  type: string
                  minLength: 1
                instruction:
                  type: string
                commands:
                  type: array
                  nullable: true
                  items:
                    type: object
                    required:
                    - tool
                    properties:
                      tool:
                        type: string
                        minLength: 1
                      args:
                        type: object
                        additionalProperties: true
                      autoRun:
                        type: boolean
                cronExpression:
                  type: string
                  minLength: 1
                timezone:
                  type: string
                enabledTools:
                  description: Free-form JSON.
                modelId:
                  type: string
                  nullable: true
                agentId:
                  type: string
                  nullable: true
                monthlyBudgetUsd:
                  nullable: true
                  oneOf:
                  - type: string
                  - type: number
      responses:
        '200':
          description: The updated task
          content:
            application/json:
              schema:
                type: object
                required:
                - task
                properties:
                  task:
                    $ref: '#/paths/~1v1~1scheduled-tasks/get/x-schemas/ScheduledTask'
        '400':
          description: Zod validation failure or a ScheduledTaskValidationError (e.g. bad `agentId`).
          content:
            application/json:
              schema:
                $ref: '#/paths/~1v1~1scheduled-tasks/get/x-schemas/Error'
        '401':
          $ref: '#/paths/~1v1~1scheduled-tasks/get/x-schemas/Unauthorized'
        '403':
          $ref: '#/paths/~1v1~1scheduled-tasks/get/x-schemas/Forbidden'
        '404':
          description: Task not found (or no client matching `X-Client-Id`).
          content:
            application/json:
              schema:
                $ref: '#/paths/~1v1~1scheduled-tasks/get/x-schemas/Error'
        '409':
          $ref: '#/paths/~1v1~1scheduled-tasks/get/x-schemas/AmbiguousClient'
        '429':
          $ref: '#/paths/~1v1~1scheduled-tasks/get/x-schemas/RateLimited'
        '500':
          description: Failed to update scheduled task
          content:
            application/json:
              schema:
                $ref: '#/paths/~1v1~1scheduled-tasks/get/x-schemas/Error'
    delete:
      operationId: deleteScheduledTask
      summary: Delete a scheduled task
      description: 'Deletes the task; its runs cascade. Required scope: `tasks:write`.'
      tags:
      - scheduled-tasks
      responses:
        '200':
          description: Deleted
          content:
            application/json:
              schema:
                type: object
                properties:
                  success:
                    type: boolean
                    enum:
                    - true
        '400':
          $ref: '#/paths/~1v1~1scheduled-tasks/get/x-schemas/MissingTargetClient'
        '401':
          $ref: '#/paths/~1v1~1scheduled-tasks/get/x-schemas/Unauthorized'
        '403':
          $ref: '#/paths/~1v1~1scheduled-tasks/get/x-schemas/Forbidden'
        '404':
          description: Task not found (or no client matching `X-Client-Id`).
          content:
            application/json:
              schema:
                $ref: '#/paths/~1v1~1scheduled-tasks/get/x-schemas/Error'
        '409':
          $ref: '#/paths/~1v1~1scheduled-tasks/get/x-schemas/AmbiguousClient'
        '429':
          $ref: '#/paths/~1v1~1scheduled-tasks/get/x-schemas/RateLimited'
  /v1/scheduled-tasks/{id}/run:
    parameters:
    - name: id
      in: path
      required: true
      schema:
        type: string
    post:
      operationId: runScheduledTaskNow
      summary: Enqueue an immediate run
      description: 'Queues the task for immediate execution (asynchronous — the response confirms queueing,

        not completion). Takes no request body. Required scope: `tasks:write`.

        '
      tags:
      - scheduled-tasks
      responses:
        '200':
          description: Queued
          content:
            application/json:
              schema:
                type: object
                properties:
                  success:
                    type: boolean
                    enum:
                    - true
                  message:
                    type: string
                    example: Task queued for execution
        '400':
          $ref: '#/paths/~1v1~1scheduled-tasks/get/x-schemas/MissingTargetClient'
        '401':
          $ref: '#/paths/~1v1~1scheduled-tasks/get/x-schemas/Unauthorized'
        '403':
          $ref: '#/paths/~1v1~1scheduled-tasks/get/x-schemas/Forbidden'
        '404':
          description: Task not found (or no client matching `X-Client-Id`).
          content:
            application/json:
              schema:
                $ref: '#/paths/~1v1~1scheduled-tasks/get/x-schemas/Error'
        '409':
          $ref: '#/paths/~1v1~1scheduled-tasks/get/x-schemas/AmbiguousClient'
        '429':
          $ref: '#/paths/~1v1~1scheduled-tasks/get/x-schemas/RateLimited'
        '500':
          description: Failed to queue task run
          content:
            application/json:
              schema:
                $ref: '#/paths/~1v1~1scheduled-tasks/get/x-schemas/Error'
  /v1/scheduled-tasks/{id}/runs:
    parameters:
    - name: id
      in: path
      required: true
      schema:
        type: string
    get:
      operationId: listScheduledTaskRunsForTask
      summary: A task's run history
      description: 'Newest first. Required scope: `tasks:read` OR `tasks:write`.'
      tags:
      - scheduled-tasks
      parameters:
      - name: limit
        in: query
        required: false
        schema:
          type: integer
          default: 50
        description: Row cap. Non-numeric or 0 falls back to 50 (`Number(...) || 50`).
      responses:
        '200':
          description: Runs for this task
          content:
            application/json:
              schema:
                type: object
                required:
                - runs
                properties:
                  runs:
                    type: array
                    items:
                      $ref: '#/paths/~1v1~1scheduled-tasks/get/x-schemas/ScheduledTaskRun'
        '400':
          $ref: '#/paths/~1v1~1scheduled-tasks/get/x-schemas/MissingTargetClient'
        '401':
          $ref: '#/paths/~1v1~1scheduled-tasks/get/x-schemas/Unauthorized'
        '403':
          $ref: '#/paths/~1v1~1scheduled-tasks/get/x-schemas/Forbidden'
        '404':
          description: Task not found (or no client matching `X-Client-Id`).
          content:
            application/json:
              schema:
                $ref: '#/paths/~1v1~1scheduled-tasks/get/x-schemas/Error'
        '409':
          $ref: '#/paths/~1v1~1scheduled-tasks/get/x-schemas/AmbiguousClient'
        '429':
          $ref: '#/paths/~1v1~1scheduled-tasks/get/x-schemas/RateLimited'
  /v1/scheduled-tasks/{id}/status:
    parameters:
    - name: id
      in: path
      required: true
      schema:
        type: string
    put:
      operationId: setScheduledTaskStatus
      summary: Pause or resume a scheduled task
      description: 'Sets the task status. Resuming (`active`) also clears any auto-pause reason and resets
        the

        consecutive-failure streak. Required scope: `tasks:write`.

        '
      tags:
      - scheduled-tasks
      requestBody:
        required: true
        content:
          application/json:
            schema:
              type: object
              required:
              - status
              properties:
                status:
                  type: string
                  enum:
                  - active
                  - paused
      responses:
        '200':
          description: Status set
          content:
            application/json:
              schema:
                type: object
                properties:
                  success:
                    type: boolean
                    enum:
                    - true
                  status:
                    type: string
                    enum:
                    - active
                    - paused
        '400':
          $ref: '#/paths/~1v1~1scheduled-tasks/get/x-schemas/MissingTargetClient'
        '401':
          $ref: '#/paths/~1v1~1scheduled-tasks/get/x-schemas/Unauthorized'
        '403':
          $ref: '#/paths/~1v1~1scheduled-tasks/get/x-schemas/Forbidden'
        '404':
          description: Task not found (or no client matching `X-Client-Id`).
          content:
            application/json:
              schema:
                $ref: '#/paths/~1v1~1scheduled-tasks/get/x-schemas/Error'
        '409':
          $ref: '#/paths/~1v1~1scheduled-tasks/get/x-schemas/AmbiguousClient'
        '429':
          $ref: '#/paths/~1v1~1scheduled-tasks/get/x-schemas/RateLimited'
  /v1/skills:
    get:
      operationId: listSkills
      tags:
      - skills
      summary: List the client's visible skill library
      description: 'Returns the skill library the authenticated key''s client can see: platform

        skills, its agency''s skills, and its own, each row annotated with `scope`,

        `editable`, and `shadowed`. Ordered by name.


        Required scope: `skills:read` OR `skills:write` (requireAnyScope).

        Auth: API key (`Authorization: Bearer secret_xxx`). Agency-wide keys must

        also send `X-Client-Id`.

        '
      parameters:
      - name: X-Client-Id
        in: header
        required: false
        description: Target client id, slug, or name. Required for agency-wide API keys; ignored for client-scoped
          keys.
        schema:
          type: string
      responses:
        '200':
          description: The visible skill library
          content:
            application/json:
              schema:
                type: object
                required:
                - skills
                properties:
                  skills:
                    type: array
                    items:
                      allOf:
                      - $ref: '#/x-schemas/Skill'
                      - type: object
                        properties:
                          scope:
                            type: string
                            enum:
                            - client
                            - agency
                            - platform
                          editable:
                            type: boolean
                            description: True when the row is owned by this client.
                          shadowed:
                            type: boolean
                            description: True when a fork owned by this client (or its agency) hides this
                              row.
        '400':
          description: Agency-wide key did not resolve a target client
          content:
            application/json:
              schema:
                $ref: '#/x-schemas/Error'
        '401':
          description: Missing, malformed, revoked, or expired API key
          content:
            application/json:
              schema:
                $ref: '#/x-schemas/Error'
        '403':
          description: 'Permission denied: key lacks skills:read and skills:write'
          content:
            application/json:
              schema:
                $ref: '#/x-schemas/Error'
        '429':
          description: Rate limit exceeded
          content:
            application/json:
              schema:
                $ref: '#/x-schemas/RateLimitError'
    post:
      operationId: createSkill
      tags:
      - skills
      summary: Create a client-owned skill
      description: 'Creates a skill owned by the authenticated key''s client. Passing

        `forkedFromSkillId` records a copy-on-write fork of an inherited

        (platform/agency) skill: the fork keeps the source''s slug and shadows it

        in the library. Slug is derived from `name` and must be unique among this

        client''s own skills.


        Required scope: `skills:write` (requireScope).

        Auth: API key. Agency-wide keys must also send `X-Client-Id`.

        '
      parameters:
      - name: X-Client-Id
        in: header
        required: false
        description: Target client id, slug, or name. Required for agency-wide API keys.
        schema:
          type: string
      requestBody:
        required: true
        content:
          application/json:
            schema:
              type: object
              required:
              - name
              - description
              properties:
                name:
                  type: string
                  minLength: 1
                description:
                  type: string
                  minLength: 1
                  description: Should say WHEN to use the skill; a description without a trigger phrase
                    is accepted but discouraged.
                content:
                  type: string
                  nullable: true
                  description: The skill body (SKILL.md).
                config:
                  $ref: '#/x-schemas/SkillConfigInput'
                isEnabled:
                  type: boolean
                  default: true
                forkedFromSkillId:
                  type: string
                  nullable: true
                  description: Id of an in-scope, non-client-owned skill to fork (copy-on-write).
      responses:
        '201':
          description: Skill created
          content:
            application/json:
              schema:
                type: object
                required:
                - skill
                properties:
                  skill:
                    $ref: '#/x-schemas/Skill'
        '400':
          description: Validation failure thrown by the skill core (missing name/description, bad `config`,
            slug clash, fork source not found), or an agency-wide key with no resolvable target client.
          content:
            application/json:
              schema:
                $ref: '#/x-schemas/Error'
        '401':
          description: Missing, malformed, revoked, or expired API key
          content:
            application/json:
              schema:
                $ref: '#/x-schemas/Error'
        '403':
          description: 'Permission denied: key lacks skills:write'
          content:
            application/json:
              schema:
                $ref: '#/x-schemas/Error'
        '429':
          description: Rate limit exceeded
          content:
            application/json:
              schema:
                $ref: '#/x-schemas/RateLimitError'
  /v1/skills/{id}:
    parameters:
    - name: id
      in: path
      required: true
      description: Skill id.
      schema:
        type: string
    - name: X-Client-Id
      in: header
      required: false
      description: Target client id, slug, or name. Required for agency-wide API keys; ignored for client-scoped
        keys.
      schema:
        type: string
    get:
      operationId: getSkill
      tags:
      - skills
      summary: Get one in-scope skill
      description: 'Returns a single skill visible to this client (its own, or an inherited

        agency/platform skill), annotated with `scope` and `editable`.


        Required scope: `skills:read` OR `skills:write` (requireAnyScope).

        Auth: API key. Agency-wide keys must also send `X-Client-Id`.

        '
      responses:
        '200':
          description: The skill
          content:
            application/json:
              schema:
                type: object
                required:
                - skill
                properties:
                  skill:
                    allOf:
                    - $ref: '#/x-schemas/Skill'
                    - type: object
                      properties:
                        scope:
                          type: string
                          enum:
                          - client
                          - agency
                          - platform
                        editable:
                          type: boolean
        '400':
          description: Agency-wide key did not resolve a target client
          content:
            application/json:
              schema:
                $ref: '#/x-schemas/Error'
        '401':
          description: Missing, malformed, revoked, or expired API key
          content:
            application/json:
              schema:
                $ref: '#/x-schemas/Error'
        '403':
          description: 'Permission denied: key lacks skills:read and skills:write'
          content:
            application/json:
              schema:
                $ref: '#/x-schemas/Error'
        '404':
          description: Skill not found or not in scope for this client
          content:
            application/json:
              schema:
                $ref: '#/x-schemas/NotFoundError'
        '429':
          description: Rate limit exceeded
          content:
            application/json:
              schema:
                $ref: '#/x-schemas/RateLimitError'
    patch:
      operationId: updateSkill
      tags:
      - skills
      summary: Update a client-owned skill
      description: 'Sparse patch of a skill owned by the authenticated key''s client. Inherited

        platform/agency skills cannot be patched (they resolve to 404 here — fork

        them with POST /v1/skills instead). Renaming a non-fork re-derives its

        slug; a fork keeps its source''s slug. On success the agent cache is

        invalidated so edits take effect immediately.


        Required scope: `skills:write` (requireScope).

        Auth: API key. Agency-wide keys must also send `X-Client-Id`.

        '
      requestBody:
        required: true
        content:
          application/json:
            schema:
              type: object
              properties:
                name:
                  type: string
                  minLength: 1
                description:
                  type: string
                  minLength: 1
                content:
                  type: string
                  nullable: true
                config:
                  $ref: '#/x-schemas/SkillConfigInput'
                isEnabled:
                  type: boolean
      responses:
        '200':
          description: Skill updated
          content:
            application/json:
              schema:
                type: object
                required:
                - skill
                properties:
                  skill:
                    $ref: '#/x-schemas/Skill'
        '400':
          description: Validation failure thrown by the skill core (empty name/description, bad `config`,
            slug clash), a zod body validation error, or an agency-wide key with no resolvable target
            client.
          content:
            application/json:
              schema:
                $ref: '#/x-schemas/Error'
        '401':
          description: Missing, malformed, revoked, or expired API key
          content:
            application/json:
              schema:
                $ref: '#/x-schemas/Error'
        '403':
          description: 'Permission denied: key lacks skills:write'
          content:
            application/json:
              schema:
                $ref: '#/x-schemas/Error'
        '404':
          description: Skill not found among this client's own skills
          content:
            application/json:
              schema:
                $ref: '#/x-schemas/NotFoundError'
        '429':
          description: Rate limit exceeded
          content:
            application/json:
              schema:
                $ref: '#/x-schemas/RateLimitError'
    delete:
      operationId: deleteSkill
      tags:
      - skills
      summary: Delete a client-owned skill
      description: 'Deletes a skill owned by the authenticated key''s client. Equipped agents

        detach via the agent_skills cascade; there is no reference guard. On

        success the agent cache is invalidated.


        Required scope: `skills:write` (requireScope).

        Auth: API key. Agency-wide keys must also send `X-Client-Id`.

        '
      responses:
        '200':
          description: Skill deleted
          content:
            application/json:
              schema:
                type: object
                required:
                - success
                properties:
                  success:
                    type: boolean
                    enum:
                    - true
        '400':
          description: Delete failed in the skill core, or an agency-wide key with no resolvable target
            client.
          content:
            application/json:
              schema:
                $ref: '#/x-schemas/Error'
        '401':
          description: Missing, malformed, revoked, or expired API key
          content:
            application/json:
              schema:
                $ref: '#/x-schemas/Error'
        '403':
          description: 'Permission denied: key lacks skills:write'
          content:
            application/json:
              schema:
                $ref: '#/x-schemas/Error'
        '404':
          description: Skill not found among this client's own skills
          content:
            application/json:
              schema:
                $ref: '#/x-schemas/NotFoundError'
        '429':
          description: Rate limit exceeded
          content:
            application/json:
              schema:
                $ref: '#/x-schemas/RateLimitError'
  /v1/web/extract:
    post:
      operationId: extractWebPageData
      tags:
      - web
      summary: AI structured extraction from a web page
      description: 'Renders the URL and runs Cloudflare''s AI JSON extraction over it, guided by a

        natural-language `prompt`, a JSON Schema, or both. At least one of `prompt` or

        `schema` is required. Read-only external fetch; the resolved client is used only

        for usage attribution, rate limiting, and the monthly `web` browsing budget.


        Required scope: `web:read`.


        Auth: API key via `Authorization: Bearer secret_...`. Agency-wide keys must also

        send `X-Client-Id` (client id, slug, or name).

        '
      parameters:
      - name: X-Client-Id
        in: header
        required: false
        description: Target client (id, slug, or name). Required for agency-wide API keys.
        schema:
          type: string
      requestBody:
        required: true
        content:
          application/json:
            schema:
              type: object
              required:
              - url
              description: Must include `prompt` and/or `schema`.
              properties:
                url:
                  type: string
                  format: uri
                prompt:
                  type: string
                  description: What to extract, in natural language.
                schema:
                  type: object
                  additionalProperties: true
                  description: JSON Schema passed to Browser Run as `response_format.json_schema`.
                waitForSelector:
                  type: string
                  description: CSS selector to wait for before extracting.
      responses:
        '200':
          description: Extracted data
          content:
            application/json:
              schema:
                type: object
                required:
                - url
                - data
                properties:
                  url:
                    type: string
                    format: uri
                  data:
                    description: Extraction result from Cloudflare, passed through unchanged. Its shape
                      follows the caller's `schema` when one is supplied, and is otherwise model-determined.
        '400':
          description: 'Validation failure (including neither `prompt` nor `schema` supplied), an

            unfetchable URL, a render timeout (30s), the monthly `web` budget being

            exhausted, or web browsing not being configured. Also returned when an

            agency-wide key omits `X-Client-Id`.

            '
          content:
            application/json:
              schema:
                type: object
                required:
                - error
                properties:
                  error:
                    type: string
                  message:
                    type: string
        '401':
          description: Missing, malformed, revoked, expired, or unresolvable API key
          content:
            application/json:
              schema:
                type: object
                properties:
                  error:
                    type: string
                  message:
                    type: string
        '403':
          description: Key lacks the `web:read` scope
          content:
            application/json:
              schema:
                type: object
                properties:
                  error:
                    type: string
                  message:
                    type: string
        '404':
          description: '`X-Client-Id` does not match any client in the agency'
          content:
            application/json:
              schema:
                type: object
                properties:
                  error:
                    type: string
                  message:
                    type: string
        '429':
          description: Per-key rate limit exceeded
          content:
            application/json:
              schema:
                type: object
                properties:
                  error:
                    type: string
                  message:
                    type: string
                  retryAfter:
                    type: integer
  /v1/web/fetch:
    post:
      operationId: fetchWebPage
      tags:
      - web
      summary: Fetch a web page as markdown, HTML, or a link list
      description: 'Renders the URL with headless Chrome (Cloudflare Browser Run) and returns the page

        as markdown (default), raw HTML, or its extracted links. Read-only external fetch;

        no client data is read or written — the resolved client is used only for usage

        attribution, rate limiting, and the monthly `web` browsing budget.


        Required scope: `web:read`.


        Auth: API key via `Authorization: Bearer secret_...`. Agency-wide keys must also

        send `X-Client-Id` (client id, slug, or name).


        Content longer than 60000 characters is truncated (`truncated: true` in the response).

        Identical requests are served from a 5-minute cache.

        '
      parameters:
      - name: X-Client-Id
        in: header
        required: false
        description: Target client (id, slug, or name). Required for agency-wide API keys.
        schema:
          type: string
      requestBody:
        required: true
        content:
          application/json:
            schema:
              type: object
              required:
              - url
              properties:
                url:
                  type: string
                  format: uri
                  description: http(s) URL to render.
                format:
                  type: string
                  enum:
                  - markdown
                  - html
                  - links
                  default: markdown
                  description: Output shape. `links` returns the page's links instead of text.
                waitForSelector:
                  type: string
                  description: CSS selector to wait for before capturing the page.
      responses:
        '200':
          description: Page content
          content:
            application/json:
              schema:
                oneOf:
                - type: object
                  description: format = markdown | html
                  required:
                  - url
                  - format
                  - content
                  properties:
                    url:
                      type: string
                      format: uri
                    format:
                      type: string
                      enum:
                      - markdown
                      - html
                    content:
                      type: string
                    truncated:
                      type: boolean
                      description: Present and true only when the content was cut off.
                    hint:
                      type: string
                      description: Present only when truncated.
                - type: object
                  description: format = links
                  required:
                  - url
                  - links
                  properties:
                    url:
                      type: string
                      format: uri
                    links:
                      type: array
                      description: Links returned by Browser Run (shape passed through unchanged).
                      items:
                        type: string
        '400':
          description: 'Validation failure, an unfetchable URL, a render timeout (30s), the monthly

            `web` budget being exhausted, or web browsing not being configured for the

            deployment. Also returned when an agency-wide key omits `X-Client-Id`.

            '
          content:
            application/json:
              schema:
                type: object
                required:
                - error
                properties:
                  error:
                    type: string
                  message:
                    type: string
        '401':
          description: Missing, malformed, revoked, expired, or unresolvable API key
          content:
            application/json:
              schema:
                type: object
                properties:
                  error:
                    type: string
                  message:
                    type: string
        '403':
          description: Key lacks the `web:read` scope
          content:
            application/json:
              schema:
                type: object
                properties:
                  error:
                    type: string
                    example: Permission denied
                  message:
                    type: string
        '404':
          description: '`X-Client-Id` does not match any client in the agency'
          content:
            application/json:
              schema:
                type: object
                properties:
                  error:
                    type: string
                  message:
                    type: string
        '429':
          description: Per-key rate limit exceeded
          content:
            application/json:
              schema:
                type: object
                properties:
                  error:
                    type: string
                    example: Rate limit exceeded
                  message:
                    type: string
                  retryAfter:
                    type: integer
                    description: Seconds until the rate-limit window resets.
  /v1/web/scrape:
    post:
      operationId: scrapeWebPageElements
      tags:
      - web
      summary: Scrape elements from a web page by CSS selector
      description: 'Renders the URL and returns the elements matching each supplied CSS selector.

        Read-only external fetch; the resolved client is used only for usage attribution,

        rate limiting, and the monthly `web` browsing budget.


        Required scope: `web:read`.


        Auth: API key via `Authorization: Bearer secret_...`. Agency-wide keys must also

        send `X-Client-Id` (client id, slug, or name).


        The serialized element payload is truncated past 60000 characters

        (`truncated: true` in the response).

        '
      parameters:
      - name: X-Client-Id
        in: header
        required: false
        description: Target client (id, slug, or name). Required for agency-wide API keys.
        schema:
          type: string
      requestBody:
        required: true
        content:
          application/json:
            schema:
              type: object
              required:
              - url
              - selectors
              properties:
                url:
                  type: string
                  format: uri
                selectors:
                  type: array
                  minItems: 1
                  description: One or more CSS selectors to extract.
                  items:
                    type: string
                    minLength: 1
                waitForSelector:
                  type: string
                  description: CSS selector to wait for before scraping.
      responses:
        '200':
          description: Scraped elements
          content:
            application/json:
              schema:
                type: object
                required:
                - url
                - elements
                properties:
                  url:
                    type: string
                    format: uri
                  elements:
                    type: array
                    description: Per-selector scrape results from Browser Run, passed through unchanged.
                    items:
                      type: object
                      additionalProperties: true
                  truncated:
                    type: boolean
                    description: Present and true only when the payload was cut off.
        '400':
          description: 'Validation failure, an unfetchable URL, a render timeout (30s), the monthly

            `web` budget being exhausted, or web browsing not being configured. Also

            returned when an agency-wide key omits `X-Client-Id`.

            '
          content:
            application/json:
              schema:
                type: object
                required:
                - error
                properties:
                  error:
                    type: string
                  message:
                    type: string
        '401':
          description: Missing, malformed, revoked, expired, or unresolvable API key
          content:
            application/json:
              schema:
                type: object
                properties:
                  error:
                    type: string
                  message:
                    type: string
        '403':
          description: Key lacks the `web:read` scope
          content:
            application/json:
              schema:
                type: object
                properties:
                  error:
                    type: string
                  message:
                    type: string
        '404':
          description: '`X-Client-Id` does not match any client in the agency'
          content:
            application/json:
              schema:
                type: object
                properties:
                  error:
                    type: string
                  message:
                    type: string
        '429':
          description: Per-key rate limit exceeded
          content:
            application/json:
              schema:
                type: object
                properties:
                  error:
                    type: string
                  message:
                    type: string
                  retryAfter:
                    type: integer
